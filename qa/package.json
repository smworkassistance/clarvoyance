{
  "name": "clarvoyance-qa",
  "private": true,
  "version": "1.0.0",
  "description": "Automated regression gate for the Clarvoyance single-file app (Playwright: Chromium + WebKit/iPhone)",
  "scripts": { "test": "playwright test" },
  "devDependencies": { "@playwright/test": "1.55.0" }
}
