# Clarvoyance — Project Blueprint

> This file is the single source of truth for architecture, data flow, and decisions.
> Update it whenever a significant feature is added, changed, or removed.
> Claude reads this at the start of every session.

---

## What is Clarvoyance?
A self-development app (mobile-first PWA). Core idea: the user opens it daily, chats with Clar AI companion, gets a personalised Vibe Feed of micro-exercises, and builds a streak. Tools, chargers, goals, quotes, and modules are layered on top. Mood is handled naturally through Clar conversation (not a separate UI element).

Hosted on GitHub Pages. Single HTML file. No build system.
Repo: https://github.com/smworkassistance/clarvoyance

### Product Philosophy
> Clarvoyance optimizes for daily micro-action over comprehensive tracking. Every interaction must be completable in under 2 minutes. The app is mood-first, not goal-first — it meets the user where they are emotionally, not where they think they should be. It should feel like a trusted friend checking in, not a productivity coach demanding output. Every new feature must pass this test: does it lower friction or add it?

---

## Tech Stack
| Layer | What |
|-------|------|
| Frontend | Single HTML file (vanilla JS, CSS, no frameworks) |
| Backend / Content | Google Sheets + Apps Script Web App (JSON API) |
| Hosting | GitHub Pages (index.html = latest stable version) |
| Storage | localStorage (all user data) |
| Analytics | GA4 (G-BZZMW9B8SN) + Microsoft Clarity (wydll6jrxn) |
| PWA | sw.js (cache version clv-v216), manifest.json, beforeinstallprompt — **bump CACHE_VERSION on every release** |

---

## Browser Testing Setup (Chrome DevTools MCP)
As of v175, Claude has a working local end-to-end test loop for auth/Supabase flows — no GitHub push required to test real Google sign-in.

- **MCP server**: `chrome-devtools` registered via `claude mcp add chrome-devtools --scope user -- npx chrome-devtools-mcp@latest --browserUrl=http://127.0.0.1:9222`.
- **Chrome must be launched with a non-default profile** — since Chrome 136, `--remote-debugging-port` is silently ignored on the default user-data-dir (security hardening). Use a dedicated profile dir instead: `chrome.exe --remote-debugging-port=9222 --user-data-dir="C:\Users\LENOVO\ChromeDebugProfile"`. Sign into Google manually (a real human action) once in that profile — after that it stays signed in and CDP can drive it.
- **Local test server**: for OAuth-dependent flows, copy the target HTML to a scratch folder, change the hardcoded `REDIRECT_URL` constant (search `var REDIRECT_URL`) to `http://localhost:8787/`, and serve it with a tiny Node static server — must strip query strings from `req.url` (via `url.parse(req.url).pathname`) or the OAuth redirect landing page (`/?error=...` or a clean `/?code=...`) 404s.
- **Supabase allow-list**: add `http://localhost:8787/` to Redirect URLs at `https://supabase.com/dashboard/project/unvwjuceuyruqdnmvxlc/auth/url-configuration` — purely additive, doesn't touch the production redirect URL.
- This setup is what caught the "Manual linking is disabled" bug in the v175 entry above — worth reaching for again whenever touching auth, Supabase sync, or anything that only manifests with a real signed-in session.

---

## File Versioning Convention
```
clarvoyance_v[MAJOR].[MINOR]_[description]_[STATUS].html
STATUS: TESTING | STABLE
```
- `index.html` = always the latest pushed stable/testing version (what users see on GitHub Pages) — currently **v227**
- Never edit old version files — always create a new version
- After pushing a new version, always copy it to index.html and push that too

**Version history:**
| Version | What was added |
|---------|---------------|
| v38 | Language selection onboarding |
| v39 | Mood onboarding |
| v40 | XP system |
| v41 | Tool registry system (dynamic file-based loader) |
| v42 | Google Sheets integration + Mind Chargers tab |
| v43 | Full Sheets integration (tools, modules, quotes, revise) |
| v44 | Vibe feed redesign foundation |
| v45 | Nickname onboarding, full vibe feed rebuild (7 card types), tools from Sheets |
| v46 | Vibe feed cards moved from hardcode → Google Sheets |
| v47 | Daily quest home-only, NN on home screen, NN edit + subpoints, tab swap |
| v48 | UI polish — tools grid, XP display size, tray layout, skip hint, next btn removed |
| v51 | Full visual redesign — Unified Material Design style (Claude Design output) |
| v52 | Clar AI companion chat — Gemini 2.5 Flash via Cloudflare Worker proxy |
| v53 | Intelligent Clar AI — full context system prompt, user profile, daily summaries, auto-profile update |
| v56 | Clar persona selection — 4 styles (Best Friend, Wise Guide, Coach, Nurturer) in onboarding + profile |
| v57 | Clar conversation flow — 3-phase model, Talk/Act session intent chips, card reason rendering |
| v58 | Search fix (no-results msg, reset on tab switch & back), My Quotes collapsible with count badge, language screen removed from onboarding, charger click bug fix (always opened topmost) |
| v59 | Chargers redesign — two-level inline accordion (categories + chargers expand in-place), icon in each row, auto-XP on word count, fullscreen button, no separate writer screen, no "I'm Charged" button, hairline list layout, Apple-style sticky search |
| v60 | Tools tab same accordion layout as chargers — categories from `category` column in tools sheet (no new Sheets tab), fullscreen heading highlight (gold), old grid/writer/toolSearch removed |
| v61 | Physical practice tools — read-only steps, duration chips (30s/1m/2m/3m/5m/10m), countdown timer, XP on completion. Default start tab → Clar AI |
| v62 | Sheets fetch: 30s timeout + retry button + SHEETS_FAILED flag + sheetsDataFailed event. New Apps Script URL (CORS fix via redeploy) |
| v63 | Fix blank vibe feed on start (vfInit re-run after sheetsDataReady), enforce Clar AI as start tab, updNNBadge null check |
| v64 | Mood UI removed — feeling chip, XP chip, emotion ribbon, mood start screen all gone. Clar AI handles mood naturally in conversation |
| v65 | Type scale fix — charger/tool category and item names use Inter Tight (matching daily quest font) instead of Cormorant Garamond |
| v66 | PWA install prompt + service worker auto-update (sw.js v66 → v67 isGenuineUpdate fix) |
| v67 | PWA UX — Later button replaces ✕ dismiss, persistent Install row in Profile tab, CSS fix (#pwa-prompt → #pwa-card), pwaInstallFromProfile() |
| v68 | PWA UX polish — pwa-card display:none on load (no flash), update toast removed, Profile App section: Install row + Updates row (Check / Up to date / Tap to update) |
| v69 | PWA fixes — how-to sheet proper UI (handle, numbered steps, styled), install card only on home/chat tab, red dot on profile chip when update ready |
| v70 | PWA clean — manual Check button removed, prof-update-row appears only on SW_UPDATED, app section auto-hides when installed+no-update |
| v71 | Analytics — GA4 (G-BZZMW9B8SN) + Microsoft Clarity (wydll6jrxn). Events: app_open, tab_view, vibe_card_complete/skip, xp_gained, clar_session_start, clar_message_sent, tool_open, charger_open, pwa_install_tapped/later |
| v72 | Offline support — sw.js v68 caches Sheets Worker response; 3-layer fallback: localStorage → SW cache → retry button; offline bar shown when serving stale data |
| v73 | Offline notifications — bar slides in on network loss, hides on reconnect + Sheets auto-refresh; Clar shows inline offline message instead of failed API call |
| v74 | Offline bar UI — Clar gold theme (var(--acc)), minimal "✦ Offline" text, uppercase
| v75 | Profile App section — always shows version (v75) + status + Check/Reload button; _setUpdateStatus() exposed globally |
| v76 | Transparent offline bar (frosted glass, gold tint) + Connect card in Profile (WhatsApp +917387400467, Instagram @beyond._thought) with official SVG brand icons |
| v77 | Profile redesigned with 4 tabs: Identity (photo upload via file picker → base64 localStorage, name/gender/age/city), Clar (persona + context merged), Updates (v77 version + check), Contact Us (WhatsApp/Instagram) |
| v78 | Profile tab labels: Identity→Profile, Clar→Clar AI; Tools category names (Mental Reset/Physical Reset) text color changed from gold to ink (matching charger rows) |
| v79 | Fix update detection — sw.js CACHE_VERSION clv-v68→clv-v79 (root cause: browser never saw a changed sw.js so SW_UPDATED was never sent); pwaCheckForUpdate now checks reg.waiting first, listens to updatefound event, 5s timeout |
| v80 | Home redesign: mood chips + goal prompt removed; Clar Commitment card (expandable, 200 words=+100XP); Chargers/Tools/Revise embedded on home via CSS flex ordering; quest moved to bottom; bottom nav → 4 tabs (Home/Vibe/Goal/Clar AI); Goal tab: Short Term Goals + Issues & Resolutions sections |
| v81 | Home fix: Chargers/Tools/Revise shown as nav card shortcuts on home (not full content); NN progress bar removed from home-widget (XP only); visibility leak fixes — clv-commitment/home-nav-cards/short-goals/issues-section added to bnavSwitch all[] array; Chargers/Tools/Revise restored as proper bottom nav tabs (7 tabs); sw.js CACHE_VERSION bumped every release |
| v82 | Greeting redesign — two-line layout (✦ LABEL small-caps + Name 2.4rem Cormorant serif, solid ink color, no invisible gradient clip); Chargers/Tools/Revise removed from bottom nav (display:none + removed from MODULE_TAB_MAP so applyModules can't un-hide them); NN section removed from home tab; Short Term Goals + Issues use goal-row card style; Curated Quotes in Revise = single container with hairline separators |
| v83 | Home reorder — Vibration Score widget removed from home; Clar Commitment → top (order:2 below greeting); Non-Negotiables → bottom of home (nnTabSec added to map.home, order:10); Greeting → single line inline ("✦ QUIET HOURS, Videh" flex-row align-items:baseline, name 1.9rem serif italic) |
| v84 | Chat UI redesign — WhatsApp-inspired: persona avatar header (✦ circle + "Clar" serif italic + persona label e.g. "your best friend"), warm-tinted message area, borderless assistant bubbles (white + micro-shadow), pill input + circular send button, tight 4px gap with auto 10px gap on sender switch, typing dots match bubble style |
| v85 | Chat full-screen (global header hidden on chat tab); Persona color system — Sienna Coral/bestfriend, Viridian/guide, Cobalt/coach, Amethyst/nurturer — applied to avatar gradient + user bubbles + persona chip active colors; Clar header tappable → opens Clar AI settings pane with ← Back to Clar button; body[data-persona] attribute updated on persona select + initChat |
| v86 | Chat fixes — (1) blank white space removed (padding-top:60px→0 since header hidden); (2) persona color moved to Clar's bubble (light tint + left accent stripe), user bubble back to consistent gold; (3) keyboard fix — on chat input focus: bottom nav hidden + chat extends to bottom:0, restores on blur |
| v87 | Bubble color swap (user=white+shadow, Clar=full persona color+white text); input box border-radius:24px forced on :focus so shape stays pill after keyboard opens; profile-section made position:fixed full-screen overlay (z-index:200) so it covers any tab cleanly |
| v88 | Send button white (background:var(--su), persona-colored icon); Clar AI nav tab icon changed from 💬 → ✦ serif; profile tab bar fixed to 4 equal 25% tabs (flex:0 0 25%, font-size:.72rem, "Contact Us" → "Contact") |
| v89 | Profile overlay z-index fix — bumped from 200 → 1000 so it correctly covers the global header (z-index:500) when opened from Home/Goal/any non-chat tab |
| v90 | Profile section redesigned as normal tab — removed position:fixed overlay; header + bnav stay visible when profile is open; content scrolls naturally between them |
| v91 | Profile added to bnavSwitch system — openProfileTab() routes through bnavSwitch(fakeEl); _doOpenProfile() handles data loading; map.profile=[profSec] ensures only profile content shows, no stray home/goal elements |
| v92 | One-time user registration form in onboarding (after nickname, before persona) — fields: name, email*, purpose* (chips), gender, age group, phone. Saves to clv_user_identity so profile auto-fills. clv_registered flag prevents re-showing. Fire-and-forget POST to Sheets Worker. |
| v93 | Reg form fixes — proper email regex (/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/), country code dropdown (16 countries, +91 default) + phone input side by side, country text field added; all saved to clv_user_identity |
| v105 | Fix Clar AI snapping back — removed _enforceStartTab 600ms/1500ms timers that kicked users back to chat tab after navigating away |
| v106 | Peak State card added to Self tab (first accordion item) — "I Am That I Am" tagline, rampage textarea, 68-second hold timer with SVG ring, +15 XP on completion (once/day) |
| v107 | Clar AI context expanded — Self tab practices with full WHY+WHEN descriptions injected into system prompt; manifestation_steps sheet injected (title+why per step); new MANIFEST card type Clar can suggest that opens the Manifestation Protocol overlay |
| v112 | My Foundation overlay — "✦ Foundation" button next to Manifest in Goal header; bottom-sheet overlay with "Why I Started" textarea + "My Tribe" multi-entry inspirations (name, category, @handle, link); saved to clv_my_why + clv_my_tribe in localStorage |
| v113 | My Personal Space — free-flow writing accordion at top of Self tab (above Peak State); charger-style layout with fullscreen button; text persists to clv_myspace_text in localStorage |
| v114 | True View Chart — perspective shifter / thought mold in Self tab; two colored zones (Story 1 pink=Illusion, Story 2 green=Pure Abundance) with annotation chips; vertical frequency bar animates as Story 2 fills; 68-second Truth Lock (+20 XP); saves to clv_tv_story1 + clv_tv_story2 |
| v115 | Pure Abundance — full-screen overlay in Self tab (opens with Back button, not accordion); two concentric zones: Cerulean outer (Infinite Intelligence — I Am the Infinite) + Silver Grey inner ring (Resistance, 50% smaller via 22% margin); English named colors with emotional associations as color tags; 68-sec absorption lock (+20 XP once/day); saves to clv_pa_field + clv_pa_ring |
| v116 | Visual skin overhaul — Apple-inspired design (SF Pro system font stack, bigger font sizes, frosted glass bottom nav); full English colour palette: 24 named colours (Racing Green → Stone) + Classic & Dark presets; accent selection auto-generates tint variants via JS colour math (_applyAccent); XP/streak elements use var(--acc) not hardcoded amber; all italic/Cormorant fonts removed from card content; theme saved to clv_theme in localStorage |
| v117 | "Where are you right now?" check-in moved from Self tab → Profile Updates tab (above Contact); Goal tab dark-canvas colors replaced with theme-consistent surface (color-mix acc+su); dark preset still restores immersive dark look for goal-row |
| v118 | "Where are you right now?" removed entirely; Goal tab: 3 sections (Major Definite Goal, Issues & Resolutions, General Manifestation) converted to single self-acc accordion with hairline separators + click-to-expand; issue/GM list items changed to hairline-separated rows (no card boxes); Add Link removed from Major Definite Goal; goalToggle() function added |
| v119 | PWA mobile overflow fixes — html+body overflow-x:hidden prevents horizontal page scroll; bnav overflow:hidden stops nav from being scrollable/pannable; header timer (tmr) scaled down so profile chip stays visible; vibe feed topbar score-wrap margin-left:auto so XP number always stays on right |
| v120 | Supabase data layer — Supabase JS CDN + anon auth; `_fetchFromSupabase()` queries all 11 content tables in parallel; `_fetchContentData()` single entry point (Supabase primary → Sheets Worker fallback, isolated for easy removal); `db/schema_v120.sql` contains full CREATE TABLE + RLS + INSERT for all 11 tables |
| v121 | User data sync — 6 Supabase user tables (user_profile, user_progress, user_goals, user_nn, user_clar, user_revise); anonymous auth; pull on load, push on every save; hooks into xpAdd, sv(), issSave, gmSave, profile save, chat save, persona + theme change; `db/schema_v121.sql` |
| v122 | Google OAuth — "Continue with Google" button in Profile Identity tab; anonymous → Google upgrade via linkIdentity (preserves user_id, no migration); auth state listener updates UI; one-time nudge after first Clar exchange; sign-out; Google name + photo shown when signed in |
| v123 | Profile account section redesign — standard app-style (iOS Settings pattern) at top of Identity pane: avatar circle + name + green sync dot when signed in, person icon + "Continue with Google" button when signed out; old sync-card removed; profile tab overflow-x fixed (screen no longer pans horizontally in PWA) |
| v124 | Sign-out data race fix — `_pullUserData()` snapshots uid before await and discards results if user signed out mid-flight (prevents 30-second slow clear); sign-out now calls `_doOpenProfile()` + `xpRefreshAll(0)` immediately so form empties instantly; sign-in calls `_doOpenProfile()` after pull so new user's data appears without navigating away |
| v125 | Streak upgrade — `clar_streak` (int, hard-reset) replaced by `clv_momentum` (float, 5%/day decay); 1 weekly freeze (user-prompt toast on miss); Clar time ≥20 min = no decay, ≥10 min = half decay; 7/14/21-day MILESTONE vibe cards (+20 XP, injected once per threshold); Clar daily usage tracked per-tab via focus events in `clv_clar_times`; time label shown in chat header; `#xp-streak-num` fixed (was always showing 0 in v124); `db/schema_v125.sql` adds `momentum`, `freeze_week`, `freeze_used`, `clar_daily_min` to `user_progress` |
| v126 | Mirror section on Home — weekly AI reflection via Gemini (same Cloudflare Worker); shows after ≥2 Clar summaries; collapsible card with personal reflection, two-path prediction (keep going / pause), auto-cached per day in `clv_mirror_cache`; ✦ Share your journey: Canvas 540×540 achievement card (streak, XP level, AI achievement line, watermark) with Save Image + native share sheet; daily log: `user_daily_log` Supabase table (`db/schema_v126.sql`) inserted once/day on app open |
| v127 | Mirror renamed → **Pulse** (AI Progress Tracker); fixes: Gemini `systemInstruction` format corrected, `responseMimeType` removed, robust JSON extraction (handles markdown fences + raw JSON), threshold lowered to ≥1 summary; new features: instant mood chip (Thriving/Building/Steady/Check in) computed from Clar time + momentum trend + quest done — no AI needed; 3 metric cards (Clar min, Streak, NN Done) with 7-day sparkline graphs (bar + line canvas); AI response expanded to include `recommendation` field → "💡 Right now" card; momentum history stored in `clv_momentum_history` (30-day rolling) for trend graphs; error message changed to "Tap ↺ to retry" |
| v138 | PWA + startup fixes (clean rewrite from v136) — (1) overscroll-behavior:none on html+body prevents rubber-band/page-pan without touching layout; (2) vibe-feed hidden in HTML + bnavSwitch(chat) runs before await load() so Clar AI appears immediately with no vibe-feed flash; (3) Fortuneteller full-screen fixed: position:fixed;inset:0 0 65px 0;overflow-y:auto like vibe feed; header+xp-bar hidden via body.fortune-tab; fortune-tab class toggled in bnavSwitch; canvas height uses sec.offsetHeight; (4) Header: overflow:hidden + logo-wrap min-width:0 keeps profile chip in bounds; @380px breakpoint hides profile name text |
| v139 | Startup speed — (1) `supabase-js@2` CDN script made `defer` so it no longer blocks HTML rendering (was causing blank white screen while ~200KB SDK downloaded synchronously); (2) `registry.js` also made `defer`; (3) main boot moved from `window.load` → `DOMContentLoaded` so Clar AI tab appears at earliest possible moment |
| v140 | Zero-flash startup — added `display:none` to 9 tab sections that were visible by default in HTML (`#dq-card`, `#home-widget`, `#clv-commitment`, `#home-nav-cards`, `#goal-row`, `#rr-quotes-section`, `.rr-row`, `#tl-main`, `#chargers-section`); these flashed on screen before `bnavSwitch(chat)` ran; now the page starts blank and only chat section appears |
| v141 | UI polish batch — (1) dark/light toggle removed from header bar (still accessible in Profile); (2) red NN dot hidden permanently from header; (3) tagline "clarity for the becoming" font-size reduced .76rem→.60rem; (4) Fortune tab gets eye SVG icon in NAV_ICONS; (5) Fortune section `overflow-x:hidden` stops sideways drift; (6) Fortune card text Apple typography (.ft-card-text system font stack, 1.08rem, .ft-card-lbl .72rem, notice text .88rem); (7) Fortune page title "Clairvoyance"→"Oracle", energy label "energy reading"→"field signal"; (8) Clar AI `body.chat-tab` class now set on `_hookNav` initial load so keyboard-focus handler correctly hides bottom nav; (9) Vibe feed multicolor/gold toggle removed — multicolor hardcoded forever (`vfSetStyleMode` init always writes 'multi'); (10) `.vf-score-wrap` gets `flex-shrink:0;margin-left:auto` so XP stays visible; (11) Pulse fix — `card.classList.add('open')` moved before `_renderMetrics()` so `.pulse-body` is visible when canvas draws; setTimeout 50ms→150ms |
| v142 | Fix 3 items from v141 that didn't land — (1) tagline: two more `!important` overrides at skin layer (cv-v50 style block + inline rule after `</body>`) changed to `.60rem!important`; (2) chat input: `padding-bottom:calc(16px + env(safe-area-inset-bottom,0px))` on `.chat-input-wrap` handles iPhone X home indicator; `overflow:hidden` on chat section cssText prevents content spilling past `bottom:65px`; (3) Pulse clarMin fix: clar timer hook now calls `_stop()` BEFORE `orig.apply()` so time is committed to localStorage before `pulseLoad()→_renderMetrics()` reads it; `Object.values` replaced with `Object.keys` loop in both NN done calculations for older Android compat |
| v143 | Startup flash fix — root causes: (1) home bnav button had `class="active"` hardcoded in HTML so it appeared highlighted before JS ran; (2) `bnavSwitch(chat)` only fired at DOMContentLoaded which is blocked by `defer` supabase-js download (2-3s on mobile). Fix: chat button now has `active` in HTML; chat-section starts with full `position:fixed` CSS (not display:none) so it's visible from first paint; synchronous inline script at end of `<body>` calls `bnavSwitch(chat)` immediately (before defer scripts) to activate chat tab and deactivate home button |
| v144 | Icon/wordmark flash fix — `swapNavIcons()` and `swapWordmark()` only ran at DOMContentLoaded so emoji icons (🏠🎯etc.) flashed before SVG icons replaced them. Fix: both calls added to the end-of-body early script (v143) so icons render correctly from first paint; colour theme was already handled by existing early IIFE |
| v146 | Supabase sync for Pulse data — `clv_clar_times` (7-day session array) + `clv_momentum_history` (30-day) added to `user_progress` as JSONB; `clv_monthly_summaries` (Fortune 6-month AI meta) added to `user_clar`; push hooks: `_syncPushProgress()` called from `_saveMomentum()`, `_syncPushClar()` called when monthly summary saved; pull restores all three; `_clearUserLocalStorage()` now correctly clears them (restorable); `db/schema_v146.sql` has the 3 ALTER TABLE statements |
| v147 | Vision image cross-device sync — uploaded photos go to Supabase Storage bucket `vision-images` (`{uid}/{timestamp}.jpg`), URL replaces base64; pasted URLs stored directly; `vis_images` JSONB column in `user_goals` stores `[{src, note}]`; pull merges remote images not already on device; delete removes from Storage too; migration auto-runs on first launch (uploads any existing base64 photos); `db/schema_v147.sql` has ALTER TABLE + bucket + RLS |
| v148 | Destiny/Free Will onboarding screen — new first screen shown before nickname (once ever, key `clv_destiny_seen`): "Destiny or Free Will?" with two choices. Free Will → commitment text ("practice religiously... no shortcuts") + "I'm in" → saves `clv_life_choice=freewill`. Destiny → "That's fine, most people do" + two options: "I change my mind" (switches to Free Will commitment panel) or "I'll come back later" (saves `clv_life_choice=destiny`, continues normally). Daily re-engagement ritual added: first app-open of each day (tracked via `clv_ritual_date`) shows a full-screen swipe visual — dark "struggle/confusion/doubt/stuck" layer swipes away (drag gesture) to reveal a golden "clarity/flow/ease/power" layer with "Which one are you living today?"; every re-open later the same day instead shows a one-line tap-to-dismiss jhatka: "Has destiny grasped you, or have you grasped your destiny?" No push notifications involved — purely triggered by organic app opens. Hooks added in `_checkPersona()`/`selectPersona()` only; no other onboarding/init logic touched |
| v149 | Ritual polish + testing mode — (1) jhatka line now ends with "Be religious in your practices — all else will be taken care."; (2) swipe screen richer: 6 floating words per layer (was 4), pulsing glow orb + breathing "OR" behind center content, vignette on the struggle layer, and a drag-progress fill bar under the swipe arrow; (3) golden reveal screen now has an explicit "Continue ✦" button (`ritualDismissSwipe()`) instead of relying on invisible tap-anywhere; (4) **TESTING ONLY** — `_checkDailyRitual()` moved from the onboarding-gated call sites (`_checkPersona()`/`selectPersona()`, now just comments) to fire directly in `langInit()` on every app start, so the ritual can be reviewed without completing the full onboarding flow each time — revert before general release |
| v150 | Fix cross-device data loss on Google sign-in — root cause: `_signInWithGoogle` called plain `signInWithOAuth()`, which does not preserve the current anonymous `user_id`; it either creates a brand-new Supabase user (orphaning everything saved anonymously, e.g. goal vision images) or switches to a different existing row, so two devices signing into the *same* Google account ended up as different, disconnected accounts (confirmed via Supabase Table Editor showing 5 separate `user_id` rows in `user_goals`, several with their own independent `vis_images`). Fixed by using `sb.auth.linkIdentity()` first, which attaches the Google identity to the current session's user_id instead of starting a fresh sign-in; falls back to `signInWithOAuth()` only when that Google account is already linked to a different user (e.g. a second device), so it correctly joins that existing account instead of staying orphaned. New `_handleAuthRedirectError()` catches the "already linked" conflict on the post-redirect return URL and retries with plain sign-in. Note: the 5 pre-existing orphaned rows in `user_goals` are not auto-merged — only sign-ins going forward are fixed |
| v151 | Revise & Repeat tab reorder — the `.rr-row` images/media block (upload, image URL, doc links, image grid) moved above `#rr-quotes-section` (My Quotes & Thoughts) and `#sheets-revise-section` (Philosophy/curated quotes/learning channels), so images now show first. Pure DOM reorder — no CSS/JS logic touched, `map.revise` membership unchanged |
| v152 | Revise & Repeat image cross-device sync — mirrors the v147 vision-image pattern exactly: new `revise-images` Supabase Storage bucket (`{uid}/{timestamp}.jpg` per-user folders, same RLS as vision-images); new `rr_images` JSONB column on `user_revise`; `_uploadRRToStorage`/`_syncRRImagesToStorage`/`_deleteRRFromStorage` added alongside the existing vision equivalents; `sv('rr')` now pushes via `_syncPushRevise()`, `delRRItem()` deletes from Storage, `handleUpload()`'s rr branch triggers the base64→Storage upload, `_buildRevisePayload()` includes `rr_images`, and `_pullUserData()` merges remote `rr_images` into `S.rrItems` on load; migration for pre-existing base64 RR images runs the same way as vision images (2s after pull). `db/schema_v152.sql` has the ALTER TABLE + bucket + RLS — **must be run in the Supabase SQL editor for this to work** |
| v153 | Fix Goal-tab vision image strip (`_renderStrip`) — (1) delete button was only shown via `mouseenter`/`mouseleave`, which never fires on touch, so it never appeared on mobile; now always visible; (2) the continuous-loop scroll (`_wireEndlessScroll`, already fully written) was never actually called and the strip only rendered each image once, so there was nothing to loop between — now renders 3 back-to-back copies (when count>1) and wires up the endless-scroll reset so it loops seamlessly in both directions |
| v154 | Fix loop animation + cross-device delete sync for vision/RR images — (1) `.goal-row .vis-grid` has `scroll-behavior:smooth` in CSS, so the v153 loop-reset jumps animated visibly instead of snapping instantly; new `_jumpScroll()` forces `scroll-behavior:auto` for just the corrective jump; (2) `_buildGoalsPayload()`/`_buildRevisePayload()` only sent `vis_images`/`rr_images` to Supabase when at least one image remained, so deleting the last image never told Supabase anything changed — now always sent, even as `[]`; (3) the pull-merge in `_pullUserData()` was additive-only (only ever added remote images, never removed ones missing remotely), so deletions never propagated to other devices even when the push worked — now remote is treated as authoritative for already-synced (URL) images, while local base64 images not yet uploaded are preserved. **Requires `db/schema_v152.sql` to have been run** — until then, `user_revise` pushes (now always including `rr_images`) will fail if the column doesn't exist |
| v155 | Fix vision image strip still not looping — root cause found: the strip lives inside the "Major Definite Goal" accordion body (`#goal-body-major`), which starts `display:none`; the strip is built once at load time while hidden, so `offsetWidth` reads 0 and all of `_wireEndlessScroll`'s distance math (added in v153/v154) was permanently miscalibrated since `goalToggle()` never re-rendered on open. Fix: `goalToggle('major')` now calls `renderVisGrid()` when opening, rebuilding the strip with correct, visible measurements |
| v156 | Global Search — search icon added to header (between logo and profile chip, `openGlobalSearch()`); opens a full-screen overlay indexing Tools, Chargers, Self-tab practices (Peak State/SSW/Non-Doing Timer/Walk & Talk/Protect Yourself/Personal Space), Revise & Repeat (philosophy, curated quotes, learning channels, My Quotes & Thoughts), and Goal content (Major Goal, Issues & Resolutions, General Manifestation) — plain substring match across name/description/`ai_best_for` fields (mid-phrase matching, no library needed at this content volume). Tapping a result switches tabs via the existing `bnavSwitch` fake-element pattern and auto-expands the right accordion section (`goalToggle`/`selfToggle`) or prefills the Chargers tab's own search (`chSearch`). Also added "Ask Clar" — a separate one-shot intent search (e.g. "I need to feel better badly") that calls the same Cloudflare Worker but with its own independent prompt (`_buildSearchAskPrompt`, module `cv-global-search`) — **does not read or modify `_buildSystemPrompt`/the chat script in any way**, by design, per explicit request not to risk Clar AI's conversational behavior |
| v157 | Self tab gets two new quick-access rows at the top ("Revise & Repeat" via new `_goRevise()` helper that jumps tabs, "My Foundation" via `foundationOpen()` with the default context) above True View Chart/Pure Abundance/Personal Space etc. Also: Pulse moved out of Home entirely into the Fortune tab — relocated the `#pulse-card` DOM block to sit right after Fortune's energy-reading divider (before the AI prediction cards), restyled every var()-based color to Fortune's fixed dark-cosmic palette (`rgba(180,130,255,..)` purple / `#f0c987` gold / `rgba(255,255,255,..)` text) since it no longer needs to support the light/dark home theme; `mirrorCard` moved from `map.home` to `map.fortune` and the `pulseLoad()` call moved from the home tab handler to the fortune tab handler |
| v158 | Removed the "Revise & Repeat" shortcut row from Home's nav cards (`#home-nav-cards`) — redundant now that it's in the Self tab (v157). Fixed image delete on Revise & Repeat: `renderRRGrid()`'s delete button shares the `.ss-del` CSS class with the old (pre-v153) vision slideshow renderer, which was hover/`:active`-gated and therefore effectively unusable on touch — same root cause as the Goal vision strip bug fixed in v153, now fixed at the shared CSS class level so it covers both call sites |
| v159 | Harden Google sign-in account consolidation — root cause found for accounts still fragmenting across devices even after v150: the "identity already linked to another user" conflict doesn't always surface as a parseable error in the post-redirect URL, so `_handleAuthRedirectError()` sometimes missed it and the device silently stayed anonymous instead of falling back to a plain sign-in. New `_checkLinkFallback()` sets a `clv_google_link_pending` flag right before calling `linkIdentity()`; on the next load, if that flag is set and the session is still anonymous, it means the link silently failed, so it retries with a plain `signInWithOAuth()` instead of relying on error-text parsing alone |
| v160 | **AI Logic Console** (Phase 1) — new "🧠 AI Logic" tab in `admin.html`, Supabase-native (bypasses the old Sheets/Apps Script path entirely). `ai_context` table extended with `display_name`, `image_urls` (admin's own reference images — a "chart" upload, never sent to the AI), `category` ('chart'\|'context'), `applies_to` (text[] tag — which AI surface reads the row: 'chat', 'fortuneteller'). Two admin sections: Charts (named personal frameworks with 0-2 reference images + full transcribed logic) and AI Context (general philosophy entries), both editable without touching code. `_buildSystemPrompt()` (chat) and the Fortuneteller prompt builder both now pull any `ai_context` row tagged for them via a generic loop (`_dynamicChartsFor`/inline equivalent) in addition to the 4 hardcoded keys each already used — so a new Chart/Context row added via admin.html is live on the next AI call with zero code changes. AI response format on both surfaces gained a required `state_read` field ("low"\|"neutral"\|"high", never shown to the user) — a coarse read of the user's state that the model must commit to before replying, meant to make chart logic apply more consistently (reuses the same low/mid/high tiering already used by `tools`/`chargers` mood/quest fields). Every reply fire-and-forget logs `{state_read, charts_live, user_msg, ai_msg}` to `admin_insights` (a v129 stub table, previously unused) via `insight_key` = 'chat' or 'fortuneteller'. `admin.html` also gained 🔍 Debug Log (reads that log back), 🧪 Sandbox (test-chat against live Charts/Context without logging a real session), and 🧭 Consultant (a separate Gemini call given the full config + recent log as context, for "where are we inefficient"-type questions) tabs. `db/schema_v160.sql` has the migration — **must be run in the Supabase SQL editor**. **Security note (deliberate, temporary):** `admin.html`'s password gate was removed at the user's request for faster iteration, and `ai_context`/`admin_insights` were opened to the `anon` role (RLS policies + GRANTs) so admin.html can write directly with the same public anon key the main app already uses client-side. This means anyone with the anon key (already extractable from the public app) could currently rewrite Clar's live prompts. Must be tightened — e.g. a token-gated Cloudflare Worker relay (same pattern already used to hide the Gemini and Sheets keys) — before wider launch |
| v161.x | AI Logic Console follow-ups (admin.html only, `db/schema_v161.sql` for the new table) — hardcoded `ai_context` keys (identity/philosophy/conversation_flow/disclaimer/the 4 Fortuneteller ones) now show a locked, disabled tag checkbox with an explanation instead of a misleading editable one, since their surface is fixed in code regardless of `applies_to`. New `admin_commands` table (`type`: 'rule' = standing instructions always fed into the Consultant's prompt, editable in a System Commands list; 'suggestion' = saved from a Consultant reply via an inline colored Save link, tracked in a Saved Suggestions list). Consultant replies switched from free text to structured JSON (`summary`/`points[]`/`next_action`) rendered with real headings/bold/highlight instead of one paragraph, plus Google Search grounding (`tools:[{google_search:{}}]`) for external research with real source links pulled from Gemini's `groundingMetadata` (not trusted from the model's own JSON). Fixed a real Consultant/Sandbox truncation bug — Gemini's uncapped "thinking" tokens were silently eating the reply budget; `thinkingConfig:{thinkingBudget:0}` fixed it. New "🗺️ Map" tab: hand-rolled SVG node graph showing every Chart/Context/Command as a node connected to whichever AI surface(s) it feeds (or a separate "not connected" section if none) — canvas/hub layout is derived from how many items each hub has (verified against live data to avoid overlap/off-canvas, not just eyeballed), click any node to open its real edit form, auto-refreshes on any data change |
| v162 | `frameworks_used` field — both chat and Fortuneteller response JSON gained a required array field where the model must name, by exact display name, which specific `ai_context` framework(s) (if any) genuinely shaped that particular reply, decided before writing the message; empty `[]` is expected and honest when no specific framework applied. Logged to `admin_insights` alongside the existing `charts_live` (what was merely tagged/available) so Debug Log can show the gap between "available" and "actually used" — the diagnostic previously missing when investigating why Clar wasn't drawing on charts at the expected moment. Verified via a 3-turn simulated conversation against live production config (12 chat-tagged frameworks, 23.5k-char system prompt): turns 1-2 correctly returned `frameworks_used:[]` during conversation_flow's Phase 1/2 "just listen" window, turn 3 correctly returned `["Contrast"]` once the user opened up — and the reply text genuinely matched Contrast's specific pivot-question technique, confirming the self-report is accurate rather than hallucinated |
| v163 | Fixed a real bug found while chasing v162 — zero real chat rows had ever landed in `admin_insights` despite actual live testing. Root cause: `init()`'s fast path (content served from a fresh, <60s-old local cache) `return`ed early *before* reaching the `_getSb()` call, so `window._sbShared` (which `_logAiInsight` depends on) and the anonymous-auth session check were silently skipped on that path entirely — no error, just a no-op. Moved `_getSb()` (plus a non-blocking auth-session check) to run first and unconditionally in `init()`, ahead of both cache-loading branches. Client creation itself is synchronous so this adds no startup delay; only the auth check runs in the background |
| v164 | v163 alone still wasn't enough — real testing afterward still showed zero logged rows in `admin_insights`. Deeper root cause: `supabase-js` loads via a `defer`red `<script>`, but the DATA LOADER's inline script executes synchronously during HTML parsing (deferred scripts don't run until parsing finishes) — so `window.supabase` can genuinely still be `undefined` at the exact line `_getSb()` runs, throwing `'Supabase SDK not loaded'`. That failure was being silently swallowed locally, and separately masked upstream too — any Supabase failure in the content-fetch path silently falls back to the Sheets Worker, so the app kept working outwardly the whole time with `window._sbShared` simply never getting set, zero visible symptom. `init()`'s Supabase-client/auth step now retries every 50ms for up to ~2s waiting for `window.supabase` to exist, instead of failing once and giving up — still fire-and-forget, still no startup delay |
| — | `db/schema_v164_fix.sql` — v163/v164 fixed the *client-side* reasons logging never fired, but real testing after that still hit a 403. Root cause found by reproducing the app's own `signInAnonymously()` flow via curl: any signed-in session (even an anonymous one) gets a JWT with role `authenticated`, not `anon` — but `admin_insights`'s grants/policies (schema_v160.sql, schema_v160_fix.sql) only ever covered `anon` (what a bare curl request and admin.html use, since admin.html never signs in). Every real chat/Fortuneteller session was being silently rejected the entire time. Added matching `GRANT`/`CREATE POLICY` for `authenticated`; reproduced the exact 403 first, then confirmed the fix resolves it, both via curl using a real anonymous-session token — not fixed blind |
| v165 | Real production data (once v164's fix made logging actually work) surfaced a further problem with `frameworks_used`: the model can fabricate a plausible-sounding name instead of leaving the array empty — caught live citing "Infinite Identity Chart" and "Altitude chart", neither of which exist in `ai_context` (it had actually drawn on a Charger suggestion, not a listed Personal Framework, and invented names rather than reporting that honestly). Prompt instruction strengthened on both chat and Fortuneteller: copy the name character-for-character, only from the Personal Frameworks list, never a tool/charger name, leave empty rather than guess. Debug Log now cross-checks every cited name against that turn's `charts_live` and flags any that don't match with a ⚠️ badge instead of trusting the citation at face value. Verified by replaying the exact real conversation that produced the fabrication through the strengthened prompt — all citations came back real across all 4 turns, zero fabrications |
| v166 | v165's prompt-strengthening reduced but didn't eliminate fabrication — real data after it: 1 fake name out of 2 cited (down from 2/2 before), confirming prompting alone caps out short of 100% as expected. Added a deterministic code-level filter (`_filterRealFrameworks`) that cross-checks every `frameworks_used` entry against that turn's actual live list (`_dynamicChartKeysFor`) *before* it's ever written to `admin_insights` — fabricated names are dropped, not just flagged, so the log can never contain one regardless of model behavior. Applied to both chat and Fortuneteller logging. Verified the filter logic against the exact real fabricated values seen in production (`['Akarta chart','Altitude chart']` → `['Akarta chart']`, `['Infinite Identity Chart','Altitude chart']` → `[]`) |
| v167 | Conversation pace now adapts to how long someone has actually used Clar — a brand-new user gets the careful, explain-everything ramp-up; someone with weeks of history gets the same care but faster and more direct, since slow pacing reads as patronizing once trust is established. Signal: `_getSummaries().length` (distinct days with a saved conversation summary, up to 30) plus total `clar_xp` as a rough engagement-depth proxy — deliberately 3 coarse tiers (new / growing / experienced) rather than a numeric score, since the model needs a clear instruction to follow, not a number to interpret. Growing (the middle/default tier) gets no extra note — current tuned behavior stays as-is; only the two edges change. Verified via a same-message, two-variant comparison: the "new" framing produced a 40-word reply spelling out options ("deadlines ya kuch aur"), the "experienced" framing produced a 26-word reply that got straight to the point — a real, measurable difference in the intended direction — **superseded by v168, see below** |
| v168 | v167's days/XP-based pacing replaced — the owner felt those signals would genuinely misjudge people (skips gamified cards but is deeply self-aware; chats briefly every day vs deeply once a week; XP measures app engagement, not conversational fluency with Clar specifically). Pace is now read fresh each turn from the conversation itself: a required `pace` field ("fast"/"slow") the model must set based on whether the user's own words show they follow Clar's terminology and frameworks — no stored stats involved at all. Logged to `admin_insights` (Debug Log shows a ⚡fast/🐢slow badge) per the owner's explicit request that every AI-behavior feature be visible there going forward, not just the ones already covered (state_read, frameworks_used). Verified across two full simulated conversations rather than a single-message comparison: one where the user stayed vague throughout (pace stayed "slow" both turns) and one where the user started vague then used Clar's own vocabulary fluently ("main abhi resistance mein hoon... isliye allow nahi ho raha") — pace correctly flipped from "slow" to "fast" mid-conversation, with the reply itself visibly shortening and dropping the explanatory tone to match |
| v169 | (admin.html only) "🧬 AI Features" registry added at the top of the AI Logic tab — a living, editable list documenting every AI-behavior capability built (State Read, Frameworks Used, Pace, Consultant, Map, etc.), stored in `admin_commands` with a new `type='feature'` (`db/schema_v169_fix.sql` extends the CHECK constraint). Explicit ask: this should live in the admin console itself, not only in this file's changelog. Seeded with 11 entries covering everything built through v168. Also labeled the Debug Log's badges ("STATE: low", "PACE: ⚡ fast") — bare unlabeled badges weren't clear at a glance |
| v170 | Audit (at the owner's request) of what Clar can and can't actually suggest, found via a direct code-vs-content comparison, not assumption: (1) 4 Self-tab practices built after v107 — My Personal Space, My Foundation, True View Chart, Pure Abundance — were never added to the hardcoded `selfPractices` block Clar reads, so it could never mention them even in a perfect-fit moment; added with factual descriptions matching what each feature actually does. (2) All 54 chargers have an empty `ai_best_for` (confirmed via Supabase query) and admin.html's Charger form never had a field to fill it in. (3) `tools` never even had an `ai_best_for` column (chargers did, just empty) — `_buildSystemPrompt()`'s toolsList already reads `t.ai_best_for` conditionally, so no code change was needed there, only the column. (4) Found "Hand Chart" exists as both a Tool name and an unrelated Chart/AI Context entry — deferred, owner will decide the resolution. Content-writing for the 54+22 items deferred as a separate, larger pass (needs the owner's intent per item, not fabricated); this version is schema/wiring only: `tools.ai_best_for` column added, and a new `saveContentAiBestFor()` in admin.html lets both forms write that one field straight to Supabase — deliberately separate from the rest of each form, which still saves through the old Sheets/Apps Script path (writing ai_best_for through that path would land in the Sheet, not Supabase, where the live app actually reads from — confirmed via curl that tools/chargers had no UPDATE grant for anon at all before this) |
| v171 | Chat bubbles render WhatsApp-style formatting instead of plain text — `_addBubble()` switched from `p.textContent=text` to a new `_chatFormat()` that converts `**bold**`, numbered lists (`1. `), and dash lists (`- `) into real `<b>`/`<div>` elements (escaped first via `_chatEsc()`, so only these exact patterns can ever produce tags — confirmed safe against a raw `<script>` injection test). System prompt's FORMATTING section instructs the model to use this only when it genuinely helps (multiple distinct points/steps → list; a word needing emphasis → bold; a natural emoji here and there) rather than on every reply. Verified with two live model calls: a "give me 3 things" message correctly produced a numbered list with bold labels and an emoji, a plain emotional message stayed short plain text with no forced list — confirming the calibration works, not just that formatting is technically possible |
| v172 | Chat bubble text sized/spaced closer to the ChatGPT app per explicit request — `.chat-bubble` font-size 1.02rem→1.15rem, line-height 1.6→1.7, padding 10px 14px→13px 16px, max-width 82%→85%; `.chat-msgs` same-sender gap 4px→6px, sender-switch gap 10px→16px for clearer turn separation |
| v173 | Fixed unnecessarily long replies to short/simple messages — diagnosed from real production data (20 logged turns pulled from `admin_insights`), not assumption: nearly every reply, even to "Hi"/"Yes"/one-word inputs, followed a rigid name+validate+restate+question template that reliably produced 2-4 sentences regardless of how little the user actually said (e.g. "Yes" → a full paraphrased paragraph ending in a question). Root cause: PHASE 1's "reflect back what you heard" instruction had no floor for trivial input, and PERSONALITY's "always use their name" was firing every message instead of "occasionally" as originally intended. Both tightened: reply length must now match input length (short input → one short sentence, no template), reflection only happens when it adds genuine insight rather than as a routine restatement, and name usage is explicitly capped to "occasionally, not every reply." Verified by replaying the exact real inputs that had previously produced long replies: "Hi" dropped from 13-24 words to 2, "Yes" dropped from 23-34 words (with a full restated paragraph) to 7, while a substantial message ("mera attitude is so bad...") still got a real 2-sentence reply — confirming the fix shortens padding without cutting real conversation short |
| v174 | v173's fix was itself wrong in principle, per direct owner correction: "match reply length to their input length" is not how real human conversation works — a long message often only needs a short reply, and length should be decided by what actually needs saying, not by mirroring the user. v173 had also accidentally dropped the old hard "Max 2-3 sentences" cap while adding that flawed proportional framing, weakening the constraint rather than fixing it — owner confirmed replies were still coming long. Rewrote PERSONALITY with a firm default (1-2 short sentences, decided only by necessity, explicit instruction to cut anything that doesn\'t need to be there) and removed the "for short/simple messages" framing from PHASE 1 so reflection is judged by whether it adds insight, not by input length. Verified with a genuinely long, multi-part complex message (45 words describing several stacked bad events in one go) alongside "Yes" and a medium message — all three replies landed in the same tight 14-19 word range regardless of wildly different input lengths, confirming length is now decoupled from the user\'s input and driven by need |
| v175 | Fix login/logout leaving tabs in a stale, inconsistent state (owner report: after signing in or out, some tabs still looked like the previous session) — root cause traced in `_setupAuthListener`/`_signOutGoogle`: on Google sign-in or sign-out, only the profile form + XP display were explicitly refreshed after `localStorage` was updated/cleared; every other tab (chat, goal, NN, revise, self) holds its own in-memory state (e.g. `chatHistory` loaded once at boot) that a `localStorage` write alone never touches, so those tabs kept showing the old session until manually re-visited. Fixed with a full `window.location.reload()` — on the Google-sign-in branch of `SIGNED_IN` (600ms after pull+push, so the `_pushAllUserData()` migration upserts land before navigation cancels them) and right after `sb.auth.signOut()` completes in `_signOutGoogle` — so every tab now boots clean from the correct account state on both login and logout, matching how e.g. Facebook fully resets on account switch. Deliberately does not touch the anonymous-session `SIGNED_IN` branch (app's own first-run bootstrap, not a user-initiated login) to avoid any reload-on-every-open risk. `sw.js` `CACHE_VERSION` bumped `clv-v174`→`clv-v175`. **Separately found and fixed while verifying this end-to-end (Chrome DevTools MCP attached to a real, already-Google-signed-in Chrome profile — see below): Supabase's "Allow manual linking" project setting was OFF, which silently broke the entire v150/v159 `linkIdentity()` fix — every real Google sign-in was failing linkIdentity with `"Manual linking is disabled"` and falling back to a plain `signInWithOAuth()`, which does NOT preserve the anonymous user_id, i.e. the exact account-fragmentation bug v150/v159 were meant to prevent was still happening on every sign-in.** Fixed by enabling "Allow manual linking" in the Supabase Dashboard (Authentication settings) — a project-config change, not a code change. Verified after the fix, against the real production Supabase project: a fresh anonymous session linking Google for the first time now correctly keeps its own user_id; an account already linked elsewhere correctly falls back to `_handleAuthRedirectError`'s existing "already linked" recovery path and joins the existing account. Both the reload fix and the manual-linking fix were tested against real Google auth, not just read from code — see "Browser testing setup" below |
| v176 | Three more login/logout issues, all reported from real usage after v175 shipped: (1) **Double Google account-picker prompt on sign-in.** Root cause: `_signInWithGoogle()` unconditionally tried `linkIdentity()` first on every sign-in — but once "Allow manual linking" was enabled (v175), that only ever succeeds cleanly on a device's genuinely first-ever link; for any *returning* Google user (the common case) the identity is already linked to their own account, so `linkIdentity()` always fails with "already linked to another user" and silently falls back to a second, separate `signInWithOAuth()` redirect — visible to the user as the Google account picker appearing twice. Fixed with a new persistent flag `clv_google_linked_before` (deliberately excluded from `_clearUserLocalStorage()`'s key list, since it's device-level history, not account data) — once set, `_signInWithGoogle()` skips `linkIdentity()` entirely and goes straight to the single-redirect `signInWithOAuth()` path. (2) **Header chip (top-right name/avatar) needing several manual refreshes after sign-in/out.** Root cause: `_updateAuthUI()` (which runs on every session change) only ever updated the Profile tab's own account section — nothing refreshed `#hdr-pname`/`#hdr-pa`, so the header kept showing stale data until an unrelated re-render happened to touch it. Fixed by having `_updateAuthUI()` call `window.updateHeaderChip()` directly (covers every call site: `initSync`, all three `onAuthStateChange` branches, and `_signOutGoogle`), plus one more explicit call in `initSync()` right after `_pullUserData()` resolves, since `_updateAuthUI()` runs *before* the pull has real data to show. (3) **No visual distinction between an anonymous/guest session and a signed-in account** — both showed the same "Me" name + "?" avatar placeholder. `updateHeaderChip()` now checks a new `window._isGoogleUser` flag (set by `_updateAuthUI()` from the real Supabase session) and shows "Guest" + 👤 whenever not signed in with Google, regardless of any nickname set locally. All three fixes verified end-to-end via Chrome DevTools MCP against the real production Supabase project: confirmed a first-time-on-device link still double-redirects once (expected, sets the flag), a second sign-in after sign-out is single-redirect with zero `?error=identity_already_exists` hop, and the header shows the correct name/avatar immediately on both sign-in and sign-out with no manual refresh needed. `sw.js` `CACHE_VERSION` bumped `clv-v175`→`clv-v176` |
| v177 | Two more real-usage reports after v176: (1) **Profile accent colour and XP/streak bar also needed a manual refresh after sign-in** (same class of bug as v176's header chip, just not caught yet) — root cause confirmed identical: `xpRefreshAll(xp)` and no theme-reapply call at all existed in the code path that actually runs (`initSync()`'s top-level `_updateAuthUI → _pullUserData` flow); both were only ever wired into the `SIGNED_IN` listener's `isGoogle` branch, which — as established investigating v176 — doesn't fire in practice (the session is already resolved via URL hash detection before the listener registers). Fixed by adding `xpRefreshAll()` (no-arg self-reads `clar_xp`) and a new `window._reapplyTheme()` call to `initSync()` right after `_pullUserData()` resolves, alongside the existing `updateHeaderChip()` call. `_reapplyTheme()` extracts the accent-application logic that previously only existed inline in a one-time boot IIFE (that IIFE now just calls it, no behavior change, removes duplication). (2) **Revise & Repeat images persisting across account switches on the same device** — confirmed real, not a misunderstanding: `_clearUserLocalStorage()` cleared `c9_rrdocs`/`c9_rrlinks` but never `c9_rr` itself (the actual image array) — every other per-account content key (`c9_vis`, `c9_nn`, `c9_goal`, etc.) was already in that list, this one was a genuine oversight. Net effect: switching accounts (or an account with no `user_revise` row yet, e.g. brand new) left the *previous* session's RR images rendering, since nothing ever cleared them and the Supabase pull's merge logic only overwrites when `rev.rr_images` is actually present. Fixed by adding `'c9_rr'` to the clear list. Both fixes verified end-to-end via Chrome DevTools MCP: deliberately set a fake theme (`dark`) and XP (`9999`) before signing in with the real Google account — both were correctly overwritten with the account's real stored values (`#4E7FA6` accent, `394` XP) immediately, no manual refresh; confirmed the same account's 24 real `rr_images` correctly disappeared from `localStorage` (`c9_rr` → `null`) on sign-out instead of persisting into the next (anonymous) session. `sw.js` `CACHE_VERSION` bumped `clv-v176`→`clv-v177` |
| — | `workers/admin-relay-worker.js` (new) + `db/schema_v178_lockdown_admin_write.sql` — closes the anon-key admin write hole flagged in v160 (see "Security" note under AI Logic Console). New token-gated Cloudflare Worker holds a Supabase `service_role` key server-side and exposes 8 actions (`ai_context.upsert/delete`, `content.updateAiBestFor` allowlisted to `tools`/`chargers`, `admin_commands.select/insert/update/delete`, `admin_insights.select`) — `admin.html` now calls these instead of using its own anon key for any of them, confirmed via grep no direct write/read of these tables remains in `admin.html`. The SQL migration then revokes exactly the anon grants/policies added in `schema_v160.sql`/`schema_v161.sql`/`schema_v170_fix.sql` (verified each was scoped `TO anon` only, never `TO authenticated`, so nothing else needed touching); `ai_context`/`tools`/`chargers` SELECT and `admin_insights` INSERT stay open since the main app needs them. Does not touch `index.html`/`clarvoyance_vXXX.html`/`sw.js` — no app version bump. **Deployed, activated, and verified working — see the Security note above.** A second migration, `db/schema_v178a_fix_service_role_grants.sql`, was needed too: `service_role` turned out to have no explicit grants on these 5 tables on this project at all (found via live "permission denied" errors during setup, unrelated to the anon lockdown itself) — fixed with one explicit `GRANT ... TO service_role` statement. Follow-up audit of every other table's grants (see Security note above for full detail) found and fixed two more real issues: `db/schema_v178b_fix_admin_insights_authenticated_leak.sql` (the `authenticated` role — i.e. every real app user — could read the entire `admin_insights` debug log, a bigger leak than the `anon` one since real traffic is always `authenticated`) and `db/schema_v178c_fix_daily_practice_log.sql` (unrelated functionality bug found during the same audit: `user_daily_log` didn't exist in PostgREST's schema at all and `user_practice_log` was missing its `anon`/`authenticated` grants — both silently broken for real users, confirmed via a real anonymous-session test, not assumed). **All four migrations (v178/v178a/v178b/v178c) confirmed run and verified live as of 2026-08-04** — `admin_insights` now 403s for a real authenticated session, `user_daily_log`/`user_practice_log` both accept real inserts (201) where they previously failed with a missing-table error and a permission error respectively. |
| v145 | Pulse clarMin + NN done fixes — (1) `_stop()` changed `Math.floor`→`Math.round` so sessions of 30+ seconds save as 1 min instead of 0 (was the main reason clarMin always showed 0 after short sessions); (2) NN done now counts only numeric main-item keys (`/^\d+$/.test(k) && val===true`), not subpoint keys like `s0_1` — fixes overcounting and inconsistency with nnTotal; (3) `clv_clar_times` removed from `_clearUserLocalStorage()` — Supabase only stores the scalar `clar_daily_min` so the full date-keyed array cannot be restored after clearing, making retention safer |
| v136 | Fortuneteller redesign — (1) notice above orb fills gap: "Every session with Clar purifies your field…"; (2) prediction cards redesigned: open oracle style, no boxy glass cards, hairline separator between sections, larger flowing italic text; (3) orb enhanced: richer gradient + inner rotating nebula (::before conic-gradient mix-blend-mode:screen + ftNebula keyframe); (4) energy field tap tooltip: explains why constellation looks like it does (Clar time, momentum, field density description) |
| v134 | Fortuneteller energy field redesign — replaced faint floating dots with: (1) aurora blobs: 5 large slow-drifting radialGradient colour clouds (purple/gold/teal) that breathe and shift; (2) constellation network: particles connected by fading lines when < 75-130px apart (distance based on energy state), constantly reforming as particles drift; (3) particle glow done via large dim halo + bright core (no expensive shadowBlur); energy state controls speed/density/link-distance/brightness |
| v133 | Fortuneteller energy animations — particles dramatically more vivid: larger sizes (up to 5.5px), higher alpha (0.28–0.95), shadowBlur glow per particle scales with energy state, gentle twinkle pulse on each particle; rings boosted from ghost opacity (5–18%) to visible (16–40%) with glow box-shadow |
| v132 | Fortuneteller fix — spinner hung forever because: (1) monthly summary fetch blocked main call with no timeout; (2) system prompt too large (4 full ai_context texts = 8k+ chars) causing Gemini to time out. Fix: monthly summary is now fire-and-forget (never blocks); ai_context values truncated to 800/600 chars each; summaries back to last 7 (not 30); 20s AbortController timeout on main fetch; Object.values replaced with Object.keys loop for compat |
| v131 | Particle field fix — ctx.scale(dpr,dpr) so draw calls use CSS px (not physical px); old canvas removed on each init so energy state refreshes; alpha floor raised (Seeking: 0.20–0.40, Steady: 0.28–0.55, Active: 0.35–0.72, High: 0.45–0.92); size floor 1.2–1.8px so particles never sub-pixel; clearRect now clears CSS-px area not physical-px area |
| v130 | Phase 3 base — (1) Flowing particle field replaces static stars in Fortuneteller: canvas-based, energy-reactive (speed/count/brightness scale with High/Active/Steady/Seeking energy state, particles drift upward, stop when tab hidden); (2) Radar/spider chart after prediction cards: 5 dimensions (Goal Clarity, Belief Alignment, Daily Action, Clar Investment, Resistance Level) computed from localStorage, animated ease-out on reveal; (3) Practice log: `user_practice_log` Supabase table (type, name, completed_at); `window.logPractice(type,name)` fire-and-forget; hooks into tool open, charger open, vibe card completion |
| v129 | Fortuneteller deep upgrade — 4 new `ai_context` rows in Supabase (`fortuneteller_philosophy`, `emotional_guidance_system`, `manifestation_signals`, `prediction_language`); feelings-as-primary-signal philosophy; full emotional guidance scale (20 levels, powerlessness→joy); safety disclaimer embedded in AI prompt; goal staleness detection at 2-day threshold; goal_last_visited tracked on bnavSwitch goal tab open + synced to Supabase; NN completion rate injected into Fortuneteller prompt; monthly meta-summary generation (compresses 30 daily→1 paragraph, stored `clv_monthly_summaries`, max 6 months); concern card ("🌀 Clar notices") shows only when AI raises a concern; chat summary cap extended 7→30 days; Pulse slice updated to 30 days; `db/schema_v129.sql` adds goal_last_visited + goal_created_at to user_goals, monthly_summaries to user_clar, admin_insights table stub (Phase 2) |
| v128 | **Fortuneteller** — "Clairvoyance" oracle in its own 🔮 Fortune bottom-nav tab; cosmic dark UI (animated floating crystal orb with radial gradient + glow, 3 concentric rotating rings, 60 JS-generated twinkling star particles, glassmorphism prediction cards); instant behavioural energy reading — no AI wait (reads Clar time + momentum + trend → e.g. "High, rising energy detected"); Gemini oracle call: injects Clar's `ai_context.philosophy` from Sheets, predicts near future (7-14 days) + far future (30-90 days) + `watch_for` (specific real-life manifestation sign); staggered card reveal animation (opacity+blur+translateY, 0/120/260ms delays); daily cache `clv_fortune_cache`; `fortuneLoad`/`fortuneRefresh` exposed globally; bnav "Vibe Feed" → "Vibe" for 6-tab layout |
| v178 | Owner asked whether "time spent on app" data was being recorded/displayed/used for progress judgment — audit found `clv_clar_times` only ever tracked time in the **Clar AI chat tab specifically**, not the whole app, and confirmed that's used correctly (momentum decay, Fortuneteller prompt + radar chart, Pulse) but the main chat AI deliberately ignores it since v168 (stats-based pacing was replaced with live in-conversation pace detection — not a bug, an intentional prior decision). Owner wanted **total app-open time** tracked too, so added a fully separate `clv_app_times` (rolling 7-day `{date,minutes}`, same shape as `clv_clar_times`) tracked via `document.visibilitychange` + `pagehide` from `DOMContentLoaded` — counts time the app is open on ANY tab, not gated to chat. Synced to Supabase (`user_progress.app_times` JSONB + `app_daily_min`, `db/schema_v178_app_time.sql`), restored on pull, cleared on sign-out/account-switch alongside `clv_clar_times`. Displayed as a new "App Time Today" metric card + 7-day sparkline in the Pulse dashboard (own full-width row below the existing 3-card grid, so the existing Clar/Streak/NN Done cards are untouched). Deliberately does not feed into any AI prompt (chat or Fortuneteller) — the ask was specifically to record/display it, and expanding either AI's judgment inputs was out of scope. `sw.js` `CACHE_VERSION` bumped `clv-v177`→`clv-v178`; stale `#prof-app-ver` label in Profile→Updates (had been hardcoded at "v147" since some earlier release, never caught) corrected to v178 while touching that area |
| v179 | Owner pushed back on v178's "out of scope" call within the same session: momentum decay + Fortuneteller were always *meant* to read overall engagement, not just chat — a user doing real work in Goal/Chargers/Self tabs without opening Clar chat was getting full streak decay anyway, which the owner considers unfair, not a feature. Reworked so **`clv_app_times` (total app time) is now the single signal** everywhere decay/Fortuneteller/Pulse read time from — `clv_clar_times` (Clar-chat-only) is kept recording only for the literal "X min today" label in the Clar chat header itself (a genuinely different, narrower thing: time in *this conversation*) and for the `user_daily_log.clar_minutes` analytics column. Changed: `_decayRate()`/renamed `_clarMinToday()`→`_appMinToday()` now reads `clv_app_times` (20+/10+ min thresholds left unchanged per owner — app time is a superset of chat time so decay only gets easier to avoid, never harder); Fortuneteller's `_energySignal()`, main oracle prompt ("X min in the app this week"), radar chart's `Clar Investment` dimension (renamed **App Time**), and field-signal tooltip all switched from `clv_clar_times` to `clv_app_times`; Pulse's `_computeMood()` and weekly-reflection AI prompt ("X min in the app this week") switched too; Pulse dashboard consolidated back from 4 cards to the original 3 — the first card (`pm-clar-val`/`pg-clar`, DOM ids left as-is) now sources and displays `clv_app_times` under the label **"App Time"**, and the redundant 4th "App Time Today" card added in v178 was removed. New: a small recommendation nudge under the Clar chat header's minute count — "10 min recommended today" — shown (via `clar-time-lbl` innerHTML, still Clar-chat-time-sourced) whenever today's chat time is below 10 min, hidden once crossed; `#clar-time-lbl` gained `text-align:right` for the two-line layout. No schema change needed (v178's `app_times`/`app_daily_min` columns already cover this). `sw.js` `CACHE_VERSION` bumped `clv-v178`→`clv-v179`; `#prof-app-ver` → v179 |
| v180 | **Practice Plans** — a multi-session design discussion (roadmap/milestones → problem/goal-triggered "prescriptions" → renamed for legal safety) landed as a scoped v1: Clar can now, rarely and only after a genuinely trust-earned moment, suggest a short time-boxed practice instead of/alongside a one-off card. Named **"Practice Plan"**, deliberately not "prescription" (owner's own legal-safety concern — avoids any medical/clinical implication, consistent with the existing chat disclaimer). New Supabase table `user_practice_plans` (`db/schema_v180_practice_plans.sql`: source `chat_problem`\|`goal`, practice_type `clar_chat`\|`nn_zero_skip`\|`tool`\|`charger`\|`vibe_card`\|`self_practice`, target_value/target_unit, duration_days, status `active`\|`completed`\|`lapsed`, display_text). New "PRACTICE PLANS" script block: `_loadPracticePlans`/`_savePracticePlan`/`_checkPlanAdherence`/`_refreshPlanStatus`/`_renderPracticePlanCard`/`_practicePlanContextForChat` — adherence is computed from data that already existed (no new tracking infra): `clv_clar_times` for `clar_chat`, `c9_nnhist` for `nn_zero_skip`, `user_practice_log` for everything else. Duration is clamped server-side by type (`clar_chat` max 7 days, matching `clv_clar_times`'/`clv_app_times`' existing 7-day rolling cap; `nn_zero_skip` max 30; everything else max 14) — the model is instructed on these ranges but never trusted blindly. `_buildSystemPrompt()` gained an optional `practice_plan` field in the response JSON (rare — explicitly "omit on almost every reply") plus an `ACTIVE PRACTICE PLAN(S)` context block read from a synchronous cache (adherence is checked async in the background via `_refreshPlanStatus()`, refreshed on boot and right after a new plan saves, so the sync prompt-builder never has to await) — this is how Clar proactively, warmly mentions a gap in the next session rather than needing a push notification (none exists yet). New Home tab card (`#clv-practice-plan`, wired into `bnavSwitch`'s `all[]`/`map.home` like the existing Clar Commitment card) shows active plans as "Day X of N" + a qualitative note ("staying with it" / "a few days slipped") — **deliberately never a raw adherence percentage or grade**, per the app's own repeated lesson (v173/v174) that raw stats read as judgmental and misjudge people; the system prompt carries the same instruction for Clar's own phrasing. `logPractice()` calls added to 3 Self-tab completion points that previously logged nothing (Peak State, Non-Doing Timer, Walk & Talk) so `self_practice`-type plans referencing them are actually trackable — purely additive, no existing behavior changed. Out of scope for this pass (explicit): Goal-tab/vision-images-driven plans, daily emails, push notifications, the broader multi-stage "roadmap" concept discussed earlier. `sw.js` `CACHE_VERSION` bumped `clv-v179`→`clv-v180`; `#prof-app-ver` → v180 |
| — | Owner asked to verify (not assume) whether `admin.html`'s Chargers/Categories were actually connected to Supabase, since the app moved to Supabase-primary back in v120 — audit + live curl checks against both the Supabase REST API and the Sheets Worker confirmed: `admin.html`'s Tools/Chargers/Charger Categories/Charger Rules tabs still wrote only to Google Sheets via the old `api()`/Apps Script path, while `index.html`'s `_fetchContentData()` tries Supabase first unconditionally and only ever falls back to Sheets if Supabase itself errors — so any edit made there was silently invisible to real users the moment Supabase (which is always up) served its own, un-updated copy. Live-verified both sources currently hold identical data (54 chargers, 4 categories, 6 rules, 22 tools, matching `schema_v120.sql`'s original seed exactly) — nothing has actually diverged yet, so no backfill/reconciliation was needed, only fixing where new edits go from here. Fixed by extending the existing secure write pattern (the same one `ai_context` already uses) rather than opening anon write access to these tables, which have no INSERT/UPDATE/DELETE policy for anon or authenticated at all: `workers/admin-relay-worker.js` gained 8 new actions (`chargers`/`charger_categories`/`charger_rules`/`tools` × `upsert`/`delete`, same `sbFetch()` + `on_conflict=id` upsert pattern as `ai_context.upsert`); `admin.html`'s `loadData()` now reads these 4 tables straight from Supabase in parallel with the still-Sheets-sourced vibe_cards/quotes/revise_repeat/learning_channels/modules; all 8 add/edit/delete handlers switched from `api()` to `adminApi()`, with numeric fields (`xp`, integer `order` columns) now explicitly `parseInt`'d and `active` sent as a real JSON boolean instead of the Sheets-era `'TRUE'`/`'FALSE'` string, since Postgres enforces column types where Sheets never did. `db/schema_v181_grant_charger_content.sql` grants `service_role` on `charger_categories`/`charger_rules` — `db/schema_v178a_fix_service_role_grants.sql` had already covered `tools`/`chargers` (needed for the pre-existing `content.updateAiBestFor` action) but missed these two, a gap only found by reading the actual migration history rather than assuming all 4 tables were equally covered. Does not touch `index.html`/`clarvoyance_vXXX.html`/`sw.js` — no app version bump. **Owner ran the worker redeploy + SQL migration; verified live via Chrome DevTools MCP (real browser against the deployed `admin.html`) and direct Supabase curl — reads confirmed working (4 categories/54 chargers/6 rules/22 tools loading correctly), add confirmed working (owner added a test tool + 3 real charger categories, all landed in Supabase, curl-verified).** Testing also caught two real bugs, both fixed and pushed the same session: (1) `loadData()`'s `Promise.all` let the pre-existing, unrelated Google Apps Script CORS failure (that endpoint fails intermittently when called from the `github.io` origin) take the new Supabase reads down with it — split into two independent try/catches so Tools/Chargers load regardless of Sheets' state; (2) a pre-existing, unrelated bug in `doConfirm()` (`closeConfirm()` nulled `_confirmFn` *before* the `if (_confirmFn)` check that was supposed to invoke it) meant **no delete button anywhere in admin.html had ever actually worked** — not caused by this change, but only surfaced by testing the new Chargers/Tools delete actions; fixed by capturing the function reference before closing. **Owner re-tested delete after the fix went live — confirmed working (test tool removed successfully).** |
| v181 | Two more real-usage reports, both root-caused in code and fixed: (1) **The "Has destiny grasped you..." jhatka line was appearing on every single reload**, not once a day as designed — root cause was two-fold: `langInit()` still had the v149 `TESTING ONLY` direct call to `_checkDailyRitual()` that a past session's own comment said to revert and never was (confirmed by reading that comment); and even reverting it to its intended gated call sites (`_checkPersona()`/`selectPersona()`) wouldn't have been enough on its own, since that whole onboarding-check waterfall (`_checkDestiny→_checkNick→_checkMood→_checkReg→_checkPersona`) already runs on every `langInit()` for a fully-onboarded user regardless — the real gap was that `_checkDailyRitual()`'s date-check only throttled the big swipe screen (once per day via `clv_ritual_date`), the jhatka fallback had no per-day cap of its own at all. Fixed both: removed the testing hook and restored the two intended call sites; added a second date key (`clv_ritual_jhatka_date`) so the jhatka can fire at most once per calendar day, same as the swipe screen. (2) **Any reload snapped back to the Clar AI tab even when working on a different tab** — root cause: two separate hardcoded `bnavSwitch(chatBtn)` calls, one in an early pre-`DOMContentLoaded` IIFE (v144, added specifically to guarantee zero icon/tab flash on boot) and one at the top of the `DOMContentLoaded` handler itself (v139) — neither had ever taken "which tab was the user actually on" into account; chat was always forced, unconditionally, on every load. Owner explicitly wanted last-active-tab restored on reload, but confirmed a real tradeoff first: Chat's content is local (instant, zero-flash-safe) while Goal/Chargers/Tools/Self need Sheets/Supabase data to render meaningfully — chose **Option B** (chat still shows first for the zero-flash guarantee, then silently switches to the real last-active tab once that data has loaded) over Option A (jump straight to the last tab and risk a brief empty/loading flash there, the exact class of bug v138-144 were fought to eliminate). Implemented via a new `clv_last_tab` key written on every `bnavSwitch()` call, restored after `await load()` resolves in the boot sequence — guarded by `window._userNavigated` so a real tap during that loading window correctly wins over the automatic restore (required resetting `_userNavigated` back to `false` right after the boot's own forced chat-switch, since `bnavSwitch()` sets that flag on every call including automatic ones, not just real user taps — without that reset the guard would always see `true` and the restore would never fire). `sw.js` `CACHE_VERSION` bumped `clv-v180`→`clv-v181`; `#prof-app-ver` → v181. **Live-tested via Chrome DevTools MCP before reporting done — first pass actually had a bug**: the tab-restore read `clv_last_tab` from localStorage *after* both automatic boot-time chat-switches had already overwritten it back to `'chat'` (since `bnavSwitch()` persists whichever tab it's given, automatic or not), so real usage would have silently never restored anything despite the code looking correct. Fixed by capturing the value into `window._bootLastTab` at the very start of the pre-`DOMContentLoaded` IIFE, before either automatic switch runs, and reading that capture instead of a fresh localStorage read. Re-verified live against the real deployed site (not just syntax-checked): (a) reload while on Goal correctly restores Goal (`activeNavTab`/`goalRowDisplay` both confirmed, `clv_last_tab` no longer clobbered); (b) ritual on a simulated new day shows the swipe screen and sets both date keys; (c) a same-day reload immediately after shows neither swipe nor jhatka, confirming the once-per-day cap actually holds across multiple reloads, not just once. |
| — | Owner added real charger content post-v181 (Self Love category — 5 chargers; "CLAR Practice/Tapas/Grit" category — 5 chargers, the owner's chosen name/framing for the discipline-reframe content discussed earlier) — both **confirmed live via curl**, no issue. Separately reported a new "Death Awareness" charger added under Monk mode not showing in the app. Root-caused precisely (curl-verified, not guessed): the live "Monk mode" category's real `id` in Supabase is `"Monk "` — **with a trailing space**, a legacy quirk from the original v120 data migration — but `admin.html`'s shared `v()` input-reader `.trim()`s every field it reads, including the charger-category `<select>`, so any charger saved under Monk mode got `category_id:"Monk"` (no space), which matches no real category and therefore renders nowhere in the app (the chargers accordion does a strict `c.category_id===cat.id` match, [index.html:7717]). Confirmed via a full live audit (curl, cross-referencing every charger's `category_id` against every real category `id`) that exactly **2 chargers** were affected this way: "Death Awareness" (the reported one) and **"Accept yourself"** (same bug, not yet reported by the owner — found only because the audit checked all chargers, not just the one complained about). Fixed in `admin.html`: the charger-category field is now read via the raw DOM `.value` instead of the trimming `v()` helper. Does not touch `index.html`/`sw.js` — no app version bump. **The 2 already-broken rows still need the owner to re-open and re-save each in admin.html** (re-selecting Monk mode so the corrected code writes `"Monk "` this time) — the code fix alone doesn't retroactively repair already-saved rows. |
| v182 | Feature-gated AI usage limits, admin-adjustable, message-count based (decided over token-count after live-testing real Gemini calls against the actual production config — the ~5,000-token system prompt dominates per-message cost regardless of content, so message count is already an accurate proxy, and it's the number a user actually understands). New `feature_gates` table (one row per feature+tier, `limit_count` NULL = unlimited) seeded with a starting recommendation — 8 msgs/day free, 500/day premium (a safety ceiling, not literal infinity) — every number editable live from a new **Limits** tab in `admin.html`, no code change needed (free tier has since been adjusted to 20/day, confirmed live via curl 2026-08-19). New `user_usage_log` table (own-row RLS, same pattern as `user_practice_log`) records every gated use for the owner's own visibility; enforcement itself reads a local per-day counter in `localStorage` rather than querying Supabase live, so an allowed message sends with zero added latency — deliberately soft (clearing localStorage resets it), a reasonable v1 tradeoff over hardening against deliberate circumvention nobody's actually doing yet. `user_profile` gained `subscription_tier` (defaults `'free'`) — no payment integration exists yet, so `admin.html`'s Limits tab also has a manual "set tier by email" lookup tool for granting Premium by hand until Razorpay webhooks land as a separate task. `_sendToAI()`'s `chatSend()` checks the gate before adding the user's message to history at all; over-limit shows an in-chat upgrade message instead of calling the AI. `workers/admin-relay-worker.js` gained 5 new actions (`feature_gates.select/upsert/delete`, `user_profile.findByEmail/setTier`) behind the same token gate as everything else in there. Real numbers behind the defaults, verified live against the actual Worker/Gemini config, not estimated: real per-message cost ≈ ₹0.15 (not the ₹0.20-0.26 originally guessed), and implicit prompt caching is confirmed **not** currently reducing that — two back-to-back calls with an identical system-prompt prefix showed no `cachedContentTokenCount` in Gemini's response, consistent with a documented Gemini limitation where implicit caching is unreliable specifically when `system_instruction` is set directly (exactly how this app sends its prompt) — explicit caching (a real, separate engineering task, ~90% discount on the dominant cost driver if built) noted as a high-value follow-up, deliberately not built as part of this pass. `sw.js` `CACHE_VERSION` bumped `clv-v181`→`clv-v182`. **`db/schema_v182_feature_gates.sql` has been run — confirmed live via curl (`feature_gates` table populated: free=20 msgs/day, premium=500 msgs/day, both `enabled:true`).** |
| v183 | **Quick-capture** — a floating pencil button, docked to the vertical-middle-left edge of the screen, visible on every tab (Home/Self/Goal/Fortune/Vibe/Clar AI). Tapping it opens a fullscreen free-write textarea directly — no prompts, no structure, no navigation required. First draft shared storage with the Self tab's "My Personal Space" (added v113); owner explicitly asked for the opposite — this box must **never persist** anything, unlike My Personal Space which still saves to `clv_myspace_text` as before. Now fully decoupled: opens empty every time, nothing written to localStorage, closing without acting on it discards the text with no confirmation prompt (deliberate, matches the zero-friction "on the go" framing this feature was built for). Second piece added same session: an **"Ask Clar ✦"** button in the footer — takes the written text, switches to the Clar AI tab, drops it into the real chat input, and calls the actual `chatSend()` pipeline (same usage gate, offline guard, and phase-aware system prompt as if the user had typed and sent it themselves — not a separate parallel AI call). Placement (mid-left edge) went through two failed positions before landing here, found by testing in a real browser rather than assuming: bottom-right collided pixel-for-pixel with the Clar AI chat tab's own send button; bottom-left then covered the start of the chat input's placeholder text. Hidden together with the bottom nav on chat-input focus (extends the existing v86 keyboard-hide hook). Verified end-to-end with real clicks and real typing in a live Chrome instance, not just code review: box opens empty regardless of prior text; typed content correctly does *not* appear in My Personal Space (confirming the storage split actually held); tapping "Ask Clar ✦" switched tabs, sent the message, and a real Gemini-backed reply came back in-phase ("What's the heaviest part about it right now?" — a genuine Phase-1 reflect-and-ask, not a canned string). `sw.js` `CACHE_VERSION` bumped `clv-v182`→`clv-v183`; `#prof-app-ver` → v183 (also corrected a stale "v181" left over from the v182 pass, which had bumped the changelog text but missed this specific label).
| v184 | **Push notifications** — web-first (Web Push/VAPID), schema deliberately platform-aware from day one so Android/iOS via Capacitor slot in later without a rework (discussed at length before building: Capacitor apps need native FCM/APNs via `@capacitor/push-notifications`, not the Web Push API, since it doesn't reliably work inside a WebView — `user_push_subscriptions.platform`/`native_token` exist now so that's additive later, not a redesign). Owner explicitly asked for zero hardcoded cadence — the entire admin-configurable rule engine lives in new `notification_rules` (`db/schema_v184_notifications.sql`): `trigger_type` is `'schedule'` (fixed times of day, e.g. Non-Negotiables at 9am/9pm) or `'inactivity'` (per-user, e.g. "hasn't touched Clar in 2 hours"), and every timing/repeat/cap knob — `schedule_times`, `days_of_week`, `inactivity_threshold_minutes`, `min_prior_engagement_minutes`, `cooldown_minutes` (doubles as the repeat cadence for inactivity rules — inactivity_threshold=120 + cooldown=120 together mean "first nudge after 2h quiet, repeats every 2h after that"), `max_sends_per_day/week/month` — is a column, not code; a third rule of either type is just a new row, no redeploy. Two rules seeded disabled (owner enables from admin.html when ready): the Non-Negotiables reminder and the Clar-inactivity nudge originally discussed. New `user_push_subscriptions` (RLS'd to `auth.uid()` like every other `user_*` table — the main app writes it directly, no relay-worker round-trip needed) and `notification_send_log` (not just an audit trail — the cron worker queries it to actually enforce the caps/cooldown). `user_progress` gained `last_clar_active_at`/`last_app_active_at`, updated from the client at the exact moments that mean "touched Clar" (entering the chat tab, every 60s while still on it, every message sent) or "touched the app at all" (existing v178 visibility-based app-time tracker) — piggybacks on the existing `_syncPushProgress()` push pipeline rather than a new sync call. `workers/admin-relay-worker.js` gained the actual send mechanism: RFC 8291 (`aes128gcm`) payload encryption + RFC 8292 VAPID JWT auth, hand-implemented with only the standard Web Crypto API (`btoa`/`atob`/`crypto.subtle`) — no npm package, keeping this a single paste-into-dashboard file like the rest of the project's workers — **verified correct via a real encrypt→decrypt round trip and a JWT sign→verify round trip against the exact shipped code before it went anywhere near the Worker, not written and assumed**. A new Cloudflare Cron Trigger (`scheduled()` export, schedule `0,15,30,45 * * * *` — every 15 min) is what actually evaluates rules and sends; `fetch()` gained `notification_rules.select/upsert/delete`, `notification_send_log.select`, and `notifications.testSend` (sends one real push immediately to a user looked up by email, bypassing the rule engine entirely — the fast way to confirm delivery works without waiting for a real trigger condition to occur). New "🔔 Notifications" tab in `admin.html`: rule CRUD (same upsert-by-id pattern as every other tab), a test-send box, and a recent-sends log viewer. Client gains a "Reminders" row in Profile → Updates (`notifToggle()`/`_notifSubscribe()`/`_notifUnsubscribe()`) and `sw.js` gains `push` (shows the notification) and `notificationclick` (focuses/opens the app at `?notif_tab=<target_tab>`, read by the boot sequence — takes priority over the ordinary last-active-tab restore, since a tapped notification is an explicit "go here" from the user) handlers. **Deployed and confirmed** — `db/schema_v184_notifications.sql` run, `workers/admin-relay-worker.js` redeployed with the two new secrets (`VAPID_PRIVATE_KEY_JWK`, `VAPID_SUBJECT`), and the Cron Trigger added in the Cloudflare dashboard (owner confirmed the schedule shows "Runs: At 0, 15, 30, and 45 minutes past the hour"). `sw.js` `CACHE_VERSION` bumped `clv-v183`→`clv-v184`; `#prof-app-ver` → v184.
| v185 | Fix found live-testing v184's push notifications: `notifications.testSend` (and the pre-existing `user_profile.findByEmail` tier-lookup in Limits, same root cause) searches `user_profile.email` — but that column was only ever set once from a manually-typed onboarding field (v92/v93), completely independent of the actual Google sign-in email; Google sign-in (`_updateAuthUI`) only ever updated the displayed name/photo, never wrote `session.user.email` back to `clv_user_identity`/`user_profile`. Owner hit this directly: signed in with Google, tried a test push to their real login email, got "no user found" because their onboarding-typed email (or a blank one) didn't match. Fixed in `_updateAuthUI`'s Google branch — on every session check, if `session.user.email` differs from the locally-stored `clv_user_identity.email`, overwrite it and push via the existing `_syncPushProfile()` pipeline (idempotent — only writes/syncs on an actual mismatch, confirmed via a two-pass test: first pass with a deliberately mismatched stored email correctly overwrote it and called sync once, second pass with matching emails correctly called sync zero times). This also silently fixes the pre-existing Limits-tab tier-lookup for the same class of account. `sw.js` `CACHE_VERSION` bumped `clv-v184`→`clv-v185`; `#prof-app-ver` → v185.
| — | Worker-only fix, found live-testing v184/v185: `workers/admin-relay-worker.js`'s `sendWebPush()` referenced `env.VAPID_PUBLIC_KEY` for the Authorization header's `k=` param, but the same file's own header comment (and every instruction given to the owner) said that key does NOT need a Worker secret — it was only ever put in the client as `window.CLV_VAPID_PUBLIC_KEY`. A real inconsistency: the code needed a value nothing ever told the owner to set. Symptom chain that led here: `notifications.testSend` kept failing with `"undefined" is not valid JSON` even after the owner correctly set `VAPID_PRIVATE_KEY_JWK` (confirmed present, correctly named, right Worker, via a screenshot) — that error is `JSON.parse(undefined)`'s exact message, meaning `env.VAPID_PRIVATE_KEY_JWK` was somehow still not reaching the running Worker; investigating that surfaced this separate, real bug on the next line down. Fixed two ways: (1) `VAPID_PUBLIC_KEY` hardcoded as a plain `const` in the Worker file itself (not sensitive — safe to hardcode, matches the client's copy, removes a dashboard step entirely rather than adding one); (2) `sendWebPush()` now checks `VAPID_PRIVATE_KEY_JWK`/`VAPID_SUBJECT` explicitly before use and throws a named, actionable error ("X secret is not set on this Worker") instead of letting a missing secret surface as a cryptic `JSON.parse` failure — so if the private-key issue turns out to be separate/still unresolved, the next error message will say so directly instead of needing another round of guessing. **Confirmed working end-to-end after redeploy — owner received a real test push via admin.html's "Send test" against their own account.**
| — | Fix: `notification_rules.id` was created `GENERATED ALWAYS AS IDENTITY`, which rejects any INSERT/upsert supplying an explicit `id` — exactly what editing an existing rule does (admin.html's upsert-by-id pattern round-trips the row's own id on every edit). Surfaced immediately when the owner tried to enable the seeded Non-Negotiables rule: "cannot insert a non-default value into column \"id\"". `GENERATED BY DEFAULT AS IDENTITY` still auto-generates an id when none is given but allows one to be supplied when editing. `db/schema_v184a_fix_notification_rules_id.sql` has the one-line `ALTER COLUMN ... SET GENERATED BY DEFAULT` fix (existing rows/ids untouched); `schema_v184_notifications.sql` corrected too so a fresh install doesn't hit the same bug. Only `notification_rules` had this problem — `notification_send_log`/`user_push_subscriptions` are never upserted by an explicit client-supplied `id`. **Owner needs to run `db/schema_v184a_fix_notification_rules_id.sql`, then retry enabling the rule.**
| — | Fix (found chasing the "enabling a rule doesn't seem to save" report above): `admin.html`'s shared `fYN()` Yes/No toggle helper renders its "Yes"/"No" text once when the form opens and never updates it when the checkbox is actually clicked — the underlying `.checked` state was always read correctly at save time, but the stuck label made every toggle in the whole admin panel (notification rules' Enabled, feature gates' Enabled, anything else built with `fYN`) look like clicking silently did nothing, which is exactly the symptom reported. Fixed by giving the label span an id and an `onchange` handler that updates it — verified via a live click-simulation both directions (off→"No", on→"Yes") in a real browser before shipping.
| v186 | **Engagement Engine** — replaces the old flat "+1 per day, %/whole-day decay" momentum system with one driven by per-section daily time+XP targets, Non-Negotiables completion, and hour-based accelerating decay. Built after a long multi-turn design discussion (owner explicitly pushed back twice — first on collapsing XP to a binary gate ["xp scale differences seem good for us because they're distributed unevenly based on effectiveness/impact — think of this as well"], then on choosing raw XP over a normalized ratio ["its about what is right instead of thinking in a way as what is easy to draw a graph"] — both corrections adopted, not just heard). Per section (vibe/chat/fortune/goal/home/self): `section_score = average(time_ratio, xp_ratio)`, each ratio capped at 100% of an **admin-set target** (`section_engagement_targets` table) — XP is included as a ratio against its own target rather than a flat present/absent check specifically so a practice's deliberately higher XP value (reflecting real effectiveness) still counts for more, without raw cross-feature XP-scale differences making the score unstable. Across all 6 sections: `daily_score = average(6 section_scores) × NN_multiplier` — Non-Negotiables is a **multiplier on top of**, not a 7th equally-weighted section, since "non-negotiable" means skipping it should hurt disproportionately (owner: "NN matlab NN"), with an explicit `[CRITICAL]` flag now also reaching Fortune's prompt when NN% drops below the partial threshold, not just a soft one-line mention. Momentum growth changed from a flat `+1`/day to `momentum + daily_score` (0 to ~1, same ceiling as before — every existing 7/14/21-day milestone still means the same thing). Momentum decay changed from `%/whole-calendar-day-off` to a continuous, accelerating curve computed in real hours since the last day that crossed `engaged_day_threshold_pct` (not just any day boundary): `pct_retained = exp(-decay_k × hours^decay_exponent)` — defaults tuned to ~95% retained at 24h, ~87% at 48h, ~77% at 72h, ~39% at 7 days, verified via direct calculation before shipping, not just eyeballed. Separately fixes the actual bug that started this discussion: Fortune's AI prompt never told the model how many days since the last *real* Clar conversation (computed from `clv_chat_summaries` dates, which only exist after an actual conversation — not just tab-opens), so a long silence read as neutral/current instead of a real decline; now explicit with a `[engagement gap]` flag at 3+ days, alongside a plain per-section "minutes today" breakdown as its own parallel fact set (deliberately *not* blended into momentum or XP — a second, real design correction: momentum stays one clean, debuggable number, not an all-in-one blend of time+XP+NN+gap that nobody could reason about). New `user_daily_log` columns (`section_minutes`/`section_xp`/`nn_pct`/`daily_score` jsonb/numeric — extends the existing v126 table rather than a parallel one, since it's already shaped exactly right for the future usage graphs/charts this is explicitly the foundation for) and two new tables (`section_engagement_targets`, single-row `engagement_settings`), both admin-editable — every constant (6× target minutes, 6× target XP, decay k/exponent, engaged-day threshold, 3 NN multiplier tiers) is a number in a form, nothing hardcoded, `db/schema_v186_engagement.sql`. New **"📊 Engagement" tab** in `admin.html` — target editors per section, a global-settings form, and a read-only formulas block written to match the code exactly, specifically so a future admin (or a future session) can understand what each number actually does without reading source. `xpAdd()` now also calls `_recordSectionXp()`, categorizing every XP award by whichever tab is currently active (`clv_last_tab`) — a reliable proxy since XP is only ever awarded while on the tab that earned it, and far simpler than tagging all 20 existing `xpAdd()` call sites individually. `workers/admin-relay-worker.js` gained 4 new CRUD actions. Verified in a live browser before shipping (not just Node): per-section timer correctly attributes minutes on real tab switches, XP correctly attributes to whichever tab was active, `computeDailyScore`/`_engagementDecayFactor` produce the exact numbers shown above, and the new admin tab renders and opens its edit forms correctly. `sw.js` `CACHE_VERSION` bumped `clv-v185`→`clv-v186`; `#prof-app-ver` → v186. **Live-verified end-to-end post-deploy (owner ran `db/schema_v186_engagement.sql`):** curl-confirmed both new tables populated with the seeded defaults and the new `user_daily_log` columns exist; confirmed the live site's `SHEETS_DATA` actually loads both new tables over Supabase (not just the hardcoded fallback); confirmed the per-section time tracker and XP categorizer both fire correctly on the real deployed site. Testing Fortune specifically surfaced a real, separate bug — see v187.
| v187 | Fix found while live-verifying v186's Fortune changes: the actual Gemini response (captured via the browser's Network panel, not guessed) showed `finishReason:"MAX_TOKENS"` with `thoughtsTokenCount:477` vs only `candidatesTokenCount:19` — Gemini's internal "thinking" tokens were eating nearly the entire 500-token budget, truncating the JSON after a few words ("energy_reading": "You're pouring significant energy into` and nothing else). This is the *exact* bug already fixed in chat's `_sendToAI()` and the admin Consultant/Sandbox (CLAUDE.md v161.x, `thinkingConfig:{thinkingBudget:0}`) — that fix was simply never applied to Fortune's own payload. Re-tested the exact same prompt shape directly against the live Worker with the fix added: `finishReason` changed to `"STOP"`, complete valid JSON, zero thinking tokens consumed, confirmed via curl before shipping. While in this exact code fixing this exact bug class, grepped every `generationConfig:{` call site in the file and found two more with the identical gap — Pulse's weekly-reflection call and the monthly-summary-compression call — both fixed the same way (the monthly-summary one omits `responseMimeType:'application/json'` since that call intentionally returns a plain-text paragraph, not JSON). `sw.js` `CACHE_VERSION` bumped `clv-v186`→`clv-v187`; `#prof-app-ver` → v187.
| v188 | Follow-up after live-testing v186/v187 on a real account: owner correctly observed that only "Clar notices" (the `concern` field) actually reflected real usage data — `energy_reading`/`near_prediction`/`far_prediction`/`watch_for` still read as generic feeling-narrative extrapolated from chat themes. Root cause confirmed by re-reading the actual prompt code: NN's `[CRITICAL]` flag was the *only* fact ever given an explicit directive telling the model to act on it — `chatGapContext` and `engagementBreakdown` (both added in v186) sat as passive context with no equivalent instruction, so the model never felt compelled to cite them. Owner's own framing: Fortune "should give analysis of user and show him his own mirror" — real facts, not just vibes. Added one new instruction block (`MIRROR:`) giving every signal (chat gap, momentum, per-section minutes, NN) the same explicit directive NN already had — verified by replaying a realistic scenario (3-day streak, NN 0%, 5-day chat gap, 0 min in goal today) directly against the live Gemini endpoint before shipping: the response now explicitly cites "3-day streak and 120 XP", "5 days since our last conversation", "0% this week", "0 minutes in the 'goal' section", and names specific sections in `watch_for` — a genuine before/after difference, not assumed. `sw.js` `CACHE_VERSION` bumped `clv-v187`→`clv-v188`; `#prof-app-ver` → v188.
| v189 | Same root-cause bug as v186/v188, found live by the owner in a *different* function this time: Pulse's weekly reflection (`_callAI()` in the Pulse block, distinct from Fortune's) — owner's real report: "jab maine week me kuch kiya hi nahi clar pe to ye ese kyu bol raha he" (reflection described feelings from an old conversation as if they were this week's). Root cause confirmed worse than Fortune's original gap: Pulse's summaries were stripped of dates entirely (`s.summary||s`, no date at all), and Pulse never received Non-Negotiables data in the first place, despite the Pulse card displaying "NN Done: X/7" right next to the reflection it generates. Fixed with the same treatment already verified for Fortune: dates restored to each summary, explicit chat-gap detection + a "may be outdated, do not describe as current" instruction, NN% added with the same `[CRITICAL]` flag, and an explicit instruction to ground the reflection in the real numbers rather than defaulting to old summary content. Verified against the live Gemini endpoint with the exact failure scenario (old summary, 10 min this week, NN 0%, 12-day gap) before shipping: the reflection now explicitly says "this engagement gap means the feeling... you shared on 08-09 might be outdated" and names the 0% NN directly — the precise fix for the reported symptom, not assumed. `sw.js` `CACHE_VERSION` bumped `clv-v188`→`clv-v189`; `#prof-app-ver` → v189.
| v190 | Follow-up on v188: owner asked to confirm the engagement gap actually shapes `near_prediction` (not just gets mentioned in passing), and explicitly wants Fortune to push the user toward re-engaging with Clar regularly, not just describe the gap as a consequence. Added a `THE GAP IS THE LEVER` instruction — only injected when `chatGapContext` actually flags a real gap — telling the model `near_prediction` must actively and directly recommend returning to regular Clar conversation as the concrete lever, not optional color. Verified against the live Gemini endpoint with the same 5-day-gap scenario used for v188: `near_prediction` now explicitly reads "the most immediate step is to re-engage with daily check-ins with Clar. Consistent, daily conversation can help you process what's truly holding you back," and `far_prediction` reinforces it ("if you embrace regular check-ins and recommit to your Non-Negotiables") — a direct, confirmed behavior change from v188's output, not assumed. `sw.js` `CACHE_VERSION` bumped `clv-v189`→`clv-v190`; `#prof-app-ver` → v190.
| v191 | Real regression in v185's own fix, found from a direct owner report ("naam friend dikha raha he" — name shows "Friend" even after signing in). Root cause: the v185 email-sync code ran directly inside `_updateAuthUI()`, which — traced through both call sites (`_setupAuthListener`'s SIGNED_IN branch and `initSync()`) — always fires **before** `_clearUserLocalStorage()`/`_pullUserData()` on a fresh sign-in, not after. That meant it was reading and pushing `clv_user_identity` (name, nick, etc.) from the *previous* session's still-present, stale localStorage, straight to the newly-signed-in account's Supabase row — overwriting a real account's stored name with stale/blank data on every sign-in, before the correct data had even been pulled down. Fixed by extracting the logic into a standalone `window._syncGoogleEmailIfNeeded(session)`, called explicitly only *after* `_pullUserData()` resolves, in both places that call it — never inside `_updateAuthUI()` itself again. Owner separately reported sessions auto-logging-out on phone — investigated `_ensureAuth()` (confirmed it silently falls back to a brand-new anonymous session, no attempt to restore the real one, whenever no active Supabase session is found — which would present exactly as "logged out" and is a plausible fit for mobile/PWA storage-eviction behavior) but this is **not yet confirmed as the specific cause** — no concrete reproduction found the way the name bug had one; needs more detail from the owner (does it happen after inactivity, PWA vs browser tab, every reopen or intermittent) before treating it as fixed. `sw.js` `CACHE_VERSION` bumped `clv-v190`→`clv-v191`; `#prof-app-ver` → v191.
| v192 | Owner reported the name/onboarding issue persisted even after v191, plus onboarding screens ("what should we call you") reappearing entirely — asked to diff against recent versions to find the real cause. Confirmed via git history that `_checkNick`/`saveNick`/`selectPersona`/the registration form (all in the onboarding flow) predate every recent change (last touched May 2026) — not a new regression, a pre-existing systemic gap: **none of the three onboarding-completion save points ever pushed their data to Supabase** — `saveNick()` and `selectPersona()` only ever wrote to localStorage, and the registration form only ever sent data to the legacy pre-Supabase-migration Google Sheets beacon. All three relied entirely on some later, unrelated trigger (an XP gain, a momentum update) to incidentally sync eventually. Since Google sign-in's `_clearUserLocalStorage()` wipes local onboarding state on every sign-in expecting `_pullUserData()` to restore it from Supabase, any account whose nickname/persona/registration never happened to get pushed before the next sign-in would have it come back empty — re-triggering the nickname screen and "Friend" fallback, repeatedly, exactly as reported. Fixed by adding `_syncPushProfile()` (the same, already-proven function used elsewhere) to all three save points — purely additive, nothing removed or restructured, matching the owner's explicit request to be careful not to break anything. Verified in a live browser before shipping: `saveNick()` and `selectPersona()` both confirmed to still behave identically (same localStorage values, same UI transitions) while now also calling the sync exactly once each. `sw.js` `CACHE_VERSION` bumped `clv-v191`→`clv-v192`; `#prof-app-ver` → v192.
| v193 | **Fortune tab gets "Your Insights"** — a new opt-in, collapsible charts panel (placement discussed and agreed with the owner: inside Fortune, below the AI oracle reading, so the daily-glance app stays uncluttered per its own "micro-action over comprehensive tracking" philosophy) with 4 charts: (1) Momentum trend line, (2) a 6-axis section-balance radar (Vibe/Chat/Goal/Self/Home/Fortune, using the real v186 Engagement Engine per-section time+XP ratios and admin-configured targets, not the abstract 5-dimension radar already on the AI reading above it — a different, more concrete chart, both now coexist), (3) a GitHub-style Non-Negotiables completion heatmap (green intensity = day's NN%), (4) Daily-performance bars (the Engagement Engine's own 0–1 `daily_score`, purple→gold gradient by value). Own timeline selector — 3 Days / This Week / This Month / Custom (two date inputs + Go) — all four charts redraw together on any range change. Data source: queries `user_daily_log` directly from Supabase for the selected range (RLS already scopes it to the signed-in user, same table the Engagement Engine already populates nightly — no new table, no new schema); since that table only gets a finalized row at day-rollover, today's row is synthesized read-only from the Engagement Engine's own live local trackers (`clv_section_minutes_today`/`clv_section_xp_today`/`c9_nnhist`/`clv_momentum`) via the already-exposed `window._computeDailyScore`, so "today" isn't blank until tomorrow. New `window._engagementTargets` export (the existing `_targets()` function, previously internal to the Engagement Engine script) lets the radar read the same admin-configured per-section targets used by the real scoring formula instead of guessing its own. Entirely new, self-contained script block (`ftChartsToggle`/`ftChartsSetRange`/`ftChartsApplyCustom` + 4 draw functions) placed after the Fortuneteller script closes — does not touch the AI oracle call, the existing 5-dimension radar, or any Pulse/Fortune logic. Verified live via Chrome DevTools MCP against the real Supabase project before shipping: confirmed the query pipeline genuinely round-trips (a fresh test account correctly returned 0 rows, not a silent failure disguised as zero), then seeded 7 real rows under that same test account's own uid and confirmed all 4 charts render correctly against real data (radar percentages matched the seeded ratios, heatmap intensity matched seeded NN%, bar colors correctly shifted purple→gold with score) before deleting the seeded test rows again. `sw.js` `CACHE_VERSION` bumped `clv-v192`→`clv-v193`; `#prof-app-ver` → v193.
| v194 | Three real bugs from a direct owner report ("8 din ka streak dikha raha he but maine to itna use kiya hi nahi... ulta aaj-kal use kiya to bhi 8 din ka gap bata raha he, graphs bhi sab zero"), all root-caused in code before fixing, not guessed. **(1) Momentum/Engagement-Engine finalization was gated behind 100% Daily-Quest completion.** `_bumpStreak()` — which runs momentum growth/decay AND `_engagementFinalizeAndReset()`'s push to `user_daily_log` — had exactly one call site: `dqComplete()`, only reached once every single item in the small home-widget Daily Quest checklist was ticked. `computeDailyScore` never even reads quest-completion state, so this gating was never an intentional part of the v186 design — it was inherited unexamined from the pre-Engagement-Engine flat-streak system `_bumpStreak` was originally built for. Net effect for any real day quests aren't fully completed (the common case): momentum's decay curve never evaluates (so it can sit stuck at an old value that no longer reflects real usage) and no row is written to `user_daily_log` (so v193's charts render empty) — both exactly matching the report. Fixed by also calling `_bumpStreak()` once per day on boot (`DOMContentLoaded`, alongside the existing `dqRender`/`_refreshStreakDisplay` calls), decoupled from quest completion; the function's own `SD_KEY` same-day guard makes this safe to run alongside the pre-existing `dqComplete` call without double-applying. **(2) Chat-gap detection only ever read `clv_chat_summaries`**, a compressed digest that lags by design — `initChat()` only compresses *yesterday's* session into a summary on *today's* first open, and only if that day's history had more than one message — so genuine same-day/previous-day Clar conversations can still show as an old gap if the compression step hasn't caught up yet. Fixed by adding `window._lastRealClarDate()` (new export in the CLAR TIME TRACKING block) reading `clv_clar_times` — real per-day minutes, written the moment a session ends, independent of the summary pipeline — and having both Fortune's and Pulse's gap calculations take whichever of the two sources is more recent. Verified in an isolated logic test replaying the exact reported scenario (real Clar minutes logged today+yesterday, summary trail stuck 8 days back): old logic → 8-day gap reported; fixed logic → 0-day gap, correctly resolved from the real minutes log. **(3) The streak/momentum number and the chat-gap line were never explained as measuring different things** — momentum is deliberately whole-app engagement (Vibe/Goal/Self/Home/Fortune/Clar combined, a deliberate decision from v179) while the chat-gap is Clar-conversations specifically, so a solid streak alongside a real chat gap is expected, legitimate behavior — but neither prompt ever told the model this, so readings could narrate them as if contradictory. Added an explicit `NOTE:` line to both Fortune's and Pulse's system prompts stating the distinction and instructing the model to say so plainly rather than treat the two as contradictory. Also fixed the **charts-showing-zero** symptom at its second, independent cause found while investigating: v193's `_fetchRange` gave up immediately if `window._sbShared`/`_sbUid` weren't ready yet, indistinguishable from "genuinely no data" — the exact class of startup race already documented for `_getSb()` in CLAUDE.md v163/v164, just never applied here since the charts panel is brand new. Now retries every 150ms for up to 3s before accepting empty as real. Not yet re-verified live in a browser (Chrome DevTools MCP was disconnected when this shipped) — logic-level fixes verified via isolated Node replay of the exact reported scenario and a full script-block syntax check; owner asked to confirm against real usage over the next day or two. `sw.js` `CACHE_VERSION` bumped `clv-v193`→`clv-v194`; `#prof-app-ver` → v194.
| v195 | Owner pushed back on v194's fix #3 within the same session: rather than just explaining in the prompt that streak (whole-app) and chat-gap (Clar-specific) are different signals, unify them — the streak/momentum number was already whole-app engagement by deliberate v179 design, so the "gap" should read the same way instead of narrowly tracking Clar conversations. Switched Fortune's and Pulse's gap detection from `clv_chat_summaries`/`clv_clar_times` to `clv_last_engaged_at` — the Engagement Engine's own record of the last day `daily_score` crossed the "genuinely engaged" bar, the same value momentum decay itself resets on — so streak and gap can no longer disagree; they're the same underlying measurement now. Removed v194's now-obsolete "these are different signals" NOTE from both prompts since there's nothing left to reconcile. Tradeoff made explicit and accepted: Fortune can no longer specifically flag "you're active elsewhere but avoiding Clar conversation" — the LEVER instruction still recommends returning to Clar conversation whenever a gap fires (it remains the app's core re-engagement tool) but no longer implies Clar chat specifically was what lapsed. **Live-testing this surfaced a second, more consequential bug** in the same code path: `_engagementFinalizeAndReset()`'s Supabase push (the write that feeds both `clv_last_engaged_at` and the v193 charts) had no retry for `window._sbShared`/`_sbUid` readiness — same race class already fixed for the charts' read side in v194, but never applied to this write side. Confirmed via a Supabase-call trace (a proxy wrapping `.from().upsert()` to log every real call): on a cold boot, `_bumpStreak` correctly advanced `SD_KEY` to today (proving the function ran) while zero upsert call was ever actually made — sb/uid were still resolving at that exact 300ms mark. This meant even v194's "run finalization daily on boot" fix could silently no-op on exactly the kind of slower/cold load it was meant to fix. Fixed with the same 150ms-retry-for-3s pattern as the charts fetch, applied only to the fire-and-forget push (the function's synchronous return value never depended on Supabase, so momentum math is unaffected). Verified by reproducing the exact race live: cleared `user_daily_log` + `clar_streak_date`, reloaded, confirmed old code left zero rows after 4s; same test against the fixed code produced a real row within ~1.5s. `sw.js` `CACHE_VERSION` bumped `clv-v194`→`clv-v195`; `#prof-app-ver` → v195.
| v196 | **Momentum retired entirely**, at the owner's own initiative: "maine momentum app ke initial phase me banaya tha kyuki tab performance calculate karne ka aur koi tarika nahi tha... par ab ham har practice ko alag alag monitor kar rahe he" (momentum was built in the app's early phase because there was no other way to measure performance then; now every practice is tracked individually) — and separately, "mujhe to wo aur momentum ek hi dikh rahe he" (streak and momentum look like the same thing to me). Both correct: `clar_streak` was the pre-v125 flat streak, momentum (v125) replaced it with a decaying scalar, and v186 bolted the new Engagement Engine's real per-day records into that same scalar's growth/decay formula instead of ever actually retiring it — leaving a mutated, stored number layered on top of data that was already sufficient on its own, and directly responsible for 3 of this session's bugs (quest-gating, the write race, gap staleness). Fix: momentum is no longer stored or mutated anywhere — the streak is computed **fresh, every time, directly from real records**. New local cache `clv_engaged_days` (`{date,engaged,score}`, mirrors `user_daily_log`) is rebuilt from real Supabase rows on load (`window._rebuildEngagedDaysFromRemote`, cross-device-correct by construction, no separate sync path needed since it's fully derived); `window._realStreak()` walks backward from today (live-checked via new `window._liveTodayEngagement()` so a streak already extended today shows immediately, not after tonight's rollover) until the first non-engaged day; `window._daysSinceEngaged()` replaces v195's `clv_last_engaged_at` timestamp with the same fresh-from-records treatment. The weekly freeze mechanic is preserved but now marks explicit covered dates (`clv_freeze_covered_dates`) that count as engaged, rather than resetting a decay clock. Milestones (7/14/21) now fire off the real streak. Every display site was rewired: Home's 🔥 badge, the XP-bar streak number (two separate renderers), Pulse's "Streak" metric card + sparkline (now real `daily_score` history, not a frozen momentum trend), Fortune's energy signal / pattern-map "Belief Align" dimension / PERSON line / field-signal tooltip / share-canvas big number, and the v193 "Your Insights" panel's Streak line chart. That last one turned up a second, independent latent bug while auditing every `clv_momentum` read: the chart's `streak_day` field was written only by `_logDailyEntry()`, a function that has never actually run — it calls `window._getSb`/`window._getUid`, which don't exist as globals anywhere in this file — so the chart was silently flat/zero for every real row regardless of usage. Fixed by computing the line from a rolling count of consecutive engaged days within the fetched window, using the real `daily_score` field the other 3 charts already fetch. **Live-tested this before shipping and caught a real, separate bug of its own**: the new backward-day-walk functions mixed local-time `Date` arithmetic (`setDate`/`getDate`) with a UTC `toISOString()` read to build date-string keys — harmless in UTC, but for any positive UTC-offset timezone (confirmed live against the actual IST browser environment this owner runs in) this silently shifts every computed date back by one full day. Caught via a seeded freeze-prompt test that produced dates one day off from what was intended; reproduced the exact mechanism in an isolated Node script with `TZ=Asia/Kolkata` before and after the fix to confirm it, then re-ran the identical live browser scenario against the corrected code and got the right dates. Fixed by switching all three affected functions (`_computeRealStreak`, `_computeDaysSinceEngaged`, `_dailyEngagementCheck`'s missed-dates calc) to pure UTC-day arithmetic (`setUTCDate`/`getUTCDate`, with `Date.UTC(...)` re-anchoring where a local-parsed date needed to enter that space), matching the UTC-calendar-day convention `user_daily_log`/`clv_engaged_days` already use via `todayKey()`. `clv_engaged_days`/`clv_freeze_covered_dates` added to the sign-out clearing list alongside every other per-account key. Old momentum keys/functions left defined but no longer called anywhere in the display/boot path (matches this file's own established convention of not deleting still-harmless code); the Supabase sync payload for the old `momentum`/`streak` fields was deliberately left untouched rather than risking the shared payload function, so those columns simply stop updating rather than being actively migrated. `sw.js` `CACHE_VERSION` bumped `clv-v195`→`clv-v196`; `#prof-app-ver` → v196.
| v197 | Owner reported charts still blank after v196. Root cause: `_dailyEngagementCheck` (the daily finalize+push) only ever runs ONCE per calendar day, gated by `clar_streak_date`. If that gate was satisfied by an earlier run on the SAME calendar day — one that predated v194/v195's fixes, or whose Supabase write failed for any transient reason — every later reload that day skips straight past the guard and never retries, no matter how correct the code now is. This meant verifying a same-day fix required waiting for a whole new calendar day, which is exactly the slow feedback loop that made this look unfixed across several reports. Added `_verifyTodayPushedOrRetry()`, called independently of the SD_KEY guard on every boot: checks whether today's row genuinely exists in `user_daily_log` and, if not, re-runs `_engagementFinalizeAndReset()` immediately regardless of what the local guard says. Safe to call repeatedly — the push is a plain upsert keyed on `(user_id,date)`, so re-finalizing "today" again just overwrites it with the current live snapshot. **Live-verified the exact failure mode before shipping**: manually set `clar_streak_date` to today with zero rows in `user_daily_log` (reproducing "guard satisfied, write never landed"), reloaded, and confirmed the old code left it blank while the new code produced a real row within ~2s without needing a day boundary. `sw.js` `CACHE_VERSION` bumped `clv-v196`→`clv-v197`; `#prof-app-ver` → v197.
| v198 | Owner reported charts still dead even after v197 — on a genuinely new calendar day this time, ruling out v197's specific fix as the remaining cause. Rather than guess a 6th time, audited every Supabase call on the engagement-charts path and found the actual gap: every one of them was fire-and-forget with its result discarded (`.then(function(){})`), so a real RLS/permission/network error on the owner's real account would look byte-for-byte identical to a clean, silent success — there was no way, for the owner or for me investigating remotely, to distinguish "genuinely no data yet" from "the write or read is actually failing for a reason nobody has seen." Added: (1) real error capture on the daily-log push, the verify-and-retry pass, and the charts' own fetch — each now records a plain-language note (`window._clvLastPushNote` / `_clvLastVerifyNote` / `_clvLastChartsNote`) instead of swallowing the result, and `console.warn`s on failure; (2) `window._clvDebugEngagement()`, a one-shot diagnostic callable from any browser console that dumps uid, session state, local cache, computed streak/gap, and a live 10-row Supabase read — a single command that replaces another guess with an actual answer; (3) a visible status line in the charts panel itself (reusing the existing loading element) that shows a real reason ("Could not load your data — fetch error: …") instead of just going quiet when a range's fetch returns zero rows, since the owner's real device may not have easy console access; (4) both retry windows (charts fetch, daily-log push) widened 3s→8s as a safety margin against slower real-world conditions than this file's own test environment. Live-verified the plumbing before shipping: confirmed a genuine Supabase error surfaces with a real `.message` (tested via a deliberately malformed column query), confirmed the empty-state message renders correctly when a range has zero real rows, and confirmed the success path still renders all 4 charts with zero regression. This version does not claim to have found the root cause — it's built specifically so the *next* report comes with an actual error message or diagnostic dump instead of just "still dead." `sw.js` `CACHE_VERSION` bumped `clv-v197`→`clv-v198`; `#prof-app-ver` → v198.
| v199 | The v198 diagnostic did its job — owner ran `window._clvDebugEngagement()` on the real account and it confirmed `remoteError: null`, a genuine successful read, with exactly one real row on record (score ~4%, correctly below the engaged threshold). Root cause of "charts still dead" was never a bug at all: the engine had only been reliably writing since v197/v198 shipped, so there was authentically almost no history yet — a graph can't show a trend from one point. Per the owner's explicit request, the charts panel's empty-state message was rewritten from a neutral "no data" line to something that both states the real reason and motivates the actual fix: "Not enough data yet to form this graph — talk to Clar as much as you can, and it will fill in fast," matching the app's existing pattern of treating Clar conversation as the core re-engagement lever (same framing as Fortune's "the gap is the lever" instruction). Threshold changed from `rows.length===0` to `rows.length<2`, since a single real day still isn't enough to draw a trend from either — the same message now covers both cases; the distinct "could not load — [real error]" message is preserved unchanged for genuine fetch failures. Live-verified all three thresholds against real Supabase writes before shipping: 0 rows → message shown, 1 row → message still shown, 2 rows → message hidden and charts render normally. `sw.js` `CACHE_VERSION` bumped `clv-v198`→`clv-v199`; `#prof-app-ver` → v199.
| v200 | **Goal-matched YouTube Shorts** — first piece of the broader "visual/interactive platform, but pulling content by the user's stated goals rather than algorithmic engagement bait" direction discussed at length with the owner. Researched feasibility across YouTube/Spotify/Pinterest/Instagram/Facebook before building anything: Instagram/Facebook ruled out entirely (Meta closed third-party content discovery deliberately — Instagram Basic Display API was shut down Dec 2024, no discovery/search endpoint has ever existed for outside apps, and the only workaround is scraping, which violates ToS and carries real legal exposure — not attempted); Pinterest ruled out the same way after verifying live (official API v5 confirmed to have no public search endpoint at all, only account-management for your own pins — the only keyword-search options are unofficial third-party scrapers, same risk class as Meta); YouTube confirmed genuinely workable (verified live: `search.list` costs 100 units/call against a 10,000/day default quota, `videos.list` for duration detail costs only 1 unit). Built as: (1) `workers/youtube-relay-worker.js` (new, deliberately NOT token-gated like admin-relay-worker since regular users call it, not just admin.html) — searches YouTube once per **topic** (10 fixed topics: business, manifestation, law_of_attraction, fitness, mindfulness, discipline, confidence, relationships, productivity, spirituality), not per user, filters results to ≤200s duration (YouTube expanded Shorts eligibility to 3 minutes in late 2024; no official "isShort" API filter exists, this is the closest real proxy), and caches results in Supabase for 24h — the whole app's real YouTube quota usage stays a couple dozen searches/day regardless of user count. (2) `db/schema_v200_youtube.sql` — `youtube_topic_cache` table, public-SELECT/service_role-write, same lockdown pattern as every reference table since v178. (3) Client-side: the user's Major Definite Goal text is matched to a topic via plain keyword lists (deterministic, zero AI-call cost for v1 — no Gemini classification yet, that's a clean upgrade path later) and shown as a horizontal strip of vertical thumbnails inside the Major Definite Goal accordion, lazy-loaded on open via the same pattern v155 already established for vision images (`goalToggle` gained one more conditional call, not a rewrite); tapping a thumbnail opens a full-screen `youtube.com/embed` player. **Live-testing this surfaced a real, non-obvious bug**: the embed failed with YouTube's "Error 153 — Video player configuration error" when tested over `file://` (no origin/referrer to send at all) — researched the exact cause (confirmed via search: missing/blocked referrer or origin headers, a documented YouTube embed failure mode, not specific to this app) and added `referrerpolicy="strict-origin-when-cross-origin"` to the iframe; re-verified by spinning up a real local HTTP server (`http://localhost`, matching this project's own established pattern for origin-sensitive testing already documented in the Browser Testing Setup section) rather than trusting the fix blind — confirmed real playback (visually verified the actual video rendering) once served from a genuine HTTP origin, exactly matching how GitHub Pages will serve it. **Owner must still, manually (cannot be done by Claude — requires their own Google Cloud/Cloudflare account access):** create a YouTube Data API v3 key in Google Cloud Console (APIs & Services → Credentials, with "YouTube Data API v3" enabled, key restricted to that API), deploy `workers/youtube-relay-worker.js` as a new Cloudflare Worker with `YOUTUBE_API_KEY` + `SUPABASE_SERVICE_KEY` secrets set, paste that worker's real URL into `WORKER_URL` in this feature's script block (currently a placeholder `https://clarvoyance-youtube.smworkassistance.workers.dev/`), and run `db/schema_v200_youtube.sql` in the Supabase SQL editor. Until those steps are done, the section shows "No videos found right now" gracefully (confirmed via live test) rather than breaking anything. `sw.js` `CACHE_VERSION` bumped `clv-v199`→`clv-v200`; `#prof-app-ver` → v200.
| v201 | v200's manual setup completed by the owner (YouTube API key created, `youtube-relay-worker.js` deployed as **`clar-youtube`**, `db/schema_v200_youtube.sql` run) — real deployed URL is `https://clar-youtube.smworkassistance.workers.dev/`, pasted into the `WORKER_URL` placeholder. Setup hit three real snags along the way, each root-caused rather than guessed at: (1) worker initially still ran Cloudflare's default "Hello World" template — code had been created but not actually pasted/deployed yet; (2) `SUPABASE_SERVICE_KEY` secret returned Supabase's own "Invalid API key" error even after re-pasting the value and confirming both the Supabase project URL and the exact variable names matched — root cause turned out to be that Cloudflare had saved the secret without pushing it into the *live* deployment; an explicit redeploy fixed it; (3) once auth worked, hit "Could not find the table 'public.youtube_topic_cache'" — the SQL migration simply hadn't been run yet, fixed by running it. **Live-verified via curl against the real production worker before shipping**: first request returned `source:"live"` with real YouTube Shorts (titles/thumbnails/channel names, all ≤200s), the identical repeat request returned `source:"cache"` — confirming both the search path and the 24h cache path work correctly end to end. Also live-verified the client integration itself against the real worker (via a local HTTP server, not file://, matching this project's own established origin-sensitive testing pattern): set a goal mentioning "law of attraction," opened Major Definite Goal, and got back real, correctly-matched Law of Attraction videos rendered in the strip. `sw.js` `CACHE_VERSION` bumped `clv-v200`→`clv-v201`; `#prof-app-ver` → v201.
| v202 | **Vibe Feed video cards** — the goal-matched YouTube videos now also appear directly in the core Vibe Feed, not just the Goal-tab strip, per the owner's explicit spec after a "discuss first" round: a video every 2-3 regular cards (not stricter 1:1 alternation, to protect the feed's own "under 2 minutes" character), autoplay immediately (no tap-to-play), XP unlocks at 50% actually watched (not a flat timer), and repeats avoided where possible. Implementation: (1) real YouTube IFrame Player API (not a plain iframe embed) — required specifically because 50%-watched needs live `getCurrentTime()/getDuration()`, which a bare embed can't expose; polling reuses the existing `vfChalInterval` slot BREATHING/MOVEMENT already use for their countdowns, so the existing per-card cleanup already handles it. (2) `vfPickCard`/`vfRender`/`vfInit` wrapped (not edited) — same technique already used elsewhere in this file (e.g. v125's `bnavSwitch` wrap) — keeping the whole feature removable without touching the base Vibe Feed card system; `VF_PTS`/`VF_BG`/`VF_CBG`/`VF_TYPE_LABELS` gained a `VIDEO` entry via direct mutation (`const` only locks the binding, not the object). (3) `clv_video_seen_ids` (rolling 200-id cap) avoids repeats, falling back to allowing them once a topic's whole cached pool has been seen rather than ever showing nothing. (4) Topic source made flexible per the owner's ask ("ye flexible rahne dena, list se choose karwa lenge ya AI predict karega") — a new `⚙` picker (shared between the Goal-tab strip and the feed) lets the user explicitly choose topics, saved to `clv_video_interests`; explicit choice wins, otherwise falls back to the existing goal-text keyword match. AI-topic-prediction was discussed as a third source but deliberately not built this pass — the selection function is the one seam it would slot into later. Reused the Goal-tab script's `TOPICS` vocabulary and matcher via two new `window._videoTopics`/`window._matchGoalToTopics` exports rather than duplicating them. **Shipped a real bug and caught it before calling this done**: the first version silently did nothing at all — none of the wraps took effect, `videoInterestsOpen` didn't even exist — traced to an HTML comment closed with a JS-style `*/` instead of `-->` right before the new `<script>` tag, which left the comment open and caused the browser's HTML parser to swallow the entire script block as inert comment text (confirmed by grepping the raw file, not guessed). Fixed the single character sequence and re-verified live from scratch. **Live-verified thoroughly after the fix** (via a real local HTTP server, matching this project's established origin-sensitive testing pattern — the IFrame API needs a real origin same as the plain embed did in v200/v201): confirmed a VIDEO card appeared within 2 cards of opening Vibe Feed, confirmed it was a real `youtube.com/embed` iframe with `autoplay=1&mute=1&enablejsapi=1` actually inserted by `YT.Player` (not a static thumbnail), confirmed XP correctly stayed locked immediately after the card appeared (not pre-unlocked), confirmed swiping away before completion still applies the existing −5 XP skip penalty unchanged, and confirmed the topic picker correctly saves selections to `clv_video_interests`. `sw.js` `CACHE_VERSION` bumped `clv-v201`→`clv-v202`; `#prof-app-ver` → v202.
| v203 | **Extended user interest profile** — unifies `clv_user_profile`, the fragmented per-context `clv_fnd_*` "My Tribe" lists (v112), and v202's standalone `clv_video_interests` into one object the owner can see and edit, per a "discuss first" round that also asked Claude to verify two things rather than assume: confirmed `clv_user_profile` was still genuinely alive (read by chat, has a real if narrow auto-update instruction), and confirmed "My Tribe" really was fragmented and never synced to Supabase (grepped for the `clv_my_why`/`clv_my_tribe` keys an earlier changelog entry claimed existed — zero matches; real storage is per-context `clv_fnd_<key>`, explaining the owner's own complaint about not being able to keep it updated). New profile shape adds `mentors[]` (`{name,category,handle,link}`), `vision_topics[]`, `strengths[]`, `weaknesses[]` alongside the 4 existing scalar fields. One-time `_migrateOnce()` (gated by `clv_profile_v203_migrated`) sweeps every `clv_fnd_*` key's `tribe[]` into `mentors` (case-insensitive dedup across all of them) and folds v202's `clv_video_interests` into `vision_topics` — **live-verified**: seeded two different Foundation contexts with an overlapping name in different casing, confirmed the migration deduped correctly and preserved existing scalar fields. New "My Interests" card in Profile → Clar tab (topic chips + add/remove tag lists for mentors/strengths/weaknesses) via `window.profAddTag/profRemoveTag`, wired in without touching `_doOpenProfile`/`saveProfile`'s own code (wrap pattern, as elsewhere in this file). `foundationSave()` similarly wrapped to mirror any new tribe name into the same unified `mentors` list the moment the owner saves a Foundation panel — Tribe no longer needs separate upkeep. A new shared `window._activeVideoTopics(goalText)` replaces both v202's Vibe-Feed-only topic picker and the Goal-tab strip's goal-text-only matcher: explicit `vision_topics` win if set, else falls back to goal-text keyword match, and every named mentor is *always* appended as its own `mentor_<slug>` topic (`"<name> motivation advice shorts"`) regardless of the other source — **live-confirmed** pulling a real David Goggins video via this exact path. Two new topics added to the shared `TOPICS` vocabulary (`wealth`, `happiness`). Video UX changed per explicit correction mid-session: unmuted by default (`mute=0`, both the Vibe Feed player and the Goal-tab overlay), expands to fill the screen the instant `PLAYING` fires (not tap-triggered), and loops forever (`loop:1`+matching `playlist:<same id>`) until the user manually swipes to the next card — no shrink-back-on-`ENDED` logic, since looping means `ENDED` never fires. New ♡/♥ "more like this" like buttons in both the Vibe Feed video card and the Goal-tab strip (hidden on mentor-specific videos, where "more like this topic" doesn't apply) call `window._profileAddOnly()` to add the liked topic to `vision_topics` — pure append, silently no-ops if already present. Clar's own profile-reading and -writing behavior extended and, per the owner's explicit instruction ("clar ai should add data to users form but before removing or editing it should ask"), deliberately asymmetric: the system prompt now surfaces `mentors`/`vision_topics`/`strengths`/`weaknesses` alongside the 4 existing fields, and the response JSON gained a separate `profile_additions` field (mentors/strengths/weaknesses/vision_topics, pure append) alongside the existing `profile_update` (the 4 scalars). `profile_update`'s own handling was tightened at the same time — the live prompt text was found to only ever have told the model to infer `present_challenge`, never the other 3 fields, a real drift from the feature's original v53 design never previously caught. Per this file's own documented lesson from v165/v166 (prompting alone didn't stop `frameworks_used` fabrication — a deterministic code-level filter was needed), the add-only/no-silent-overwrite rule is enforced in code, not just prompted: the response-parsing code only ever writes a `profile_update` field into an *empty* slot, silently dropping it otherwise (the model is told to say so in-conversation instead), and all `profile_additions` route through the same `_profileAddOnly()` the UI buttons use, which can only ever push new, deduped entries — there is no code path anywhere that lets Clar edit or clear an existing value. **Two real bugs found and fixed during live testing, not just a syntax check** (this file's own v202 precedent — a syntax-clean script that silently did nothing — was treated as a standing reason to test live before shipping, not just once): (1) `saveProfile()`'s wrapper only re-preserved `vision_topics` after the original function's whole-object overwrite, silently wiping `mentors`/`strengths`/`weaknesses` on every profile save — reproduced live (seeded a mentor + a strength, saved from the UI, watched both vanish) then fixed by snapshotting all three extended fields before the original save runs and restoring them after, alongside `vision_topics`; re-verified the same scenario post-fix. (2) The video topic pool was only ever re-picked once per `vfInit()` call (i.e. once per Vibe Feed session) — with 3 active topics, a user could get stuck seeing only one topic's videos for an entire session purely by the luck of that one random pick, undermining the "flexible, multi-source topics" point of this whole feature; reproduced live (7 video cards in a row, all the same mentor topic) and fixed by re-rolling the pool in the background after each video is shown, so the *next* video 2-3 cards later has a real chance to draw from a different active topic — re-verified live (topics visibly alternated across the next several video cards). Also live-verified: real 50%-watched XP unlock firing during actual playback (polled `vfXpUnlocked` against a real timer, confirmed it flipped `true` partway through a real video, not before); Goal-tab strip's like button and looping overlay; multiple simultaneous mentor topics (added a second mentor mid-test, confirmed its videos started appearing too). `sw.js` `CACHE_VERSION` bumped `clv-v202`→`clv-v203`; `#prof-app-ver` → v203.
| v204 | Three follow-up corrections from the owner after using v203: (1) **All four "My Interests" form fields now feed video-fetching, not just vision_topics/mentors** — `strengths`/`weaknesses` were previously context-only for Clar's chat prompt and had no effect on which YouTube topics got pulled. Owner's explicit example: a weakness like "timidity" should pull videos to *resolve* it. `window._activeVideoTopics()` now also always appends `strength_<slug>` (query: "how to build on `<strength>` strength growth shorts") and `weakness_<slug>` (query: "how to overcome `<weakness>` shorts") alongside the existing vision-topic/goal-match and mentor entries — **live-verified** against the real worker: a seeded weakness of "Timidity" pulled back real, correctly-matched videos ("5 Powerful Tips to Overcome Shyness and Boost Confidence", "Try this if you're afraid of public speaking"). New `window._profileStrengths()`/`_profileWeaknesses()` exports added alongside the existing `_profileMentors()`/`_profileVisionTopics()`. (2) **Swipe-to-skip now works on the full-screen video itself** — previously the existing Vibe Feed swipe-up gesture (bound to the card area) silently never fired while a finger was over the video, because a cross-origin `youtube.com/embed` iframe is its own browsing context and never bubbles touch events to the parent page at all (confirmed this is *why*, not guessed). Fixed with a transparent `.vf-video-touch-ov` overlay div sitting on top of the iframe (z-index between the iframe and the existing like button, so the like button stays clickable — verified via `elementFromPoint` resolving to the button, not the overlay, at the button's exact screen position) with its own touchstart/touchend listener calling `vfNext()` on the same >50px upward-swipe threshold the rest of the feed already uses — **live-verified** with synthesized `Touch`/`TouchEvent` objects: a 100px swipe advances the card, a 30px swipe correctly does not. (3) **Videos no longer repeat once a topic's pool is exhausted** — `_pickVideo()` previously fell back to re-serving already-seen videos from the same 12-video cached pool once every one had been shown ("pool exhausted — repeat rather than show nothing", a deliberate v202 choice the owner has now explicitly reversed: "only new videos should come in feed"). `_pickVideo()` now returns `null` instead of repeating; `_ensurePool()` gained an `excludeTopic` param so `vfPickCard` can force a *different* active topic in the background the moment the current one comes up empty, rather than being stuck retrying the same exhausted topic every cycle. **Live-verified** with two independent clean-state runs (21 videos across 50 card-advances, then 42 across 100) — zero duplicate video IDs in either; separately confirmed the true-exhaustion end state behaves correctly: after burning through all 48 videos across 4 active test topics (4×12, the worker's `RESULTS_PER_TOPIC`) via heavy manual test iteration, the feed correctly stopped showing video cards entirely rather than repeating — the intended tradeoff (no video is better than a repeat), not a bug. `sw.js` `CACHE_VERSION` bumped `clv-v203`→`clv-v204`; `#prof-app-ver` → v204.
| v205 | Two more owner asks, both scoped to what's buildable without new external dependencies (a Pinterest/Spotify discussion was deferred — owner explicitly said "do this first" about these two, while asleep and unavailable for further clarification, so scope was kept to what's safely buildable and verifiable solo). **(1) Books.** New "Books you like" field in My Interests (Profile → Clar), same add/remove pattern as mentors/strengths/weaknesses; `window._profileBooks()` export added alongside the existing profile getters. Feeds video content two ways: (a) each book is appended to `_activeVideoTopics()` as its own `book_<slug>` topic (query: "`<title>` book summary key lessons shorts") — "book summary" is a real, common YouTube Shorts genre, so this reuses the existing video pipeline with zero new infrastructure, **live-verified** pulling real, correctly-matched Atomic Habits summary Shorts; (b) tapping a book tag generates (and caches) a short AI summary + 2-3 quotes via the *existing* Gemini Cloudflare Worker that already powers Clar AI chat (`cold-frog-d555`) — no new API or worker needed. Per this file's own v165/v166 lesson (an AI told to give "real quotes" will sometimes fabricate a plausible one rather than admit uncertainty), the prompt explicitly offers a takeaway as the honest fallback instead of a fabricated quote. **Live-verified** against the real Gemini endpoint for "Atomic Habits" — returned an accurate summary plus two genuinely real, correctly-attributed quotes and one takeaway (not a third fabricated quote). Caught and fixed one real rendering bug from that same test: the model's own returned quote strings sometimes already include their own quote-mark characters, which combined with the display template's own wrapping quotes produced visibly doubled marks (`""…""`) — fixed by stripping any pre-existing leading/trailing quote characters before display, re-verified clean. `saveProfile()`'s existing extended-field-preservation wrapper (from v203/v204) extended to also snapshot/restore `books`. Clar's system prompt surfaces books alongside mentors/strengths/weaknesses, and `profile_additions`/`_profileAddOnly()` both extended to accept `books` through the same add-only, never-silently-overwrite path already enforced for the other fields. **(2) Video pool "never dries up".** Root cause of the v204 "no video shown once exhausted" behavior: `youtube_topic_cache` reads were capped at `RESULTS_PER_TOPIC` (12) rows and writes were gated behind a 24h-freshness check that discarded/ignored anything older — meaning a topic's total available pool was hard-capped at whatever the first search returned, forever, regardless of how many more genuinely relevant Shorts actually exist on YouTube for that query. `workers/youtube-relay-worker.js` rewritten: cache reads (`fetchAllFromCache`) now return everything cached for a topic (capped generously at 300, not 12, and with no more age-based discarding — a video fetched days ago that hasn't been watched yet is still good content, discarding it by age was actively working against "never dry"); a new `more:true` request action (`growTopicPool`) fetches the *next* YouTube search page (real pagination via `pageToken`, not a fresh page-1 re-search) and *appends* to the cache rather than replacing it, tracked per-topic in a new `youtube_topic_state` table (`db/schema_v205_video_pagination.sql`: topic, original query, next_page_token, exhausted flag) so a later `more:true` call knows exactly where to resume without the client needing to resend the query. Deliberately quota-conscious: once YouTube itself stops returning a `nextPageToken` for a topic, `exhausted` is set and every subsequent `more:true` for that topic is a pure no-op (zero additional YouTube API calls) rather than retrying forever. **Verified via an isolated mocked-fetch test harness (real worker code, `import()`ed directly, YouTube/Supabase calls intercepted)** simulating 3 pages of results: call 1 (empty cache) did one real search and cached page 1; call 2 (plain re-read) served from cache with zero API calls; two successive `more:true` calls correctly fetched pages 2 and 3 and appended (pool grew 3→6→8 videos); a further `more:true` after the last page correctly detected exhaustion and made zero additional YouTube calls — the exact state machine intended, confirmed mechanically rather than assumed. Client side: new `_growPool(topic)` (fire-and-forget `{topic, more:true}` POST — safe against the old, not-yet-redeployed worker too, since it would just ignore the unknown field) is called from `vfPickCard` in two places — opportunistically once a topic's remaining unseen count drops to ≤3 (so it's topped up *before* running dry, not just after), and reactively on the exhaustion branch that already existed from v204. **Live-verified the client wiring** by intercepting `window.fetch`: with pools already depleted from heavy testing, 150 card-advances produced 75 real `{topic,more:true}` requests — confirms both trigger points fire correctly; a clean-pool run correctly produced zero (nothing needed growing yet). **Not yet live in production** — like every other Worker change in this project, `workers/youtube-relay-worker.js` needs the owner's manual paste-and-redeploy into the existing `clar-youtube` Cloudflare Worker, and `db/schema_v205_video_pagination.sql` needs to be run in the Supabase SQL editor, before the pagination/never-dry behavior actually takes effect — until then the client's `more:true` calls harmlessly no-op against the old worker code, exactly as designed, so nothing breaks in the meantime. `sw.js` `CACHE_VERSION` bumped `clv-v204`→`clv-v205`; `#prof-app-ver` → v205.
| v206 | Owner tested v205 live: all 12 already-cached videos per topic played through (ones already seen from earlier testing), then videos stopped appearing — asked to properly check functionality rather than assume, fast. Root cause confirmed two ways, not guessed: (1) **curled the live production Worker directly** with `more:true` and got back `"source":"cache"` — proof the v205 Worker rewrite has not been deployed yet (needs the owner's manual paste-and-redeploy into `clar-youtube`, same as flagged in the v205 entry), so server-side pagination literally cannot run yet; this alone explains most of the symptom. (2) Auditing the client logic while at it surfaced a second, independent, real bug that would persist even after the Worker is deployed: `_ensurePool` only ever fetches a topic's cache ONCE per session, short-circuiting every call after (`_poolTopic===topic && _pool.length` skips a real fetch as long as the in-memory pool is non-empty) — meaning even a Worker that's successfully growing a topic's cache server-side would never actually get re-read by a session that keeps landing on the same topic (or has only one truly active topic, the exact case here), since nothing ever prompts a fresh fetch for a topic already in memory. Fixed by having `_growPool()` read its own `more:true` response instead of firing-and-forgetting it — the Worker's reply already carries the full, current cache (`growTopicPool`'s `videos` field) — and replacing the in-memory `_pool` with it whenever it's larger than what's already held for the still-active topic; strictly additive (a superset of what was there), so nothing already-unseen is ever lost. **Verified with a targeted live simulation** (real production Worker doesn't support `more:true` yet, so a temporary `window.fetch` intercept stood in for the *future* deployed Worker's response shape): seeded a fake 20-video "grown" response, ran the real Vibe Feed, and confirmed a previously-nonexistent fake video ID became pickable within 3 card-advances of the first `_growPool` call resolving — the exact mechanism the fix targets, confirmed working end-to-end rather than only reasoned about. Also re-confirmed via a clean-seen-ids re-test that ordinary video playback is unaffected by this change (real David Goggins video picked normally). `sw.js` `CACHE_VERSION` bumped `clv-v205`→`clv-v206`; `#prof-app-ver` → v206. **Still blocked on the same pending manual step as v205** — until `workers/youtube-relay-worker.js` is redeployed and `db/schema_v205_video_pagination.sql` is run, this client fix has nothing server-side to actually pick up yet (confirmed the old Worker still ignores `more:true` entirely), but it's now correctly wired to take advantage of it the moment that happens.
| — | **Worker-only hotfix, no client version bump.** Owner redeployed the v205 Worker + ran the SQL migration — confirmed live via a real curl test against a brand-new topic (`source:"live"` → `source:"grown"`, pool 12→24). But real usage on the actual production account still showed videos drying up: traced (via a `window.fetch` intercept on the live site, not guessed) to `growTopicPool`'s handling of any topic that already had cache rows from BEFORE this update — e.g. "discipline," the owner's real primary/fallback topic. Its `if (!state) return getVideosForTopic(env, topic, query);` branch, meant to "bootstrap" a topic with no pagination-state row yet, actually just calls the plain cache-read function — which no-ops immediately on any cache hit, since it only ever writes a state row on a genuinely empty-cache/fresh search. Net effect: every `more:true` call for any topic that existed before today's deploy returned the *same* unchanged 12-video cache forever, with zero attempt at a real page-2 fetch and no state row ever created to fix that — a permanent dead end for exactly the topics real users had actually been using, while only brand-new topics created after the deploy worked as intended (which is why the earlier redeploy verification, using a fresh test topic, looked completely fine). Fixed by removing that dead branch: whenever there's no state row, `growTopicPool` now always performs one real fresh page-1 search (accepting that a few of its results may already be cached — `on_conflict` dedups them harmlessly) specifically to obtain and store a genuine `nextPageToken`, "unsticking" the topic permanently — normal pagination takes over correctly from the very next `more:true` call. **Verified against the exact bug scenario in isolation** (mocked fetch, 12 pre-seeded legacy cache rows + zero state rows, matching the real "discipline" topic precisely): first `more:true` call now correctly grows 12→24 and creates a real state row (was: silently returns 12 forever); second call continues genuine pagination (24→27) using that now-real stored page token. **Owner needs to redeploy `workers/youtube-relay-worker.js` one more time** for this fix to take effect — no SQL change needed this round, the v205 migration already covers it.
| v207 | Owner reported the Vibe Feed video card works on laptop but "feels dead" on mobile — loads, never starts, no button does anything, and wanted it to open directly full-screen like Instagram Reels rather than the "small, then expands once playing" behavior from v203-v206. Root-caused two real, compounding issues rather than guessed: (1) mobile browsers are well-known to be far stricter than desktop about honoring UNMUTED autoplay (v203's `mute:0`) — when blocked, YouTube's `PLAYING` state simply never fires, and since fullscreen-expansion was gated entirely on that event (`_expandVideoFullscreen()` only ever called from `onStateChange`), a blocked-autoplay video stayed stuck small forever with zero visible sign anything had gone wrong, matching "sab dead sa lagta he"; (2) v204's swipe-to-skip touch overlay (`.vf-video-touch-ov`, added to work around cross-origin iframes never bubbling touch events) only ever handled a >50px swipe and silently swallowed every OTHER touch including a plain tap — meaning even the standard mobile fallback of "tap the video to start it when autoplay was blocked" was actively broken by this app's own overlay, exactly matching "koi button bhi work nahi karta". Fixed three ways: fullscreen (`vf-video-fullscreen` class) is now applied directly in the card's initial HTML at render time, not deferred to the `PLAYING` event, so the card is full-screen from the instant it appears regardless of whether autoplay ever actually starts — directly satisfying the Reels-style ask and incidentally removing the "looks broken" symptom even when autoplay is blocked; the touch overlay's handler now distinguishes a small movement (a tap, <20px) from a real swipe (>50px) and calls `_ytPlayer.playVideo()` on a tap, restoring a working manual-start fallback; and the player gained an `onReady` handler that also explicitly calls `playVideo()` as an extra nudge, since mobile browsers are known to sometimes honor a direct JS API play() command in cases where the declarative `autoplay=1` URL parameter alone gets ignored. **Verified what's verifiable from this environment**: confirmed via a real touch-emulated mobile viewport (390×844, touch+mobile flags, iPhone Safari UA) that the wrap carries `vf-video-fullscreen` immediately on render (before any player-ready event), that a simulated small-movement tap dispatches without error, that a real swipe (>50px) still correctly advances the card, and that an ambiguous mid-range touch (30px, between the tap and swipe thresholds) correctly triggers neither — no regressions introduced. **Honest limitation stated to the owner**: this is Chromium-based emulation (viewport/UA spoofed, not real WebKit), so it cannot reproduce or disprove actual iOS Safari's specific autoplay-blocking behavior the way a real device can; the fixes applied are the standard, well-established pattern for this exact class of problem, but the owner's own phone is the real verification this needs. `sw.js` `CACHE_VERSION` bumped `clv-v206`→`clv-v207`; `#prof-app-ver` → v207.
| v208 | Owner reported the Vibe Feed only ever showing self-discipline videos and, mid-investigation, that interests they'd previously typed had disappeared. Both root-caused with real evidence, not assumed. **(1) A serious, longstanding sync bug**: this app has two similarly-named but completely different sync functions — `_syncPushProfile()` (pushes identity fields — nick/persona/theme/name/email — to the `user_profile` Supabase table) and `_syncPushClar()` (pushes `clv_user_profile` — present_challenge/goal/mentors/vision_topics/strengths/weaknesses/books — to the `user_clar` table). Every single place in this file that saves `clv_user_profile` content — the original `saveProfile()` (since whenever the Profile context fields were first built), plus every v203-v207 addition (`profAddTag`, `profRemoveTag`, `_profileAddOnly`, `_migrateOnce`, `videoInterestsSave`, the `saveProfile` wrapper, and Clar's own `profile_update` handler) — was calling `_syncPushProfile()` instead of `_syncPushClar()`. Net effect: none of it ever actually reached Supabase; it only ever lived in localStorage, and the very next `_pullUserData()` cycle (any reload) silently overwrote it back to whatever (little) had ever genuinely synced. **Confirmed directly against the owner's real account before fixing** — queried their live `user_clar.user_profile` row and found it held only `{"present_challenge":"..."}`, identical to their stale local copy, proving every mentor/strength/weakness/book/topic they'd ever added had really never left their device. Fixed every incorrect call site to `_syncPushClar()` (two, `saveProfile()` and `_migrateOnce`'s caller context, needed BOTH functions since they also touch identity fields); additionally found and fixed `profAddTag`/`profRemoveTag` never synced at all, even via the wrong function — now sync immediately on every add/remove, not only when the main Save button is later pressed. **Verified live**: added a real tag via `profAddTag`, confirmed it landed in Supabase within ~1.5s via a direct query; removed it, confirmed the removal synced too. **The owner's already-lost interests cannot be recovered** (they were never actually saved server-side to begin with) — will need to be re-entered once, but will now actually stick. **(2) Root cause of "only discipline shows"**: with the sync bug meaning almost nothing ever stuck, and the owner's Goal-tab text also empty, `_activeVideoTopics()` had nothing from explicit interests or goal-matching and fell through to a **hardcoded `'discipline'` default** — removed per explicit request, in both places it existed (the final empty-list fallback, and a second one inside the goal-text-match branch for when goal text exists but matches no known keyword). `_ensurePool()` updated to no-op safely on a genuinely empty topic list (previously would have thrown attempting `pool[0].topic` on an empty array) — verified live: an empty profile now correctly produces zero video cards in the Vibe Feed rather than crashing or defaulting to a topic. **(3) New: free-text custom interests.** Per explicit request ("chips ke sath user ko interests type karne ka bhi option do"), a new input alongside the 12 fixed topic chips lets a user type ANY interest, not limited to the predefined list — stored as `custom_topics[]`, rendered as removable tags (same add/remove pattern as mentors/strengths/weaknesses/books), and always appended to `_activeVideoTopics()` with an auto-generated query (`"<text> shorts"`) — **live-verified** with "urban gardening" as a genuinely arbitrary test case: correctly appeared as its own topic and pulled real, relevant YouTube results (gardening Shorts) confirming the generic query template works for unpredictable free-text input, not just the anticipated cases. `window._profileCustomTopics()` export added; Clar's system prompt and `profile_additions` both extended to read/add `custom_topics` the same way as the other fields. `sw.js` `CACHE_VERSION` bumped `clv-v207`→`clv-v208`; `#prof-app-ver` → v208.
| v209 | Explicit ask: "search queries should be written by ai based on users preferences mood and what should take things to uplift" — the existing mentor/strength/weakness/book/custom-topic queries were mechanical string templates (e.g. weakness → `"how to overcome <text> shorts"`), not real reasoning about the person. Added AI-authored queries on top of them (additive, not a replacement — the deterministic ones stay exactly as-is, so a named mentor/book still reliably searches for that literal thing; the new AI layer is where real judgment about mood/uplift adds value templates can't). No live "mood" field exists to read (v64 deliberately removed the standalone mood picker in favor of Clar reading mood naturally from conversation), so the closest available real signals — recent `clv_chat_summaries` and the real engagement streak (`window._realStreak()`) — are fed to the same Gemini worker that already powers Clar AI chat, asking it to write 5-6 genuine YouTube search queries that would actually help this specific person right now, not just restate their interests as keywords. Can't call this synchronously on every video pick (an AI round trip per card would be slow/costly), so it's cached once per calendar day (same pattern Fortune/Pulse already use for their own daily AI reads): `_activeVideoTopics()` reads whatever's cached instantly and fires a background refresh (no-op if already fresh today, or if the profile is genuinely blank — a real cost guard, not just a stub) that the *next* call picks up. **Live-verified with a realistic scenario** (profile: present_challenge "feeling stuck and unmotivated at work", mentor David Goggins, strength Discipline, weakness Procrastination; chat summaries mentioning "overwhelmed," "low energy," "procrastinating again, feeling guilty") — the real Gemini response produced genuinely context-aware queries directly responding to the mood signal, not generic restatement: `"how to get unstuck in business when overwhelmed"`, `"boost energy and motivation for work shorts"`, `"how to build momentum with small wins for business shorts"` — confirmed these pulled real, highly relevant YouTube results (e.g. "How to get unstuck on a project when you feel overwhelmed," "Surprising advice if you feel overwhelmed right now | Mel Robbins"), and confirmed all of them successfully surfaced as real video cards in a live Vibe Feed run. Also verified: zero AI calls fire on a blank profile (cost guard confirmed working), and zero redundant calls fire across 10 rapid re-checks once today's cache exists (daily-cache dedup confirmed working). `sw.js` `CACHE_VERSION` bumped `clv-v208`→`clv-v209`; `#prof-app-ver` → v209.
| v210 | Owner escalated ("underperforming," data still not saving) after v208/v209 shipped, with a screenshot of an empty My Interests form and a new claim that Goal-tab text also wasn't reaching Supabase. Re-verified everything live on the real account, from scratch — the CDP connection to Chrome had actually silently dropped (the debug-profile Chrome window was open but the `--remote-debugging-port` flag'd process wasn't running), relaunched it before testing rather than assuming the tooling itself worked. Confirmed via direct real-account tests that the v208 mentor-save fix genuinely works and syncs to Supabase within ~1.5s — so that specific complaint was very likely a stale-cached-version symptom on the owner's own device, not a live regression. But investigating the *new* Goal-tab claim surfaced a real, separate, previously-unfound bug: `c9_goal` is correctly stored JSON-encoded via this file's shared `get()`/`put()` helpers (needed since most `K.*` keys hold arrays/objects) — but 6 separate call sites (`_buildGoalsPayload`, `_matchGoalToTopics`, `_activeVideoTopics`'s two internal reads, and two Fortune/Pulse-area goal reads) read `localStorage.getItem('c9_goal')` directly, bypassing the JSON-decode. For a plain string this means literal wrapping quote characters (`"like this"`) got embedded in the actual value — silently pushed to Supabase's `user_goals.goal` column as corrupted text, and fed into goal-text keyword matching (`_matchGoalToTopics`) in the same corrupted form. The primary UI display was self-correcting (the textarea reads through the proper `get()` path, which un-wraps it), which is exactly why this went unnoticed — it only broke the paths that skip that helper. Fixed with one shared `window._readGoalText()` (JSON.parse with a safe fallback to the raw string for legacy/already-plain values) replacing all 6 direct reads. **Live-verified end-to-end on the real account**: saved a new goal through the actual Goal-tab UI, confirmed Supabase's `user_goals.goal` now holds clean text with no wrapping quotes (previously would have shown `"text"` literally); confirmed `_activeVideoTopics()` correctly consumes the decoded text for keyword matching. Separately, since the owner explicitly does not want to hear about caching as an excuse but the CDN delay is a genuine, unfixable GitHub Pages platform limit, made the one thing that *is* controllable better without needing a discussion: the service worker's update check now also re-runs whenever the app returns to the foreground (tab refocused / PWA resumed), not only on a cold load — closes the gap between "the CDN's cache finally expired" and "the app actually notices" without requiring a full close-and-reopen; still only detects, doesn't force a reload, so nothing about existing behavior changes for someone mid-task. `sw.js` `CACHE_VERSION` bumped `clv-v209`→`clv-v210`; `#prof-app-ver` → v210.
| v211 | **Dream-Life Desire Extraction + Admin Recommended topics** — a multi-turn design discussion landed as a full build. The Major Goal box can now be written as a dream-life story ("I drive a white Range Rover and live in a mansion..."), and a new AI pass (separate job from v209's mood-query-writer) extracts every distinct, concrete, visually-searchable desire mentioned — deliberately uncapped, per explicit correction mid-discussion — preserving the person's full descriptive phrase ("white Range Rover" stays "white Range Rover", never shrunk to "Range Rover" or over-narrowed) while also deriving a separate `canonical_key` (the core brand/model/category only, e.g. both "white Range Rover" and "black Range Rover" → `range_rover`) that becomes the actual Worker topic — so different users' differently-worded desires for the same underlying thing share one cached search instead of each paying for their own, live-verified via two simulated users' phrasings landing on the same key and the second one correctly returning `source:"cache"` against the real Worker. Only re-parses when the goal text itself changes (content-hash gated, not a timer). Each desire's pool can later request a genuinely new AI-written query "angle" once its current one is exhausted (not just deeper pagination of the same search) — the Worker (`workers/youtube-relay-worker.js`) gained a `freshQuery` mode for this: swaps an existing topic bucket to a new search while keeping all previously-found videos, verified in isolation with a mocked page-by-page harness. **Quota-burst protection**, added after the owner did the real math and found a 20-desire story could burn ~2,020 units — ~20% of the whole app's shared daily YouTube quota — in one submission: brand-new (never-searched-by-this-user) desires are capped at 3 fresh searches/day per user; already-cached desires (found in shared cache or searched before) are never capped — this throttles quota spend, not desire count, per the owner's explicit "don't cap desires" instruction. **New: Admin Recommended topics** — a "🎬 Video Topics" tab in `admin.html` lets the owner seed content (e.g. Law of Attraction / Abraham Hicks) that appears app-wide as an on-by-default, one-tap-to-turn-off chip in every user's My Interests — visible and opt-out-able, never silently injected, per explicit request over the hardcode-vs-admin-panel question (recommended admin-panel, matching this app's established pattern of moving content out of code). New `video_seed_topics` table (`db/schema_v211_video_seed_topics.sql`, public-read/service_role-write, same lockdown pattern as every table since v178) plus 3 new `admin-relay-worker.js` actions. The whole system is documented in the existing "AI Features" registry (`admin_commands`, type='feature') via a seeded entry in the same SQL file, per the owner's explicit request to always be able to look up what an algorithm does without re-reading code. **Real bugs found and fixed live-testing, not just syntax-checked** (per this file's own repeated lesson that a clean syntax check proves nothing on its own): (1) adding `video_seed_topics` to the shared Supabase batch-fetch list before its migration had been run broke the ENTIRE Supabase content path for every table, not just the missing one — `Promise.all` threw on the first error and forced the whole app onto the degraded Sheets fallback; caught by actually loading the page against a database missing the new table (exactly the real-world state until the owner runs the migration) rather than assuming success — fixed by making per-table failures independent, with `video_seed_topics` specifically allowed to fail silently (empty array) since it's brand new, while any OTHER table failing still triggers the original safety-net fallback. (2) the desire-variant-generation code referenced `AI_TOPICS_WORKER`/`_extractJSON` from a *different* script block's IIFE, which classic scripts don't share unless explicitly exposed on `window` — this threw a ReferenceError that the surrounding `.catch()` silently swallowed, meaning the entire "find a fresh angle" feature quietly never ran; only surfaced by testing the real exhausted→variant→freshQuery chain end-to-end (not just reading the code), fixed by giving that script block its own local copies of both. **Fully live-verified after both fixes**: real desire extraction through the actual client code path for two different dream stories; real cross-user cache-sharing proof via curl; real quota-budget gating (blocks new desires once budget is spent, never blocks already-cached ones, confirmed via `_ensurePool` never attempting a budget-blocked topic across 15 real card advances); the complete exhausted→AI-variant→freshQuery chain firing for real against live Gemini (produced a genuinely creative new angle, "vintage typewriter restoration ASMR", for a test desire) and landing correctly; Admin Recommended chip rendering, default-on state, and toggle-off correctly excluding that topic from `_activeVideoTopics()`; and a full realistic combined-profile Vibe Feed session (chips + mentors + strengths + weaknesses + books + custom interests + dream desires + admin topics all active together) running cleanly with videos appearing at the expected cadence, no console errors, no regressions. `sw.js` `CACHE_VERSION` bumped `clv-v210`→`clv-v211`; `#prof-app-ver` → v211. **Owner still needs to run `db/schema_v211_video_seed_topics.sql`** for the Admin Recommended feature and its AI-Features-registry documentation entry to go live — everything else in this version works immediately without any manual step (the Worker's `freshQuery` mode needs `workers/youtube-relay-worker.js` redeployed, same as every Worker change in this project).
| v212 | Owner reported the Major Goal box "still had the old placeholder," the Edit button "wasn't working," and worried it wasn't reaching Supabase — sharp, warranted frustration since v211 built the entire dream-desire extraction engine around this exact field without ever touching its own UI copy. Investigated all three live rather than guessing. **(1) Confirmed and fixed, genuinely my mistake**: the owner's real Goal box still held literal leftover test data (`"TEST GOAL SYNC CHECK..."`) from earlier v210 testing on their live account, never cleaned up — removed it and confirmed the account is clean both locally and in Supabase. **(2) Confirmed and fixed**: the placeholder text was never updated to reflect what the field actually does since v211 — it still read like the pre-desire-extraction "write it as if it's already happening" copy, with no indication that specific named things (a car, a house) now get pulled into the video feed. Rewrote it with a concrete example and added a short instructional line above the textarea. **(3) Investigated and found NOT actually broken**: both Edit→Save and the Supabase sync work correctly when tested directly (typed real text, saved, confirmed clean in Supabase). The real explanation for "Edit doesn't work": the Major Goal box lives inside a collapsible section that's closed by default — until that header is tapped to expand it, the Edit button exists in the DOM but renders at 0×0 (confirmed via `getBoundingClientRect()`), so a real tap where it *should* be actually lands on unrelated page content instead (confirmed: `elementFromPoint` at that location resolved to the page header, not the button) — not a code regression, a discoverability gap in an already-existing accordion pattern, explained clearly to the owner rather than left as "seems fine, must be your phone." `sw.js` `CACHE_VERSION` bumped `clv-v211`→`clv-v212`; `#prof-app-ver` → v212.
| v213 | Owner explicitly asked for research + a real solution to a recurring pain point across this whole session: unable to tell whether something is a genuine bug or just stale caching while testing, and normal force-refresh isn't reliable. Researched rather than guessed. **Confirmed via response headers**: `index.html` itself carries the exact same `Cache-Control: max-age=600` as `sw.js` — GitHub Pages' CDN caches every file for up to ~10 minutes after any push, a platform limit with no header/config override available on the free tier (confirmed via Fastly's own docs — GitHub Pages doesn't expose Surrogate-Control or VCL access to repo owners). Also an honest self-correction: the `?x=timestamp` cache-busting query strings used in this session's own curl checks never actually bypassed that CDN cache either — they only appeared to work because enough real wall-clock time had passed by the time of the next check. **Considered and deliberately rejected**: versioned service-worker filenames (`sw-v213.js` etc.), a real documented GitHub-Pages-specific technique — since `index.html` has the identical cache ceiling, this would only have addressed a smaller, secondary layer of ambiguity while adding real complexity/regression risk to the update-check flow; not worth it. **What was actually built**: a genuinely reliable "Force reset" button in Profile → Updates, using the documented-correct complete sequence for clearing what a service worker owns — `navigator.serviceWorker.getRegistrations()` → unregister each, `caches.keys()` → delete each, then reload — since a normal hard-refresh (Ctrl+Shift+R, pull-to-refresh) does NOT reliably clear Cache Storage entries a service worker created, letting it keep intercepting fetches and serving stale responses regardless of what the browser's own address-bar reload does. **Honest limit stated plainly, not glossed over**: this button eliminates every LOCAL layer of caching ambiguity, but cannot make the GitHub Pages CDN serve a version it doesn't have yet — the ~10 minute post-push window is real and unavoidable either way; what this guarantees is that once the CDN does have the new version, nothing local stands between the user and seeing it. **Live-verified**: seeded a real Cache Storage entry, confirmed the reset function actually deletes it and navigates to a fresh URL with a cache-busting param, confirmed the button renders correctly in the Updates panel alongside the existing Check button. `sw.js` `CACHE_VERSION` bumped `clv-v212`→`clv-v213`; `#prof-app-ver` → v213.
| v214 | Owner reported right after v213 shipped: "sab kar liya goal me text save hi nahi ho raha he" (did everything including the new Force reset — goal text still won't save). Re-tested via a desktop-style `.click()` call first and it worked perfectly (local save + Supabase sync both confirmed) — a real discrepancy with the report, so switched to a genuinely mobile-emulated viewport (390×844, touch, iPhone Safari UA) using real pointer-event-based interaction rather than direct method calls, which is what actually caught it: the Save button rendered completely off its own accordion body, `getBoundingClientRect()` showing the whole button row's ancestor (`#goal-body-major`) measuring **5314px wide on a 390px viewport**. Root-caused precisely via computed-style inspection, not guessed: `#goal-row` is `display:grid;grid-template-columns:1fr!important`, and `.sacc-body` (each accordion body — Major Goal, Issues & Resolutions, General Manifestation — including `#goal-body-major`) is a **direct grid-item child** with no explicit `min-width:0`. Per the CSS Grid spec, a grid item defaults to `min-width:auto`, which lets a descendant's intrinsic content width override the track instead of being clipped/scrolled locally — and the descendant here is the Vision Images "endless-scroll" carousel (`#vis-grid`, v153/v154's 3×-duplicated-copy loop pattern), which for this account's ~20 real uploaded images measures ~5,280px of real content. `#goal-row`'s own `overflow:hidden` only clips what's *painted* — it does not shrink a grid item's actual box back to its track, so the button row sharing that same oversized `.sacc-body` box got carried along and pushed off-screen with it. Fixed with one scoped rule, `.goal-row .sacc-body{min-width:0}`, forcing each accordion body back to its real track width so `#vis-grid`'s own already-correct `overflow-x:auto` can finally do the clipping/scrolling it was always supposed to do. **Rigorously verified with a positive/negative control, not just "looks fixed"**: since the chrome-devtools MCP bridge was disconnected this session (Chrome itself was reachable on its debug port — confirmed via `curl :9222/json/version` — only the MCP bridge was down), built a minimal raw-CDP test harness directly on Node 24's built-in `WebSocket` (no npm deps) to drive the same real, already-signed-in Chrome profile. Injected 60 real `.vis-thumb` DOM nodes (matching the actual renderer's class/structure) into `#vis-grid` on both the unfixed v213 and the fixed v214 under identical conditions: **v213 (unfixed)** — `goalBody` 361px→**5902px**, button row 325px→**5866px**, and the carousel's own `overflow-x:auto` never engaged at all (`scrollLeft` set to the midpoint silently stayed `0`, since the box had grown to exactly match its content — nothing left to scroll); **v214 (fixed)** — `goalBody` stayed **361px**, button row stayed **325px**, Save button confirmed on-screen and tappable at a real, sane coordinate, and the carousel's `scrollLeft` jump genuinely took effect (`1960`, matching the calculated midpoint) — meaning this fix also incidentally restores the endless-scroll loop mechanic itself, which had been silently non-functional (never actually scrolling) for any account with enough vision images to trigger the containment failure in the first place. Also confirmed a full real Edit→type→Save round-trip via genuine `Input.dispatchMouseEvent` coordinate clicks (not a direct method call) on the now-correctly-positioned button, with the typed text landing in `localStorage`. `sw.js` `CACHE_VERSION` bumped `clv-v213`→`clv-v214`; `#prof-app-ver` → v214.
| v215 | **Infinite colorful quote cards for the Vibe Feed** — Pinterest-style photo-background quote cards, built as `clarvoyance_v215.html` only (NOT yet promoted to `index.html`/`sw.js` — owner explicitly asked to keep this isolated in the new version file until reviewed, so `index.html` is still v214 as of this entry). Landed after a long multi-round discussion covering sourcing, licensing, and architecture before any code was written: (1) **Real Pinterest/Instagram image scraping ruled out** — same conclusion as v200's video research, re-verified fresh for 2026: Meta has no third-party content-discovery API at all, Pinterest API v5 still has zero public search endpoint (only searches your own account's pins). (2) **Reddit ruled out** — free tier is non-commercial only; commercial use needs manual approval (2-4 week review, not guaranteed) plus $0.24/1,000 calls with a $12,000/month bundled floor; separately, subreddit-submitted images have no clear redistribution rights regardless. (3) **Quote sourcing landed on a bulk one-time import instead of a live search API** (a real architecture pivot from an initial Wikiquote-live-search plan) — reasoning: unlike YouTube video desires (genuinely unbounded per-user text), the quote universe is finite/curatable, so mirroring the video feature's live-search-and-grow machinery would be needless complexity. Imported **20,814 real, attributed quotes** from `m-ric/english_historical_quotes` (MIT licensed, HuggingFace, fetched via their public datasets-server REST API, filtered to card-friendly 15-220 char length, deduped by hash) into a new static `quotes_library` Supabase table (`db/schema_v215_quotes_library.sql`) — zero live API dependency going forward, zero rate limits, works offline once loaded. AI is **never** used to write quote text (explicit owner instruction — quotes must be real, with real author names shown); AI **is** used, cheaply and cached once/day (same pattern as v209's video-topic writer), to pick which of the dataset's real tags (a fixed, verified 60-tag vocabulary — `men`, `love`, `truth`, `success`, `courage`, etc. — the model can only copy from this list character-for-character, never invent one, same anti-fabrication discipline as the v165/v166 `frameworks_used` fix) are relevant to this person's goal/mentors/strengths/weaknesses right now — same personalization spirit as videos, choosing categories instead of writing search strings. (4) **Photo backgrounds**: compared Pexels vs. Pixabay directly — picked **Pixabay** (no attribution required at all, 100 req/min vs Pexels' 200/hour) despite its hotlink ban requiring photos to be downloaded and re-hosted, since this app already has a proven download-and-re-host-to-Supabase-Storage pipeline (v147/v152 vision/RR images) — reusing existing infrastructure rather than adding a new pattern. New `workers/photo-relay-worker.js` (owner must deploy separately, same as every other Worker in this project) searches a small **fixed** list of 18 curated aesthetic themes (sunset, ocean, forest, starry night, etc. — deliberately NOT per-user arbitrary search like video topics), downloads and re-hosts each photo once into a new public `quote-photos` Storage bucket, and self-tops-up via real Pixabay pagination (`photo_theme_state`) whenever a theme's cached pool runs low — `db/schema_v215_photo_library.sql` has the new `photo_library`/`photo_theme_state` tables + bucket + policies, same v178 lockdown pattern as every table since. (5) **Failure handling, per explicit instruction**: if a quote's background photo fails to load, the card is skipped (advances to the next Vibe Feed item) rather than shown broken — capped at 2 consecutive failures per session (an explicit safety addition flagged to and accepted by the owner, to prevent a fully-offline moment from cascading into a rapid skip-loop), after which the session falls back to the plain gradient palette (28 curated vivid gradients, no network needed) for the rest of the session. (6) **Visual design**: quote font-size adapts to quote length (4 size tiers, short one-liners get large bold type, longer quotes step down so nothing overflows), a full-frame dark gradient scrim sits between the photo and the text so legibility holds regardless of how bright/busy the photo is, a decorative oversized typographic quote-mark, right-aligned author line, and a small unobtrusive photographer credit line (Pixabay doesn't require this, but it's good practice). Implementation is a wrap of `vfBuildPools`/`vfPickCard`/`vfRender` (same non-invasive technique as v202's video cards, composes correctly on top of the existing video-card wrap already present at those same three functions — verified by reading through that existing wrap before adding this one, not assumed) — the existing QUOTE turn-taking, countdown timer, and XP unlock logic are completely untouched. Own local copies of small helpers (`_extractJSON`, `_sbReady` with the same retry-until-ready pattern documented for `window._sbShared` since v163/v164/v194/v198) rather than reaching into another script block's private scope — the exact mistake the v211 changelog entry documents. All 48 `<script>` blocks in the resulting file verified to still parse (`new Function()` syntax check) after every edit. **Owner still needs to, manually**: (a) run both new SQL migrations in the Supabase SQL editor, (b) get a free Pixabay API key and deploy `workers/photo-relay-worker.js` as a new Worker (two secrets: `PIXABAY_API_KEY`, `SUPABASE_SERVICE_KEY`), (c) paste that Worker's real URL into the `PHOTO_WORKER` placeholder in `clarvoyance_v215.html`. Until then, quote cards work immediately (real quotes, real authors, gradient backgrounds) and photos simply don't load (graceful no-op, not an error) — same "ships before the manual step is done" pattern as the YouTube video feature. Not yet promoted to `index.html`/`sw.js` — that's a separate step once the owner has reviewed this version.
| v216 | **v215 scrapped and rebuilt — replaced its bulk-import quote source with a live Wikiquote fetch**, per explicit owner direction. Root problem confirmed, not disputed: `db/schema_v215_quotes_library.sql` (20,814 quotes as literal SQL INSERT rows) was a **4.18MB file** — genuinely too large for this tool's own Read limit (confirmed by hitting that exact error re-opening it this session) and the direct cause of context exhaustion in any session that had to touch it again. Also, per the owner's actual original ask (revisited properly this session): they specifically wanted quotes tied to the same *desired people/authors/things* signal that already drives the video feed (mentors, goal, strengths/weaknesses), which a fixed 60-tag category system could never do for a NAMED person not already in the bulk dataset. Built as `clarvoyance_v216.html` plus `db/schema_v216_wikiquote.sql` and a new `workers/wikiquote-relay-worker.js`; `workers/photo-relay-worker.js` and `db/schema_v215_photo_library.sql` (Pixabay photo backgrounds) are reused completely unchanged, since those were never the problem. **Promoted to `index.html`/`sw.js` the same session, after the owner deployed the new Worker and both of us live-tested it together** (see the end of this entry) — this is not a "built but unreviewed" version like v215 was. **Cost discussion, resolved before building**: Wikiquote's own API is free/keyless/no-quota (same MediaWiki family as Wikipedia) — the only real cost is one separate, daily-cached Gemini call (same ~₹0.15/day pattern as v209's video-topic writer) for topic personalization; owner asked whether to merge this with v209's existing daily video-topic call to save that cost — recommended keeping them separate instead (the ₹0.15/user/day saving is negligible against the maintainability cost of coupling two independently-evolving prompts into one), which the owner accepted. **Wikiquote parsing — verified against 3 real live pages before writing any regex** (`en.wikiquote.org` API, fetched via curl, not guessed): a classic biographical page (Isaac Newton — flat `== Quotes ==` section), a modern figure (David Goggins — confirms modern names often DO have real pages; organized as `== Quotes ==` > `=== decade ===` > `==== book ====` nested subsections, plus a `== Quotes about Goggins ==` section that must be excluded), and a topic page (Discipline — no `==Quotes==` heading at all, organized instead under flat alphabetical `== B==`/`== C==` headers). The parser tracks only the nearest LEVEL-2 heading and walks into its level-3/4 subsections freely, skipping bullets under any level-2 heading matching an exclude list (`quotes about`, `disputed`, `misattributed`, `external link`, `see also`, etc.) — one rule that correctly handles all 3 real shapes, confirmed by running it against the actual fetched wikitext of all three pages in an isolated Node harness (not just read as plausible) before it went into the Worker. **Two real, non-obvious findings caught during that same verification, both fixed before shipping**: (1) Goggins' real quotes contain profanity ("won't shut the fuck up about it") — added a plain-word profanity blocklist, checked server-side after cleaning, before a quote is ever cached (self-development app, not a general quote archive); (2) a naive length filter alone left **zero** usable Goggins quotes — his page's 8 top-level bullets were all 322-1379 characters (whole paragraph excerpts from his books, unlike Newton's already-short curated one-liners), none fitting a card. Rather than discard those wholesale or crudely cut a quote off mid-sentence (misrepresenting it), added a fallback: if the full bullet is too long, try the quote's own first complete sentence and use it only if THAT falls in the card-friendly range (15-240 chars) — still his real, unaltered words, just a shorter genuine excerpt. Re-verified after the fix: Newton 28 quotes / Goggins 7 (1 correctly dropped for profanity) / Discipline 23, zero leftover wiki-markup, zero over-length, in the actual isolated test harness against the real fetched pages. **Caching**: `wikiquote_cache` (new table) keys on the *original* normalized search term (not the matched page title), including `not_found` rows, so a mentor name or topic word with no real Wikiquote page is cached as such and never re-searched — 30-day freshness window, since page content is far more stable than a 24h video-topic cache. **Personalization** mirrors the video feature's structure: named mentors (from My Interests) are searched verbatim as authors, zero AI involved; a separate daily-cached AI call (own cache key, own prompt, never combined with v209's) picks 4-6 short topic words from goal/present_challenge/strengths/weaknesses/recent chat-summary mood — instructed to name concepts only, never a person's name (mentors already cover that) and never to write quote text itself, same anti-fabrication discipline as `frameworks_used` (v165/v166); a brand-new profile with zero signal falls back to 4 generic uplifting topics (motivation/courage/success/perseverance) rather than showing nothing. **New QPHOTO Vibe Feed card type**: composes on top of the existing VIDEO card wrap using the identical technique (wraps `vfPickCard`/`vfRender`, own local helper copies rather than reaching into another script block's scope — the v211-documented mistake), injecting a photo+quote card every 2-3 picks; visual design (gradient-scrim-first-always, 4 font-size tiers by quote length, oversized decorative quote-mark, right-aligned author line, small photographer credit) carries over from what was already designed for the abandoned v215, unchanged. **XP timing, per explicit owner spec this session (different from v215's plan)**: a flat 5-second minimum, run as the card's own independent timer (not the generic BREATHING/MOVEMENT/QUOTE/AFFIRMATION countdown block, which QPHOTO deliberately isn't part of) — at 5s it calls the existing generic `vfUnlockXP()`; before 5s, the existing generic skip mechanic in `vfNext()` (which only ever checks `vfXpUnlocked`, already type-agnostic) applies the standard −5 XP + streak-reset penalty with zero extra code needed. All 45 `<script>` blocks verified to still parse (`new Function()` syntax check) after every edit; the Worker file separately verified as valid JS via `node --check` against a `.mjs` copy. **Two more real, non-obvious bugs found and fixed during the owner's own guided deploy-and-test pass (not just this session's own solo verification)**: (1) the deployed Worker initially returned `{"error":"Invalid API key"}` on every call — root cause was the same Cloudflare quirk already documented in this file's v201 entry (a secret saved but not pushed into the live deployment until an explicit redeploy) — fixed by having the owner re-save `SUPABASE_SERVICE_KEY` and redeploy; (2) **topic-page quotes were all misattributed to the topic name itself** (e.g. "Discipline is the soul of an army..." showing author "Discipline" instead of the real speaker) — live-caught by inspecting real curl output, root-caused to the parser never reading the real author off the quote's own "**" attribution sub-line (`* quote` / `** [[George Washington]]`), which topic pages always carry but a person's own biography page never needs (there the "**" line is a citation, not another person's name — deliberately NOT applied to `type:'author'` pages, since blindly grabbing the first wikilink there would misattribute Newton's own words to whoever he was writing a letter to). Fixed with `extractAuthorFromSource()`, verified 16/16 correct against the real live Discipline page in an isolated harness before redeploying, then re-confirmed against the real deployed Worker (`"The discipline of piety..." → Basil of Caesarea`, etc.) and re-confirmed Goggins/Newton (`type:'author'`) were correctly unaffected. **A third, separate data-quality issue surfaced testing a fresh topic ("gratitude")**: some Wikiquote topic pages are far less rigorously curated than a classic biography and mix in non-quote meta-lines formatted as if they were quotes (e.g. "Abdelnasser Abdelfattah and his sayings about Gratitude.", "Meister Eckhart، Gratitude Quotes") — added a small `isJunkLine()` filter (catches "sayings about" and a trailing "Quotes" pattern) and re-verified a fresh topic ("wisdom") came back with zero junk lines and correct real authors (Aristotle, Isaac Asimov, Augustine, etc.). **Live-tested end-to-end in a real Chrome instance** (debug-profile Chrome per this file's own Browser Testing Setup, a local static server on `:8787` since Supabase/Worker calls don't need the OAuth-specific redirect-URL setup that section otherwise describes) with a seeded test profile (mentor: David Goggins): confirmed a real QPHOTO card renders correctly (gradient background since the Photo Worker isn't deployed yet — graceful, not broken), confirmed the 5-second timer auto-unlocks XP (`✓ +3 XP`), and confirmed skipping before 5s applies the standard −5 XP penalty (tested 10→5 directly). **Owner still needs to, manually, if they want real photo backgrounds instead of gradients**: the v215 Photo Worker steps (Pixabay key + `workers/photo-relay-worker.js` deploy + `db/schema_v215_photo_library.sql`) — deliberately deferred this session (gradient-only was accepted as good enough to promote now); until done, QPHOTO cards simply show their gradient fallback, never a broken image. `db/schema_v215_quotes_library.sql` is superseded and no longer needed for anything going forward, left in place only as history (not deleted without being asked). **Promoted to `index.html`/`sw.js` this session** — `sw.js` `CACHE_VERSION` bumped `clv-v214`→`clv-v216`; `#prof-app-ver` → v216 (was still showing "v214" inside `clarvoyance_v216.html` itself until caught and fixed right before promotion — the file was a straight copy of v214 and that label is never touched by a plain `cp`).
| v217 | **Onboarding decluttered** — owner audit found 5-6 things stacking up on first/every launch (Destiny screen, Nickname screen, forced Registration form, Persona screen, a first-open swipe ritual, an install-card, all layered before a user reaches the app) and asked for a discussion before changing anything; agreed plan, then built: (1) **Destiny/Free Will screen removed from the onboarding waterfall** — owner's own idea to resurface it later as a push notification instead, not built this pass; `_checkDestiny()` now just silently marks `clv_destiny_seen` and calls `_checkNick()` — screen markup and `destinyChoose()`/`destinyConfirmFreewill()`/`destinyLater()` all left untouched (not deleted) per the owner's explicit "don't break things removing features, UI-hide is enough" instruction. (2) **Nickname screen removed** — display name now resolves from the real Google sign-in name once someone signs in, falling back to `'Guest'` otherwise (owner: "google se aaega wo use karenge ya fir guest word"); `_checkNick()` silently defaults `clv_nick` to `'Guest'` instead of showing a screen. New `window._syncGoogleNameIfNeeded(session)`, a direct sibling of the existing `_syncGoogleEmailIfNeeded` (same v185/v191-established pattern, called from the same two post-pull call sites in `initSync()`/the `SIGNED_IN` listener branch) — adopts the real Google `full_name`/`name` into `clv_nick` once signed in, but only when nick is still unset or exactly `'Guest'`, so it never overwrites a real nickname someone already had (pre-v217 users, or one typed into the registration form's own name field). (3) **Persona-select screen removed** — owner's own reasoning: persona is already changeable anytime from Profile → Clar AI (v56), so a blocking onboarding step was redundant; `_checkPersona()` now silently defaults `clv_clar_persona` to `'bestfriend'` instead of showing the screen, screen markup and `selectPersona()` left untouched. (4) **Registration form made skippable, not removed** — owner's explicit call: the form is genuinely useful for real data collection and should stay, it just shouldn't be compulsory; researched the right general pattern (progressive/contextual data collection, not a blocking gate before any value delivered) and proposed moving its trigger timing, but the owner's own answer instead was to keep it in place, make it optional, and — as a separate future pass, not built now — expand it into a deeper "help us know you better" style form with psychological questions, framed so giving personal info doesn't feel like a bare signup ask; noted as a roadmap item, not implemented this session. Added `window.regSkip()` (sets `clv_registered`, hides the screen, continues the chain) plus two entry points: a bottom `Skip for now` link, and — after live-testing found the form is genuinely taller than a typical phone viewport, so a skip option only at the bottom means scrolling through every field first to find it — a second, always-visible `Skip` pill fixed to the top-right corner of the screen regardless of scroll position. (5) **Tour refreshed to match the current app** — owner agreed with the recommended approach (a minimal step-by-step orientation now; a fuller contextual-coachmarks-plus-Help-section design deferred as a separate, larger future pass, not built this session). The existing spotlight-tour engine (`getSteps()`, unchanged) reads any element tagged `data-tour-title` — previously only 2 stale tags existed (`Home`, and a `Focus Timer` button); added tags to all 6 currently-visible bottom-nav tabs (Clar AI, Home, Vibe, Goal, Self, Fortune, in that order) with one-line real descriptions, and bumped the Focus Timer step to run last instead of colliding with the new order values — zero new subsystem, purely additive `data-tour-*` attributes on existing buttons. (6) **Real bug fix, not a preference**: found via direct code read that "Later" on the PWA install card only ever removed its `.show` CSS class with no cooldown — the existing `bnavSwitch` wrap re-ran the show-check 600ms after the very next tab switch and slid the card straight back up, so dismissing never actually stuck (root cause of the owner's own report: "Later karne pe niche puri tarah hide bhi nahi hota"). Added a 24h cooldown (`pwa_last_dismiss_ts`) checked before re-showing; "Install from Profile" clears it same as it already clears the skip counter. **A second, related overlap bug was caught live while testing the new skippable registration screen**: the install card's 8-second timer had no awareness of any blocking onboarding screen still being open, so it could pop up (z-index 9999) directly on top of the registration form (z-index 9501) — confirmed via a real screenshot during testing, not guessed. Fixed by having `_maybeShow()` bail early while `#reg-screen` is visible. **Verification**: all 45 `<script>` blocks confirmed to still parse (`new Function()` syntax check) after every edit. Live-tested in a real Chrome instance (debug-profile Chrome + a local static server on `:8787`, this file's own established pattern) with a cleared/fresh profile: confirmed Destiny/Nickname/Persona screens never appear and their localStorage defaults land correctly (`clv_nick`→`Guest`, `clv_clar_persona`→`bestfriend`); confirmed the registration screen still shows and `regSkip()` correctly hides it, sets `clv_registered`, and the chain correctly reaches the daily ritual swipe screen next; confirmed the new top-right Skip pill renders visibly on-screen without scrolling (real screenshot). The Chrome debug connection dropped mid-session (process exited, not an app bug) partway through; once it came back, re-confirmed both remaining fixes live: the install card correctly stays hidden (`display:none`) through the full 8s timer while the registration screen is open, and — the actual reported bug — a real click on "Later" followed by a real tab-switch click no longer brings the card back (previously reappeared within ~600ms); both tested via genuine UI clicks (`click` on the real DOM buttons), not direct function calls. `sw.js` `CACHE_VERSION` bumped `clv-v216`→`clv-v217`; `#prof-app-ver` → v217.
| v218 | Fix for a follow-up owner report right after v217: "app refreshes too many times too frequently, changes the user's working page... or refreshes the vibe feed for no reason." Root-caused in code, not guessed: the Data Loader's `init()` fires the `sheetsDataReady` event **twice** on almost every real app open — once instantly from the local `clv_sheets_cache` copy, then again moments later once a background refetch resolves, since the 60s cache TTL (documented in this file's own Sheets Backend section) is stale on nearly every real open, not just a rare edge case. The Vibe Feed's `sheetsDataReady` listener (present since early versions) unconditionally called `vfInit()` — which re-picks and re-renders whatever card is on screen — every time that event fired while the user happened to be on the Vibe tab; its own inline comment already said "if vibe feed is currently active **and blank**, re-init", but the code never actually checked "blank", so a user mid-typing a WRITING card or mid-timer on a BREATHING card within the first few seconds of opening the app could have it silently swapped out from under them the moment the background refetch landed — with zero user action, exactly matching the report. Fixed with a new `window._vfInitialized` flag, set at the end of `vfInit()` and checked by the listener before calling it again — `vfBuildPools()` (which merges the freshly-fetched cards into the pool for whichever card comes *next*) still runs unconditionally on every fire as before, so newer Sheets/Supabase content is never missed; only the forced re-render of whatever's already genuinely showing is now skipped. Deliberately scoped to just this one listener — the normal, expected "switching to the Vibe tab re-runs `vfInit()`" behavior (via `bnavSwitch`) is completely untouched, so intentional navigation still refreshes the feed exactly as before. All 45 `<script>` blocks confirmed to still parse (`new Function()` syntax check). **Live-tested once Chrome came back** (the debug port was down for most of the session, no `chrome.exe` process running, but returned before shipping): in a real Chrome instance with a genuinely-rendered card on screen, dispatched the exact duplicate `sheetsDataReady` event the real data loader fires — the card survived byte-for-byte unchanged; a negative control (temporarily resetting the new `window._vfInitialized` flag) reproduced the old broken behavior on that same card (a real BREATHING→AFFIRMATION swap), proving the guard is what's responsible rather than coincidence; confirmed `vfBuildPools()` still ran on both dispatches (2 fires → 2 calls, verified via a temporary spy), so fresh content still reaches the pool for whichever card comes next; and confirmed switching away from and back to the Vibe tab still renders a genuinely fresh card exactly as before (untouched normal-navigation behavior). `sw.js` `CACHE_VERSION` bumped `clv-v217`→`clv-v218`; `#prof-app-ver` → v218.
| v219 | Follow-up owner report right after v218: in a PWA, just switching tabs/apps and coming back reset the whole window. Root-caused in code, not guessed — and found real evidence the developers had already half-suspected this exact class of bug: `_setupAuthListener()`'s `SIGNED_IN` branch had a `var prevUid = _uid;` line captured but **never actually used anywhere**, a dead giveaway of an abandoned earlier attempt at this same guard. supabase-js v2 is a widely-reported case of re-emitting `SIGNED_IN` for an session that's already active — most commonly when a tab/PWA regains focus after being backgrounded (its auto-refresh logic re-validates the session on visibility/focus regain) — with nothing about the account actually having changed. The existing code treated *every* `SIGNED_IN` event for a Google-signed-in user as "the user just signed in": it unconditionally called `_clearUserLocalStorage()`, re-pulled from Supabase, and — after a 600ms delay — called `window.location.reload()`. So simply backgrounding and refocusing a signed-in PWA silently wiped local state and force-reloaded the whole app, exactly matching the report. A naive `prevUid !== _uid` guard (finishing what `prevUid` looked like it was meant for) would have been wrong, though — confirmed by re-reading this file's own v150 entry: the primary sign-in path (`linkIdentity()`) deliberately *keeps the same user_id* when linking Google to an existing anonymous session, so `_uid` doesn't actually change on a genuine first-time sign-in either; a plain uid-diff check would have skipped the necessary cascade on the most common real sign-in path. Used a different, correct signal instead — a new module-scope `_lastKnownGoogleUid`, seeded from `initSync()`'s direct `getSession()` path (a normal load for an already-signed-in user never goes through the `SIGNED_IN` listener at all — established fact from this file's own v177 entry) and updated only when the destructive cascade actually runs. The `SIGNED_IN` handler now only runs `_clearUserLocalStorage()`/pull/push/reload when `_lastKnownGoogleUid !== _uid` (a genuinely new or different account) — `_updateAuthUI()` still runs on every event unconditionally, which is all a same-account refire needs to stay in sync. **Live-verified in a real Chrome instance**, not just reasoned through: injected an early script (via `navigate_page`'s `initScript`) that transparently wraps `supabase.createClient` to capture the app's real, actual `onAuthStateChange` callback (not a rebuilt stand-in), then invoked that real captured callback directly with two back-to-back synthetic `SIGNED_IN` events carrying the identical fake Google-shaped session object — reproducing the exact "same account, fires twice" scenario. Console output confirmed `"[Sync] cleared user localStorage for new sign-in"` logged exactly **once** across both calls (not twice), and the follow-up `"[Sync] pulled user data from Supabase"`/`"[Sync] pushed all local data to Supabase"` pair also appeared only once — proving the second, duplicate event correctly skipped the whole destructive cascade instead of merely being assumed to. All 45 `<script>` blocks confirmed to still parse (`new Function()` syntax check). `sw.js` `CACHE_VERSION` bumped `clv-v218`→`clv-v219`; `#prof-app-ver` → v219.
| v220 | **Non-Negotiables moved from Self tab to Home, replacing Clar Commitment**, per explicit owner request. `#nn-tab-section` was physically relocated in the HTML — previously a deeply-nested child inside `#self-section`'s shared `.self-acc` accordion (sharing that wrapper with Non-Doing Timer/Walk & Talk/Protect Yourself), it's now its own standalone top-level `.main` child, carrying the `.self-acc` class directly on itself (same technique `#goal-row` already uses elsewhere in this file to reuse that card styling outside the Self tab, confirmed via grep that `.self-acc`'s one CSS rule is completely unscoped — safe to reuse standalone). Its former Self-tab position was patched — "Protect Yourself" (the item now at the end of that list) gained the `sacc-row-last`/`sacc-body-last` classes NN used to carry, so the accordion's hairline-separator styling still ends correctly. On Home, it takes Clar Commitment's exact old flex-order slot (`body.home-tab #nn-tab-section{order:2}` — previously `order:10`, a leftover from an even earlier version where NN briefly lived at the *bottom* of Home per this file's own v83 entry, before later being moved inside the Self tab accordion and orphaning that CSS rule entirely, confirmed via grep it wasn't taking effect on a non-direct-flex-child). `bnavSwitch()`'s `map.home` now lists `nnTabSec` in place of `commitCard`; `commitCard` itself was **not deleted** — its variable declaration, its `all[]` hide-list membership, its HTML markup, and every `clvCommit*` function are all left completely in place (matches this file's own established v217 "hide via UI, don't break things removing features" convention) — it simply no longer appears in any `map`, so the existing hide-pass keeps it permanently `display:none` with zero other code touched. Added a short line under the "Non-Negotiables" title, always visible (not tucked inside the collapsible body): *"The basis of a well-formed life."* — plain inline styling (small, muted `var(--lt)`), no new CSS class, zero shared-style risk. Checked for anything that assumed NN's old nested position before shipping: `selfToggle('nn')` is a generic ID-based toggle with no DOM-position assumptions (unaffected); `renderNNTab()` already ran unconditionally once at boot and is re-triggered directly by `toggleNNCheck`/`toggleNNSubCheck` themselves (not by a tab-switch hook), so it stays correctly live regardless of which tab it's rendered under; `window.renderSelfTab()` still redundantly calls `renderNNTab()` too (harmless, left untouched); the Profile tab's `_hideAll()` helper already referenced `'nn-tab-section'` by bare ID string (position-independent, unaffected); and v156's Global Search index (`SELF_PRACTICES`) was checked and confirmed to never have included Non-Negotiables as an indexed entry in the first place, so there was no stale tab-routing reference to fix there. All 45 `<script>` blocks confirmed to still parse (`new Function()` syntax check); a raw `<div>`/`</div>` tag-count diff against the pre-edit file confirmed the same baseline offset (a pre-existing, unrelated count from `<div>` text inside JS string literals) held exactly before and after this edit — i.e. this edit added one balanced open/close pair and nothing else. **Not live-tested this round** — Chrome's debug port was unreachable for the whole session (no `chrome.exe` process running) — shipped on thorough static verification (full re-read of both edited HTML regions, every cross-reference above individually checked in the actual file rather than assumed) rather than a live visual pass; worth a real look (confirm the card renders correctly at the top of Home with proper rounded corners, and that Self tab's list still ends cleanly at "Protect Yourself") next time Chrome's available. `sw.js` `CACHE_VERSION` bumped `clv-v219`→`clv-v220`; `#prof-app-ver` → v220.
| v227 | Two real owner reports fixed together, plus a full font/color audit of the Fortune tab, built as `clarvoyance_v227.html`, then **promoted to `index.html`/`sw.js` on explicit "push it"**. **(1) "Friend" showing instead of the real name, even after Google sign-in — also leaking into Vibe Feed card text.** Root-caused, not guessed: this app has an OLDER placeholder sentinel, the literal string `"Friend"` (from the now-dead `saveNick()`'s own default and a few other pre-v217 fallbacks), completely separate from v217's newer `'Guest'` sentinel. `_syncGoogleNameIfNeeded()` (the function meant to adopt the real Google name) only ever recognized `'Guest'`/empty as "not a real name yet" — an account already sitting on the OLDER `"Friend"` value never qualified, so the real name was never adopted, on any number of repeated sign-ins. `vfPickCard()` reads `clv_nick` directly for Vibe Feed card text with its own independent `||'Friend'` fallback, which is where the second symptom came from. Fixed two ways for defense in depth, since `_pullUserData()` can re-import a stale Supabase-stored `"Friend"` before either check runs: `_checkNick()` now normalizes a legacy `"Friend"`/`"friend"` value to `'Guest'` at the earliest point in the boot chain (pushes the correction via `_syncPushProfile()`), and `_syncGoogleNameIfNeeded()`'s own guard was separately broadened to also treat `"Friend"` as a placeholder — the actual fix for the reported case, since it's guaranteed to run after the pull regardless of what `_checkNick()` already did. Every other `||'Friend'`/`'friend'` default fallback in the file (`vfPickCard`, the dead `saveNick()`, chat's own nick resolution, a dead mood-start screen) updated to `'Guest'` for consistency. **Live-verified**: seeded `clv_nick='Friend'` on a real page load, reloaded, confirmed it read back as `'Guest'` — not `'Friend'` — on the very next boot. **(2) Full font/color audit of the Fortune tab**, per explicit owner request ("scan every font... make it iPhone standard... many fonts are not able to read... purple on dark background, change to bright colors"). Bumped every remaining sub-.85rem CSS font-size in the Fortune/Pulse/Insights region (section headers, chart titles, legends, buttons, hints, date stamps) to a .8-.92rem floor — v224's earlier pass had deliberately left small uppercase labels untouched as a design-hierarchy choice, but this pass treats "can a person actually read it" as the higher priority, per explicit instruction. Brightened every low-opacity purple TEXT color (`rgba(180,130,255,.45-.7)`, used for section headers like "PULSE"/"READING"/"YOUR INSIGHTS"/"PATTERN MAP", card labels, hints, and buttons) to a solid bright `#c8a0ff` — the same bright purple already proven readable elsewhere in the file (`.ft-range-go`), rather than a new untested color. **Canvas-drawn graph text — the actual biggest gap, confirmed never touched by any earlier CSS pass since it's JS `ctx.font=`, not CSS**: every axis label/weekday letter/section name/radar dimension label across the Pattern Map and all "Your Insights" charts bumped from 9px to 11-12px (the two radar-style charts, which pre-multiply coordinates by `dpr` instead of using `ctx.scale`, bumped consistently in that same space). **Live-testing this surfaced a real, separate regression from the size bump itself**: the Pattern Map's "Resistance" label clipped off the left edge of its own canvas ("esistance"), invisible at the old 9px but exposed once the text was large enough to no longer fit between its centered anchor point and the canvas edge — a pre-existing latent issue this pass's size increase made visible, not something the size increase itself introduced logically. Fixed properly rather than just shrinking the font back down: both radar-label loops (Pattern Map's 5-axis and Insights' 6-axis balance chart) now pick `textAlign` based on which side of the circle each label sits on (`'left'` for left-side labels so they grow rightward/inward, `'right'` for right-side labels so they grow leftward/inward, `'center'` only near the vertical axis) — fixes the edge-clipping at any font size, including the bigger one. **Live-verified thoroughly, screenshotted section by section**: confirmed "Resistance" displays in full post-fix; confirmed all 6 balance-radar labels (tighter spacing than the 5-axis one) display without clipping or overlap; confirmed the 6 "Minutes by Section" bar labels (Vibe/Chat/Goal/Self/Home/Fortune) fit cleanly at the new size despite tight per-bar slot width; confirmed the Reading section's AI text now visibly reflects v225/v226's newer prompt work in the same pass ("real shifts require making Clar a genuine daily ritual, not an occasional visit" — the RITUAL REQUIRED instruction, confirmed actually firing in real model output, not just present in the prompt); zero JS exceptions throughout. All 48 `<script>` blocks confirmed to still parse. `sw.js` `CACHE_VERSION` bumped `clv-v226`→`clv-v227`; `#prof-app-ver` → v227.
| v226 | **Usage-rank title** — built as `clarvoyance_v226.html` (on top of v225's not-yet-promoted client changes, not on top of live index.html, so that work isn't lost), then **promoted to `index.html`/`sw.js` together with v225 on explicit "push it"** (v225's client-side correlation code had never been promoted on its own — this push carries both). Discussed and agreed with the owner first: the exact slot the old "Oracle" title occupied (removed in v222 for taking up space) now shows a live, no-AI gamified status word — Seeker / Warrior / Champion / Sovereign — driven by whole-app time (`clv_app_times`, deliberately not Clar-chat-only `clv_clar_times`, matching the v179 precedent that whole-app engagement is the fair signal), averaged over the last 3 real logged days per the owner's explicit "only look at a 3-day window" instruction. Thresholds: <10 min/day avg → Seeker, 10-25 → Warrior, 25-45 → Champion, 45+ → Sovereign (`#c4c4d4`/`#8de6b0`/`#c8a0ff`/`#f0c987` — bright, high-contrast colors chosen specifically for the dark background, per explicit request; Sovereign gets a visibly bigger text-shadow glow, "extra shine" for the top tier). Deliberately its own separate colour scale, not reusing v224's red/amber/green severity system — this is a status/prestige badge, not a warning. New `_computeUsageTitle()`/`_renderUsageTitle()`, called from `fortuneLoad()` alongside the existing instant `_renderEnergy()` — same "computed straight from local records, no AI wait" category. **Live-verified in a real Chrome instance**: seeded `clv_app_times` for four different 3-day averages (5/15/30/60 min/day) and confirmed each produced the exact expected word and exact expected computed color (including Sovereign's larger glow radius/opacity) — screenshotted, sitting correctly in the old title's exact position/size. All 48 `<script>` blocks confirmed to still parse. `sw.js` not touched — no version bump, not promoted.
| v225 | **Usage↔emotional-state correlation** — a multi-turn "discuss first" design conversation landed as a scoped build. Owner's core belief: consistent Clar usage is causally linked to how well someone's life is going, and Fortune should say so directly and confidently — but explicitly did NOT want this hardwired as a blanket claim; wanted it statistically checked, per-person, on an ongoing basis, with the owner notified if the data ever stops confirming it. Flagged one real ethical risk before building anything (accepted by the owner, shaped the design rather than blocking it): attributing an individual's hardship to "not using Clar enough" risks reverse-causation (people in genuine crisis have less bandwidth for any app, not just this one) and directly contradicts this app's own repeated "honest, not fear-based, never a raw judgmental grade" design principle (v173/v174/v180). Resolved by making every claim strictly evidence-gated per person — Clar only ever cites this when it demonstrably holds in that specific account's own real history, silent otherwise, never asserted as dogma. **Real prerequisite gap found, not assumed**: `state_read` (low/neutral/high — computed on every single Fortune/chat reply since v160/v162) was being logged to `admin_insights`, but that table had **no `user_id` column at all** — a pure anonymous debug feed, structurally incapable of ever answering "does THIS person's usage track with how they're doing." `db/schema_v225_admin_insights_user_id.sql` adds the column + an index + tightens the previously wide-open (`WITH CHECK (true)`) INSERT policies to `user_id = auth.uid()`; both real insert call sites (chat's `_logAiInsight`, Fortune's inline insert) now attach `window._sbUid`. **Owner must run this migration** — existing rows are unattributed and excluded going forward, only new rows count. New `window._computeUsageOutcomeCorrelation()`: per the owner's own explicit instinct ("day by day gives more clarity than weeks... more than 3 days back becomes uncertain to predict, Clar usage shifts mood fast") — for every day a real emotional read exists, compares that SAME person's own engagement over the *preceding 3 days* (not raw all-time totals) against that day's state; splits their real days into "followed a higher-engagement stretch" vs "followed a lower one" (median split, reusing the exact low/high-half comparison style the Insights charts' `_bulletsTimeScore` already established) and compares. Requires ≥6 real usable days before saying anything at all. Fortune's `_callAI()` now kicks this off fire-and-forget on every load (same non-blocking pattern as the existing monthly-summary compression) and caches the result (`clv_usage_correlation`) for the *next* load to read synchronously when building the prompt — avoids adding a hard network dependency to the already timing-sensitive main AI call (the exact class of bug v187 fixed). Two new prompt blocks, both additive, neither touching the existing MIRROR/GAP-IS-THE-LEVER instructions: **USAGE-OUTCOME EVIDENCE** — only injected when the cached result both has enough data AND genuinely confirms for this person — instructs Fortune to cite the real percentages with real confidence, framed as personal proof, not a general claim. **RITUAL REQUIRED** — a separate, always-evaluated instruction (independent of whether there's enough data for the evidence block yet) firing whenever engagement is low or inconsistent (streak <3, energy tier is "Seeking," or a real 3+ day engagement gap — all signals already computed for the rest of the prompt, no new metric) — instructs Fortune to state plainly that occasional or light use will not produce real results, framed forward ("the lever you have"), explicitly not as a guilt trip about the past. **New admin.html tab, "📈 Usage vs State"** — a manual per-email lookup tool (same pattern as the Limits tab's tier-by-email lookup) showing sample size and the two percentages, green "✓ Confirms" / red "✗ Does not confirm" — the owner's own explicit ask #5, scoped honestly: an always-on aggregate monitor across every user (ask #2's "notify me if it stops confirming") would need real aggregate-query infrastructure that doesn't exist yet; this ships the manual per-account check now and the always-on version is flagged as a clear next step, not silently skipped. `workers/admin-relay-worker.js` gained the matching `usage_correlation.compute` action — deliberately a separate, hand-copied implementation of the identical algorithm (same 3-day window, same median split, same >0.05 threshold) rather than shared code, matching this project's own established "own local copy across script/file boundaries" convention (v211/v215) — verified as valid JS via `node --check` against a `.mjs` copy. **`ai_context` philosophy text drafted, not written** — per the owner's own instinct that this belongs in admin-editable content, not code: a new standalone framework entry ("Usage Is The Lever," tagged `applies_to:['chat','fortuneteller']`) was drafted for the owner to paste into the existing 🧠 AI Logic tab themselves via the zero-new-code generic PERSONAL FRAMEWORKS loop (v160) — deliberately not auto-written to the live database, since that already holds real, owner-edited content (`fortuneteller_philosophy` since v129 already says roughly this) that shouldn't be silently overwritten. Explicitly out of scope this pass, flagged rather than assumed: Pulse doesn't read `ai_context` at all currently (confirmed via grep — chat and Fortune do, Pulse never has), so this reasoning won't reach Pulse's reflection without a separate small addition; and the always-on aggregate admin alert (vs. today's manual per-email check). **Verified before shipping**: all 48 `<script>` blocks (index.html) and admin.html's both script blocks confirmed to still parse; the correlation *algorithm itself* verified correct via an isolated Node test with 4 synthetic scenarios (a real Supabase end-to-end test isn't possible until the migration runs) — a clear positive pattern correctly returns `confirms:true`; a flat/no-relationship pattern correctly returns `confirms:false`; only 3 real days correctly returns `hasEnoughData:false`; and, most importantly, a genuinely *inverted* pattern (lower engagement actually followed by better state) correctly returns `confirms:false` rather than forcing a positive conclusion regardless of what the data shows — directly testing that the anti-dogma design constraint actually holds in code, not just in the prompt wording. **Update**: owner ran the migration and redeployed the worker the same session (confirmed live — see admin-side push below); the client-side pieces described here were promoted to `index.html`/`sw.js` together with v226 (`sw.js` `CACHE_VERSION` `clv-v224`→`clv-v226`, skipping straight past v225 since this version was never promoted on its own). Reviewing/pasting the drafted philosophy text into `ai_context` via admin.html remains the owner's own step whenever they want it live.
| v224 | Follow-up after the owner reviewed v223's real red warnings live and pointed out two more things — built as `clarvoyance_v224.html` first ("save in new version, dont disturb anything else"), then **promoted to `index.html`/`sw.js` on explicit "push it"**. (1) **Fortune's colors were decorative, not semantic** — the owner's screenshot showed a genuinely alarming AI reading ("deep tension and overwhelm... withdrawal") rendering in soft lavender, the same tier that was already the app's "most concerning" state; the 4 energy tiers had never been mapped to actual severity, just a green/gold/blue/purple aesthetic gradient. (2) **A second, separate content-level mismatch**, caught in the owner's own screenshot: the AI headline (reads real chat content) said the person was navigating real distress, while the fixed, non-AI tooltip directly below it said "the beginning of something... the field is waiting for you" — a presumption ("low energy = brand new user") that has no way to know the AI is describing an existing user's real bad week. **Fixes, discussed and agreed before building**: `_energySignal()`'s 4 tiers recolored to an actual red→amber→green severity scale (Seeking→`#ff6b6b` red, Steady→`#e0b050` amber, Active→`#8de6b0` light green, High unchanged `#a0ffc0`, already green). Once the AI response loads, `_render()` now recolors the `#ft-energy` headline using the model's own `state_read` field (low/neutral/high→red/amber/green) — a field that already existed on every Fortune AI call for the admin debug log (CLAUDE.md v160/v162) and had never been used to drive anything the user sees; this way the color reflects what the AI is actually saying in that specific reading, not a frozen pre-AI time/streak tier that can disagree with it. The presumptuous "beginning of something"/"just begun" tooltip copy for the lowest tier was rewritten to stay true either way ("This could mean you're just starting out, or that something real has pulled your attention elsewhere..."), and the tooltip's key phrases now pick up the same severity color. The `#ft-energy-sub` line (a second, separate fixed-per-tier phrase, e.g. "Low activity right now") was found live-testing the color fix to be its own leftover source of the same contradiction — it doesn't know about the AI's real content either, so a green "thriving" headline could still sit directly above a caution-toned sub-line; now hidden once real AI text has loaded, since the fuller paragraph already says everything it would. `#ft-concern-card` (Clar's designated worry flag, "🌀 Clar notices") recolored from gold to the same red. Pulse's mood-chip colors (already a rough 4-tier scale from `_computeMood()`, just muted/dusty) brightened to match: Thriving→`#5fb98a`, Building→`#8de6b0`, Steady→`#e0b050`, Check-in-with-Clar→`#ff6b6b`. (3) **Symmetric green "good news" treatment added to the Insights panel**, per the owner's explicit "positive/soothing feedback should use green" instruction — a new `.ft-insight-good` CSS class (green, bold) alongside v223's `.ft-insight-warn` (red unified to the same `#ff6b6b` — was a slightly different `#ff5c5c` before), `_setBullets()` extended to accept `{good:true,text}` the same way it already accepted `{warn:true,text}`. Applied to 5 of the 7 charts where a clear "genuinely great" threshold makes sense (a real judgment call, decided rather than guessed): Momentum (streak ≥7 days, matching the app's own 7/14/21 milestones), Non-Negotiables (100% completion on every logged day, or ≥90% average), Daily Performance (trending up ≥15 points), Best/Worst Day of Week (a day averaging ≥80%), and Time-vs-Score (a ≥20-point real gap favoring more time spent) — Balance and Minutes-by-Section left neutral since "where time went" isn't inherently good or bad the way these other five are. (4) **Font sizes normalized** across the whole Fortune/Pulse/Insights surface, using the Reading section's existing `.ft-card-text` (1.08rem — the AI near/far/watch/concern text) as the reference "this is the readable size" the owner pointed to: body/prose text bumped to match or approach it (`#ft-energy` 1.0→1.08rem, `.pulse-reflection` 1.05→1.08rem, `.pulse-path-text` .87→1rem, the energy tooltip body .8→1rem, `.ft-insight-list` bullets .82→.92rem, `.ft-notice` intro paragraph .88→.95rem, `#ft-energy-sub` .78→.92rem, loading/error/empty status lines .82-.92→.92-1rem); small uppercase labels (headers, dates, hints, legends) left at their own smaller scale — appropriate for short glanceable tags, not body prose — but any genuinely-too-tiny ones floored up to .7rem (`.ft-energy-lbl` was .6rem, `#ft-date`/`#ft-energy-hint` were .65rem, `.ft-nn-legend` was .68rem); buttons/controls left untouched (not "content to read," lower risk, out of scope). Checked for contrast issues while touching colors — Fortune's whole view is dark-background/light-text throughout with no light-surface components inside it (Pulse's share-sheet modal is the one light-themed exception, already driven by the app's own theme CSS variables (`--tx`/`--lt`/`--su`) used app-wide, not something this pass touched or needed to fix) — the new red/amber/green values were chosen as light/vivid variants specifically so they stay readable against the near-black background, confirmed visually via screenshots below. **Live-verified in a real Chrome instance**: (a) deterministically injected a crafted AI response into the cache (`state_read`:"low"/"neutral"/"high") to test the color wiring precisely without depending on a real Gemini call's mood — confirmed exact computed colors `rgb(255,107,107)`/`rgb(224,176,80)`/`rgb(141,230,176)` for the three states respectively, confirmed the concern card's border color and visibility toggle, confirmed `#ft-energy-sub` computes to `display:none` once real AI data has loaded; (b) screenshotted both the red "low" state and the green "high" state — visually confirmed the headline color now genuinely matches the emotional tone of the text (a negative reading is red, a thriving reading is green), and the tooltip's rewritten copy no longer contradicts either; (c) confirmed the new `.ft-insight-good` class computes to the correct green (`rgb(141,230,176)`) and renders correctly via direct DOM injection (a real Supabase seed of a strong week hit an RLS write rejection specific to that fresh throwaway test session — unrelated to any code touched this round, since zero Supabase-side code changed — so the green-bullet *generation logic* itself was verified by code review plus this direct rendering check rather than an end-to-end seeded scenario this time; the red-warning path was re-confirmed still firing correctly against the same account's real, still-empty data, and zero JS exceptions were seen throughout). All 48 `<script>` blocks confirmed to still parse. `sw.js` `CACHE_VERSION` bumped `clv-v223`→`clv-v224`; `#prof-app-ver` → v224.
| v223 | Follow-up the same day the owner reviewed v222's actual numbers on their real account and flagged a real, important gap — built as `clarvoyance_v223.html` first (owner said "save in new version, don't disturb anything else"), live-verified, then **promoted to `index.html`/`sw.js` on explicit "push it"**. Owner's screenshots showed a genuinely near-all-zero week (0-day streak, 0% NN, a real gap) and pointed out two things at once: (1) the Insights bullets were declaring meaningless "winners" between literal ties — *"Home is closest to its target (0%)"* next to *"Vibe is furthest (0%)"*, both zero — and (2) more importantly, a flat/inactive week is **the single most important thing for this app to flag**, since it's the exact pattern behind drifting from a goal — and neither the bullets nor the AI reading were treating it that way; staying silent or stating a fake precision both miss the point. **First, live-verified the actual hypothesis before writing any fix**: seeded a realistic *mixed* week (some strong days, a skipped day, partial NN) directly onto the real signed-in account via Supabase (same seed-then-delete pattern as the v193 entry — snapshotted first, deleted the 5 seeded rows after, confirmed via a follow-up query that only the account's one real original row remained). With real variation, Fortune and Pulse actually **converged** on the same facts (213 min, 48% NN, ~2-day gap) and both pushed toward the same action — confirming the original stark disagreement was mainly a degenerate-all-zero-data artifact, not a structural flaw, and that the tie-bug genuinely only bites on a flat week (real variance produced correct, distinct, non-tied bullets in every one of the 7 charts). That live test also surfaced one small, separate finding: the model twice misread "NN" as if it were a navigable section ("explore your feelings more deeply in the 'NN' section") rather than a completion percentage. **Two fixes shipped**, both scoped exactly to what was agreed: (1) **`nnContext`'s label** (built once, read by both Fortune's and Pulse's prompts) changed from `'NN: X% this week.'` to `'Non-Negotiables completion (a %, not a section/tab): X% this week.'` — spells out the distinction at the source instead of relying on the model to infer it. (2) **Every one of the 7 Insights-panel bullet functions now explicitly checks for "essentially nothing happened"** (`_isFlatInactive()` — real days all scoring <5%, or zero real days at all — plus per-chart equivalents for the balance/NN/weekday/time-score/section-time charts, each checking its own actual metric rather than reusing daily_score where a more direct signal exists, e.g. section-time checks raw total minutes, NN checks NN% specifically) and, when true, renders a direct, specific warning **instead of** either the old false-tie claim or silence — never both a warning and the normal bullets. Per the owner's explicit instruction, every warning renders in **red, bold** (`.ft-insight-warn`, new CSS: `#ff5c5c`, `font-weight:700`, a ⚠ marker replacing the normal gold ✦) — a new `_setBullets()` accepting either a plain string or a `{warn:true,text}` object, so normal and warning bullets share one rendering path. Every warning's wording deliberately echoes the owner's own framing ("pulls you away from your goals," "worth addressing directly, not softening" — reusing the exact phrase this file's own v186 NN-critical-flag language already established) rather than 7 independently-invented alarms. **Verified before and after shipping, both directions, on a real account**: all 48 `<script>` blocks confirmed to still parse; then two full live scenarios run back-to-back with cleanup between them — a genuinely flat/all-zero seeded week (6 rows, all-zero) produced a red-bold warning in exactly all 7 charts, zero false-tie claims, zero silent charts, zero JS exceptions (screenshotted — the ⚠ red-bold styling renders correctly against the dark theme); immediately after, the *exact* same realistic mixed week used to validate the original hypothesis was re-seeded and reproduced **byte-for-byte identical** bullet text to the pre-fix version, confirming zero regression to the normal (non-flat) case. (This second live-verification round happened to run against a fresh, disposable anonymous Supabase test account rather than the owner's own — an incidental side effect of testing from a different local port/origin, which Supabase auth treats as a separate session; harmless, since it's a throwaway account nobody will ever sign into again, but noted here for the record rather than glossed over.) `sw.js` `CACHE_VERSION` bumped `clv-v222`→`clv-v223`; `#prof-app-ver` → v223.
| v222 | **Fortune tab reports/charts reworked** — discussed first, built as `clarvoyance_v222.html`, then live-verified via a raw-CDP browser check (chrome-devtools MCP bridge was down for the session; drove the real Chrome debug port directly instead, same fallback pattern as the v214 entry) before the owner asked to push: all 4 accordion buttons opened/closed correctly with real content behind each (screenshotted every section), "Oracle" title confirmed gone, Pulse's sparklines confirmed redrawing at real width on open (not blank), all 3 new charts confirmed rendering with correct axes/labels/colors, zero JS errors. The signed-in test account had very little real `user_daily_log` history, so charts rendered correctly-empty rather than with populated numbers — bullet-generation guards were confirmed to suppress output correctly on sparse data rather than showing anything misleading. **Promoted to `index.html`/`sw.js` same session** (owner asked to review before it goes live, same pattern as v215/v216). Owner's complaint: opening Fortune dumped the AI reading, the 5-dim pattern radar, Pulse, and the "Your Insights" charts all stacked open at once ("sab ek jagah bikhara hua"), and the existing 4 Insights charts felt "dead"/not insightful. Fixed both: (1) **Fortune restructured into 4 closed-by-default openable buttons** — Reading (near/far/watch/concern cards, was always-open inside `#ft-content`), Pattern Map (the 5-dim radar, pulled out of the Reading card into its own accordion since it's a local-data computation, not an AI field), Pulse (already had its own header/body but was force-opened via `card.classList.add('open')` on every load — that forced auto-open removed; its `offsetWidth`-based sparkline canvases now redraw on open via `pulseToggle()` instead of only once at load time), and Insights & Charts (already collapsible, unchanged mechanism). New `ft-reading-card`/`ft-pattern-card` accordions mirror the existing Pulse/Insights CSS pattern exactly (`.ft-reading-hdr`/`.ft-reading-body`, `.ft-pattern-hdr`/`.ft-pattern-body`, same header row styling, new `ftReadingToggle()`/`ftPatternToggle()` toggle functions). `_drawRadar()` itself untouched — it draws on a fixed 240px canvas that doesn't depend on layout, so it renders correctly even while its new accordion is still closed. (2) **"Oracle" title removed** — the large serif `.ft-title` div took up header space for no functional benefit; subtitle ("What your present is already creating") now carries the header alone. Animated background (particle field + orb) left untouched, per explicit owner request to keep it. (3) **3 new simple X-vs-Y charts added to Insights**, all computed client-side from the same `user_daily_log` rows the existing 4 already fetch — no new AI call, no new backend: **Best & Worst Days of the Week** (X=weekday, Y=avg daily_score — the first chart in this panel that groups by weekday instead of by date), **Time Spent vs. How the Day Went** (scatter, X=total minutes that day, Y=daily_score — directly answers "does more time actually help?"), **Minutes by Section** (X=section, Y=raw total minutes — deliberately literal/un-normalized, distinct from the existing balance radar which shows ratio-vs-target not actual totals). (4) **Plain computed (non-AI) inference bullets added under all 7 charts** (the 4 existing + 3 new) — per explicit "not on the AI part" instruction, every bullet is a JS-computed fact off the same `days` array the charts already draw (best/worst day, current vs. longest streak run, NN completion count, score trend first-half-vs-second-half, correlation direction between time and score, top/bottom section by raw minutes), phrased as plain observations rather than a grade — same "no raw judgmental scoring" lesson already learned for Practice Plans (this file's own v180 entry). New shared `_setBullets()`/`.ft-insight-list` CSS pattern (gold ✦ bullet marks, matching the panel's existing palette) reused by all 7. **Verified before shipping**: all 48 `<script>` blocks confirmed to still parse (`new Function()` syntax check); a stack-based tag-nesting check (Node script walking every div/ul/li/canvas/span/button tag in the Fortune section as a balanced-parens problem, not just a raw open/close count) confirmed the restructured section nests correctly with zero mismatches. That static pass was then followed by the real-browser raw-CDP check described at the top of this entry, which is what the owner's "push" go-ahead was based on. `sw.js` `CACHE_VERSION` bumped `clv-v221`→`clv-v222`; `#prof-app-ver` → v222.
| v221 | **"Peak State" quick-access button** — a second floating button (`#pk-fab`, ⚡, hardcoded orange/red gradient) stacked directly below the existing v183 Quick-capture pencil (`#qc-fab`) on the same left-edge dock, per explicit owner request discussed first: same exact UI/mechanics (full-screen overlay, discard-on-close — never persisted, "Ask Clar ✦" sends the text through the real `chatSend()` pipeline exactly like Quick-capture does), deliberately reused rather than rebuilt, with two differences — its own color (so it reads as a distinct action, not following the theme accent the pencil does) and its own placeholder text steering toward the specific empowering self-talk the owner described: *"See every situation with positivity and possibility. Unshakeable faith in life and the universe. Extreme courage and confidence. Enormous power. Invincibility. Say it like it's already true…"*. Positioned via its own independent CSS rule (`top:50%;transform:translateY(calc(-50% + 52px))`) rather than moving the pencil button's existing rule at all, so the original is untouched. The v86 keyboard-focus-hide hook (which already hid the pencil alongside the bottom nav so neither sits atop the expanded chat input) was extended to grab and hide/restore this second button too, reusing the same `qc-hide` class. Considered during the discussion but deliberately not built this pass, per the owner's own final answer: persistence (owner confirmed discard, matching Quick-capture exactly) and literal "every minute" reinforcement (flagged as impractical/against this app's own "lower friction" philosophy — a realistic alternative, reusing the existing v184 `notification_rules` push engine for periodic reminders, was discussed but not requested). **Live-verified in a real Chrome instance**: both buttons render correctly stacked; the overlay opens with the exact placeholder and title ("Peak State"); typing updates the word count; closing and reopening confirmed the text is genuinely discarded (not restored — verified via direct `pkClose()`→`pkOpen()`, not assumed); "Ask Clar ✦" correctly switched to the Clar AI tab, sent the written text through the real send pipeline, and a real Gemini-backed reply came back in the chat; and the keyboard-focus-hide/-restore hook correctly hides and restores both buttons together. All 45 `<script>` blocks confirmed to still parse. `sw.js` `CACHE_VERSION` bumped `clv-v220`→`clv-v221`; `#prof-app-ver` → v221.

---

## Google Sheets Backend

**Apps Script URL (direct — do not use in app, CORS blocked from GitHub Pages):**
```
https://script.google.com/macros/s/AKfycbw8CsadkFYEPFccU2YZ5JDnYKlF6DiP_bHLDWaGf3PUNHELjY61ZE7F2E6huc4oKaUjYw/exec
```

**Sheets Cloudflare Worker (use this in app — proxies Apps Script + adds CORS headers):**
```
https://clarvoyance-sheets.smworkassistance.workers.dev/
```

**Active sheets (SHEET_NAMES in Apps Script):**
| Sheet | Purpose |
|-------|---------|
| `modules` | Which app tabs are active (active=false → tab hidden) |
| `charger_categories` | Categories for Mind Chargers |
| `chargers` | Mind Charger content items |
| `charger_rules` | Rules applied to chargers |
| `tools` | Tool definitions (name, emoji, description, placeholder) |
| `vibe_cards` | ALL vibe feed card content (BREATHING, MOVEMENT, AFFIRMATION, WRITING, QUOTE, CHARGER) |
| `quotes` | Curated quotes (injected into Revise tab + vibe feed) |
| `revise_repeat` | Revise & Repeat section content |
| `learning_channels` | Learning channel links |
| `ai_context` | Key-value pairs driving Clar AI system prompt (evolvable without code changes) |

**`ai_context` sheet columns:** `key | value`
Keys used: `identity` `philosophy` `conversation_flow` `disclaimer`

**AI-context columns on `tools`, `chargers`, `vibe_cards`:** `ai_why | ai_best_for | ai_how_to_use`
These are optional — when filled, they make Clar's suggestions more precise and contextual.

**Caching:** Apps Script server cache = 60s. Client localStorage cache = 60s (key: `clv_sheets_cache`).

**`vibe_cards` sheet columns:**
```
id | type | title | content | placeholder | timer | xp | icon | category | active
```
- `type` values: `BREATHING` `MOVEMENT` `AFFIRMATION` `WRITING` `QUOTE` `CHARGER` `TOOL`
- `timer`: seconds for countdown (BREATHING/MOVEMENT/AFFIRMATION/QUOTE use this)
- `placeholder`: for WRITING cards (textarea hint text)
- `icon`: emoji for MOVEMENT cards
- `category`: for CHARGER cards
- `active`: FALSE = row ignored by Apps Script

---

## Analytics

### GA4 — G-BZZMW9B8SN
Account: Clar | Property: Clar App | Stream: Clar PWA
Custom dimensions registered (Event scope): `tab_name`, `card_type`, `streak`, `amount`, `persona`, `intent`, `tool_name`, `charger_name`, `is_pwa`

**Custom events tracked (v71+):**
| Event | Parameters | When |
|-------|-----------|------|
| `app_open` | `is_pwa` | Every app load |
| `tab_view` | `tab_name` | Every tab switch |
| `vibe_card_complete` | `card_type`, `streak` | Card XP unlocked |
| `vibe_card_skip` | `card_type` | Next tapped before complete |
| `xp_gained` | `amount` | Any XP award |
| `clar_session_start` | `persona`, `intent` | initChat() called |
| `clar_message_sent` | — | User sends message |
| `tool_open` | `tool_name` | Tool row expanded |
| `charger_open` | `charger_name` | Charger row expanded |
| `pwa_install_tapped` | — | Install button tapped |
| `pwa_install_later` | — | Later button tapped |

### Microsoft Clarity — wydll6jrxn
Project: Clar | URL: smworkassistance.github.io/clarvoyance
Auto-captures: session recordings, heatmaps, rage clicks, dead clicks, scroll depth.
Data appears 24-48h after first traffic.

---

## App Architecture

### Onboarding Flow (first launch only)
```
Language select → Nickname input → Daily mood select → Clar persona select → App opens
Keys: clar_lang | clv_nick | clar_mood_date | clv_clar_persona
```
**Persona options:** `bestfriend` | `guide` | `coach` | `nurturer`
Changeable anytime from Profile (👤 button in chat header).

### Tab Structure (bottom nav)
| Tab key | What | Module ID in Sheets |
|---------|------|---------------------|
| `home` | Home widget, NN progress, XP | always on |
| `vibe` | Vibe Feed | `vibe_feed` |
| `tools` | Tools grid + inline sections | `tools` |
| `chargers` | Mind Chargers | `chargers` |
| `revise` | Revise & Repeat + curated quotes | `revise_repeat` |
| `nn` | Non-Negotiables checklist | `non_negotiables` |
| `goal` | Goals + vision images | `goals` |

Tabs are shown/hidden by `applyModules()` based on `modules` sheet data.

### Key localStorage Keys
```javascript
K = {
  e:  'c9_emo',      // current mood index
  v:  'c9_vis',      // vision images (goal tab) — array of {src, note}
  n:  'c9_nn',       // non-negotiables list
  nh: 'c9_nnhist',   // NN completion history by date
  g:  'c9_goal',     // goal text
  gl: 'c9_goallinks',
  q:  'c9_quotes',   // user's custom quotes
  rr: 'c9_rr',       // revise & repeat images
  rd: 'c9_rrdocs',
  rl: 'c9_rrlinks',
}
// Other important keys:
// clv_sheets_cache  — Sheets JSON cache (60s TTL)
// clar_lang         — selected language (en/hi/hinglish)
// clv_nick          — user's nickname
// clar_mood_date    — date of last mood log
// clar_xp           — total XP / vibration score
// clarQuotes        — user's My Quotes (shown in vibe feed)
// clv_chat_session  — {date, history[], intent} today's Clar AI conversation
//   intent: 'talk' | 'act' | null — set by session intent tap at start of session
// clv_user_profile  — {present_challenge, permanent_challenge, goal, intention}
// clv_chat_summaries — [{date, summary}] last 7 days of conversation digests
// clv_clar_persona  — 'bestfriend' | 'guide' | 'coach' | 'nurturer'
```

---

## Vibe Feed Architecture

### Card Sources (priority order)
1. **Google Sheets `vibe_cards`** — ALL card types (primary source, loaded via sheetsDataReady)
2. **Google Sheets `chargers`** — injected as CHARGER type cards
3. **localStorage `c9_vis`** — vision images → GOAL_VISION cards (dynamic)
4. **localStorage `clarQuotes`** — user's My Quotes → QUOTE cards (dynamic)

### Card Types & XP
| Type | XP | Completion trigger |
|------|----|--------------------|
| BREATHING | 10 | Timer (from sheet `timer` col) |
| MOVEMENT | 8 | Timer (from sheet `timer` col) |
| AFFIRMATION | 4 | Timer (15s default) — "Say it 3 times" |
| WRITING | 7 | Auto when ≥ minChars typed |
| GOAL_VISION | 6 | Auto when ≥ 20 chars typed |
| CHARGER | 5 | Tap "I'm Charged" button |
| QUOTE | 3 | Timer (20s) — "Read it 5 times" |
| TOOL | 8 | Auto when ≥ 10 chars typed |

### Skip Mechanic
Next button before task complete = −5 XP + buzzer + streak reset.
Next button after task complete = clean advance (green "Continue ↑").

### Streak System
- 3 consecutive completions → 1.5× XP multiplier
- 5 consecutive completions → 2× XP multiplier
- Any skip → streak resets to 0

### Anti-repeat
`vfLastType` tracks previous card. Same type never shown twice in a row.

---

## Tools Architecture

### Tool cards in Tools tab
- Rendered from Sheets `tools` data via `sheetsDataReady` event
- Grid card (`.tc`) + inline section (`.isec`) both built dynamically
- Fallback: `registry.js` + HTML file fetch (for rich tool content)

### Tools in Vibe Feed
- `VF_TOOLS` array populated from Sheets `tools` data
- Generic card: shows tool name + description + textarea (≥10 chars → XP)

---

## Key Functions Reference
| Function | Where | What it does |
|----------|-------|-------------|
| `langInit()` | onboarding script | Entry point on DOMContentLoaded |
| `bnavSwitch(el)` | bottom nav | Switches visible tab, triggers renders |
| `applyModules(modules)` | sheets loader | Shows/hides tabs based on Sheets data |
| `vfInit()` | vibe feed | Shuffles deck, starts feed |
| `vfRender()` | vibe feed | Renders current card |
| `vfPickCard()` | vibe feed | Selects next card (anti-repeat + pool logic) |
| `vfNext()` | vibe feed | Advance or skip (checks vfXpUnlocked) |
| `vfUnlockXP()` | vibe feed | Awards XP, flips button green |
| `renderChargers()` | chargers tab | Renders charger categories from Sheets |
| `renderSheetTools()` | tools tab | Renders tools from Sheets data |
| `xpAdd(pts)` | XP system | Adds XP, checks level up |
| `xpRefreshAll(score)` | XP system | Updates all XP display elements |

---

## Clar AI Architecture

### Overview
Clar is an AI companion tab powered by Gemini 2.5 Flash (free tier) via a Cloudflare Worker proxy. The API key lives server-side in the worker — never in the browser or GitHub.

**Cloudflare Worker URL:** `https://cold-frog-d555.smworkassistance.workers.dev/`
(Passes the request body directly to Gemini; adds API key server-side; returns raw Gemini response)

### System Prompt (`_buildSystemPrompt`)
Built fresh on each API call from:
1. `SHEETS_DATA.ai_context` — identity, philosophy, conversation_flow, disclaimer rows
2. `localStorage clv_nick` — user's nickname
3. `localStorage c9_emo` — mood index → label
4. `localStorage clv_user_profile` — present_challenge, permanent_challenge, goal, intention
5. `localStorage clv_chat_summaries` — last 7 days of conversation digests
6. `SHEETS_DATA.tools` + `SHEETS_DATA.chargers` — with ai_why / ai_best_for / ai_how_to_use columns if filled

### AI Response JSON Format
```json
{"message":"...","cards":[{"type":"tool","id":"t1","reason":"short personal reason why this card right now"}],"profile_update":{"present_challenge":"..."}}
```
- `cards`: empty array or 1 card (tool or charger by id) — rendered inline as tappable card
- `cards[].reason`: optional short string — personal reason why this specific card for this moment, shown below the card chip
- `profile_update`: if Clar infers user's challenge from conversation, auto-saved to `clv_user_profile`

### Clar Conversation Philosophy (core — do not compromise)

**The fundamental principle: Earn the right to suggest.**
Clar's first job is not to help. It is to make the person feel *completely heard*. The moment you suggest something, you signal "I've heard enough." That closes people down. A suggestion only lands when the person is open — and they only open when they feel safe.

**The trust loop:**
```
Person feels heard → opens up more → trust builds → becomes open to suggestion → suggestion lands → person acts → feels better → trusts Clar more
```
Without the trust loop, cards are just interruptions.

**Three-phase conversation model:**

| Phase | Name | What Clar does | Cards |
|-------|------|----------------|-------|
| Phase 1 | Hear | Reflect back what was shared. Ask ONE deeper follow-up. No agenda. | `[]` always |
| Phase 2 | Understand | Keep listening. Build a genuine picture of their emotional state. Watch for the "exhale" — when their tone shifts from heavy to lighter/clearer. | `[]` always |
| Phase 3 | Sense the moment | When the exhale comes: suggest one action confidently, with a personal reason. Not a question — a statement. | 1 card max |

**The "exhale" signal** — what to watch for:
- Person has said what they needed to say (topic naturally closes)
- Tone shifts: less urgent, more reflective, or slight positivity emerges
- They use words like "haan", "theek hai", "samjha", "ab kya karun"
- Minimum 2-3 exchanges have happened regardless

**How to suggest in Phase 3 (critical):**
- Confident statement, NOT a submissive question ("kya tum try karna chahoge?" is WRONG)
- Frame it around the rising momentum: "Ab jo positivity aa rahi hai — isko pakad le"
- The reason must be personal — reference something specific from *this* conversation
- Short reason: why THIS card for THIS person RIGHT NOW
- If resistance (short reply, redirect, silence): acknowledge, continue conversation, try again after 2+ more exchanges
- Goal is upliftment, not compliance — if the person isn't ready, keep building trust

**Session intent (v57+):**
At session start, user can tap: "💬 Bas baat karte hain" OR "⚡ Kuch try karna hai"
- `talk` intent → Phase 1+2 extended, Phase 3 still happens but patience is higher
- `act` intent → Phase 1 shortened (1 exchange), move to Phase 3 faster
- No tap → default to full 3-phase flow
Stored in `clv_chat_session.intent`

**What Clar must never do:**
- Suggest a card in the first 2 exchanges (regardless of intent)
- Ask permission to suggest ("do you want to try something?")
- Suggest more than 1 card per session unless user explicitly asks
- Break conversation flow — suggestion should feel like a natural next sentence
- Be generically warm — every message must feel personal to this person's situation

---

### Daily Summaries
On each new day's first `initChat`, yesterday's last 4 messages are compressed into a one-line summary and saved to `clv_chat_summaries` (max 7 entries). Injected into every system prompt for continuity.

### User Profile Modal
Accessible via 👤 button in chat header. 4 fields: present_challenge, permanent_challenge, goal, intention. Also auto-updated by Clar when it infers context from the conversation.

### Key Chat Functions
| Function | What |
|----------|------|
| `initChat()` | Entry point — restores today's session or starts fresh |
| `chatSend()` | User message → history → AI |
| `_buildSystemPrompt()` | Assembles full context from sheets + localStorage |
| `_sendToAI()` | Calls Cloudflare Worker with payload |
| `_addCardEl(card)` | Renders inline tappable tool/charger card |
| `chatProfileOpen/Close/Save()` | Profile modal controls |

---

## AI Logic Console (`admin.html`, v160+)

The owner's private tool for editing what Clar actually thinks — separate file, not linked from the main app, talks directly to Supabase. As of v181, `admin.html`'s Tools/Chargers/Charger Categories/Charger Rules tabs also read and write Supabase (via `admin-relay-worker.js` for writes — see the v181 entry below); only Vibe Cards/Quotes/Revise & Repeat/Learning Channels/Modules still go through the Sheets/Apps Script path (`api()`).

**Why it exists:** the owner reasons about self-development through hand-drawn "charts" (personal frameworks, e.g. an emotional-frequency ladder derived from Abraham-Hicks-style theory + his own tested conclusions). Feeding these to Clar means transcribing each chart's logic into text — an image alone isn't reliable input for an LLM, so images are kept only as the owner's own reference, never sent to the AI. See `emotional_guidance_system` in `ai_context` (added v129, originally Fortuneteller-only) for the reference example of what a well-transcribed chart looks like: numbered states, explicit "do this / not that" rules, and a closing instruction on how to detect which state applies.

**Data model — `ai_context` table (Supabase), extended in v160:**
| Column | What |
|--------|------|
| `key` | machine id, primary key (e.g. `emotional_guidance_system`) |
| `content` | the full logic paragraph(s) — exactly what gets fed to the AI |
| `display_name` | friendly name shown in admin (e.g. "Board Chart") |
| `image_urls` | 0-2 reference image URLs (Supabase Storage bucket `chart-images`) — admin's own memory aid, **never read by the AI** |
| `category` | `'chart'` or `'context'` — splits the admin console into two sections |
| `applies_to` | text[] tag — which AI surface reads this row: `'chat'`, `'fortuneteller'` |

**How a new chart goes live:** add it in `admin.html` → 🧠 AI Logic tab (name, optional image(s), full logic text, tick which surface(s) it applies to) → Save. Both `_buildSystemPrompt()` (chat) and the Fortuneteller prompt builder loop over all `ai_context` rows tagged for them (excluding the 4 keys each already injects by name — `identity`/`philosophy`/`conversation_flow`/`disclaimer` for chat; `fortuneteller_philosophy`/`emotional_guidance_system`/`manifestation_signals`/`prediction_language` for Fortuneteller) and append them as a "PERSONAL FRAMEWORKS" block. No code change or deploy needed for new entries — only for changing *which* surfaces exist to tag against.

**State reading:** both AI response formats now require a `state_read` field (`"low"|"neutral"|"high"`, internal only, never shown to the user) — the model must commit to a coarse read of the user's state before replying, reusing the same low/mid/high tiering `tools`/`chargers` already use for `mood`/`quest_low/mid/high`. Intent: make chart logic apply consistently turn-to-turn rather than only sometimes.

**Observability:** every chat and Fortuneteller reply fire-and-forget logs `{state_read, charts_live, user_msg, ai_msg, ts}` to `admin_insights` (`insight_key`='chat'\|'fortuneteller') — a table stubbed in v129 and unused until now.

**Four tabs in `admin.html`, all Supabase-native:**
| Tab | What |
|-----|------|
| 🧠 AI Logic | Charts + AI Context CRUD described above |
| 🔍 Debug Log | Read-only feed of `admin_insights` — most recent turns first, filterable by chat/fortuneteller, shows state_read + which charts were live + message snippets |
| 🧪 Sandbox | Talk to a test instance of Clar (`_buildTestPrompt()` — a simplified rebuild of `_buildSystemPrompt()` using live `AI_ROWS`, same Worker/model) to check a chart's wording actually works — never logged to `admin_insights`, never a real session |
| 🧭 Consultant | A *separate* Gemini call (`_buildConsultantPrompt()`) given the full AI config + last 30 `admin_insights` rows as context; the owner can ask it things like "where are we inefficient" and get config-review answers grounded in the actual live text and actual logged interactions |

**✅ Security fix — deployed, activated, and verified (2026-08-04).** `admin.html`'s password gate was removed at the owner's request to iterate faster (see `db/schema_v160.sql` comments), which required opening `ai_context`/`admin_commands`/`admin_insights`/`tools`/`chargers` write (and for `admin_commands`/`admin_insights`, read) access to the `anon` role — meaning anyone who extracted the public anon key could read/write Clar's live prompts and the debug log. Fixed with `workers/admin-relay-worker.js` (deployed at `https://clarvoyance-admin-relay.smworkassistance.workers.dev/`, holds a Supabase `service_role` key server-side, token-gated) — `admin.html` now calls it for all 8 write/read operations on those tables instead of using its own anon key. `db/schema_v178_lockdown_admin_write.sql` has been run, revoking exactly the anon grants opened in v160/v161/v170. Deployment surfaced one extra real issue not caused by this change: `service_role` had no explicit grants on these 5 tables at all on this project (unrelated to the anon lockdown — discovered via "permission denied" errors even with a confirmed-correct, correctly-signed `service_role` key), fixed by `db/schema_v178a_fix_service_role_grants.sql`. Verified via curl against the live production project, both directions: the anon key now gets 401 on `ai_context` PATCH, `tools` UPDATE, `admin_commands` SELECT, and `admin_insights` SELECT; `ai_context` SELECT (200) and `admin_insights` INSERT (201) — both required by the main app — still work; and `admin.html`'s worker path (upsert/delete on `ai_context`, `admin_commands.select`) still succeeds end-to-end post-lockdown.

**Follow-up audit (same session, 2026-08-04):** checked every other table for the same class of issue by grepping all `GRANT` statements across `db/*.sql` rather than assuming. Found one more real leak: `schema_v164_fix.sql` had granted the `authenticated` role (not just `anon`) unrestricted SELECT on `admin_insights` (`USING (true)`, no row scoping) — and per that same file's own finding, every real app user is always `authenticated` (auto-signed-in anonymously), so any real user could query `admin_insights` directly and read every other user's logged chat/Fortuneteller snippets. Fixed by `db/schema_v178b_fix_admin_insights_authenticated_leak.sql` (revokes that SELECT, leaves `authenticated` INSERT alone). Storage buckets (`vision-images`, `revise-images`) and all 8 `user_*`/`user_daily_log`/`user_practice_log` tables were checked too and are correctly scoped (`auth.uid()`-restricted or folder-path-restricted) — no changes needed there for security. Separately (a functionality bug, not security): a real anonymous-session test found `user_daily_log` doesn't exist in PostgREST's schema at all (`schema_v126.sql` evidently never actually ran) and `user_practice_log` exists but is missing `anon`/`authenticated` grants entirely (same root-cause class as the `service_role` gap above) — both silently broken for real users this whole time. Fixed by `db/schema_v178c_fix_daily_practice_log.sql` (recreates `user_daily_log` idempotently, grants both tables, forces a PostgREST schema reload).

---

## Deliberately Deferred (not built yet)
- Challenges sheet (40 research-backed challenges) — Apps Script SHEET_NAMES needs `challenges` added
- Quest system linked to challenges sheet
- Mood-based card filtering (charger_rules sheet)
- Remove legacy tool HTML files (`tools/clarity.html` etc.) — keep until confirmed stable

---

## How to Add New Content (no code needed)
| Want to add | Just do this |
|-------------|-------------|
| New vibe card | Add row to `vibe_cards` sheet |
| New charger | Add row to `chargers` sheet |
| New tool | Add row to `tools` sheet |
| Turn off a tab | Set `active=FALSE` in `modules` sheet |
| New quote | Add row to `quotes` sheet |


*Last updated: 2026-09-06 — v227 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v226`→`clv-v227`) — two real owner-reported bugs plus a full Fortune-tab font/color audit. Fixed "Friend" showing instead of the real name even after Google sign-in (and leaking into Vibe Feed cards): root-caused to an OLDER "Friend" placeholder sentinel that predates v217's "Guest" one, which `_syncGoogleNameIfNeeded()`'s guard never recognized — fixed in two places for defense in depth, live-verified the normalization actually happens on next boot. Then bumped every remaining small/dim CSS text in Fortune/Pulse/Insights to a more readable floor, brightened every low-opacity purple text color to a solid bright purple, and — the biggest real gap — bumped every canvas-drawn graph label (Pattern Map + all Insights charts), which no earlier CSS pass had ever touched since it's JS `ctx.font`, not CSS. Live-testing that surfaced a real regression (the bigger font exposed a pre-existing "Resistance" label clipping off the radar's left edge) — fixed properly with position-aware text alignment rather than shrinking the font back down, re-verified clean via screenshots. See the v227 entry above for the full trail.*

*Previously — 2026-09-06 — v226 promoted to `index.html`/`sw.js` together with v225 (`sw.js` `CACHE_VERSION` `clv-v224`→`clv-v226` — v225 was never promoted on its own, so this push carries both) — the exact slot the old "Oracle" title used to occupy now shows a live, no-AI gamified status word (Seeker/Warrior/Champion/Sovereign) driven by whole-app time averaged over the last 3 real logged days, per explicit owner design (discussed first: whole-app not chat-only, bright dark-background-friendly colors, Sovereign gets extra glow). Live-verified: seeded four different 3-day averages and confirmed each produced the exact expected word and color. See the v226 entry above for the full trail.*

*Previously — 2026-09-06 — v225 built as `clarvoyance_v225.html` ONLY, not promoted (owner asked to keep it isolated) — a "discuss first" design conversation about the app's core belief (consistent Clar usage tracks with how well someone's life is going) landed as a scoped, evidence-gated build: `db/schema_v225_admin_insights_user_id.sql` (owner must run) adds `user_id` to `admin_insights`, a real prerequisite gap found while investigating — that table logging `state_read` on every AI reply had no way to attribute a row to a person at all. New `window._computeUsageOutcomeCorrelation()` compares each real day's emotional read against that same person's own engagement over the *preceding 3 days* (recency-weighted per the owner's own instinct that mood shifts with Clar usage fast), feeding Fortune two new prompt blocks: cites the real evidence with confidence only when it genuinely confirms for that account, and separately always tells anyone with low/inconsistent engagement that occasional use won't produce real results — framed forward, never as blame for the past. New admin.html "📈 Usage vs State" tab (manual per-email check, green/red) plus a matching `usage_correlation.compute` worker action. Verified the algorithm itself via 4 isolated synthetic tests — critically, confirmed it correctly reports `confirms:false` on a genuinely inverted pattern rather than forcing a positive conclusion regardless of the data. Philosophy text for `ai_context` drafted for the owner to review and paste in themselves, not auto-written. See the v225 entry above for the full trail.*

*Previously — 2026-09-06 — v224 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v223`→`clv-v224`) — Fortune's colors switched from a decorative rainbow to an actual red/amber/green severity scale (energy tiers, the AI headline via its own `state_read` field, the concern card, Pulse's mood chip), the tooltip's "you're just beginning" copy rewritten so it stops contradicting a concurrent AI reading of real distress, and a matching green "good news" treatment added to 5 of the Insights panel's 7 charts. Also normalized font sizes across the whole Fortune/Pulse/Insights surface toward the Reading section's own readable size, per explicit owner instruction ("we want to read them"). Live-verified via deterministic cache injection (exact color-per-state confirmed) and direct rendering checks before promoting; a live Supabase seed for the green-bullet scenario hit an unrelated test-session RLS hiccup, noted honestly rather than glossed over. See the v224 entry above for the full trail.*

*Previously — 2026-09-06 — v223 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v222`→`clv-v223`) — a same-day follow-up after the owner reviewed v222's real numbers and flagged that a flat/inactive week is the single most important thing this app should catch, not something to stay silent about. Fixed the Insights panel's tie-breaking bug (declaring a false "winner" between two identical zeros) by having every one of the 7 charts detect genuine total inactivity and show a direct red-bold warning instead of either a false claim or silence; also fixed the AI prompts' "NN" label being misread as a navigable section. Live-verified on a real account both ways before promoting: seeded a flat all-zero week → all 7 warnings fired correctly in red bold, zero errors; re-seeded the same realistic mixed week used earlier → bullet text came back byte-for-byte identical to before the fix, confirming no regression. See the v223 entry above for the full trail.*

*Previously — 2026-09-05 — v222 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v221`→`clv-v222`) — Fortune tab restructured per explicit owner feedback that everything (AI reading, Pattern Map radar, Pulse, Insights charts) was stacked open at once and the existing 4 Insights charts felt "dead"/uninsightful. Fortune now opens to 4 closed-by-default buttons (Reading / Pattern Map / Pulse / Insights & Charts); "Oracle" title removed (subtitle carries the header alone, animated background kept as-is); 3 new simple X-vs-Y charts added (Best/Worst Day of Week, Time Spent vs. Score scatter, Minutes by Section); plain code-computed (non-AI) inference bullets added under all 7 Insights charts. Live-verified via a raw-CDP browser check (chrome-devtools MCP bridge was down; drove Chrome's debug port directly, same fallback as v214) before promoting — all 4 buttons, Pulse's sparkline redraw-on-open, and all 3 new charts confirmed rendering correctly with zero JS errors. See the v222 entry above for the full trail.*

*Previously — 2026-09-01 — v221 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v220`→`clv-v221`) — added a second floating quick-access button, "Peak State" (⚡, orange/red), stacked below the existing v183 Quick-capture pencil on the same left-edge dock. Deliberately reuses that button's exact mechanics (full-screen overlay, discard-on-close, "Ask Clar ✦" through the real send pipeline) per explicit owner request — only the color and the placeholder text differ, the latter steering toward specific empowering self-talk (positivity/possibility, unshakeable faith, courage, power, invincibility) the owner described. The pencil's own CSS rule was left untouched; the new button has its own independent position rule instead. The existing keyboard-focus-hide hook was extended to cover both buttons. Live-verified in a real Chrome instance: correct stacked placement, overlay opens with the right placeholder/title, word count updates, text genuinely discarded on close/reopen (not restored), "Ask Clar" round-tripped through a real Gemini reply, and the keyboard hide/restore hook covers both buttons together. See the v221 entry above for the full trail.*

*Previously — 2026-09-01 — v220 promoted to `index.html`/`sw.js` (`sw.js` `CACHE_VERSION` `clv-v219`→`clv-v220`) — owner-requested layout change: Non-Negotiables moved from the Self tab to Home, taking Clar Commitment's old top spot; Clar Commitment removed from view (markup/functions left in place, not deleted, matching this file's own established convention). `#nn-tab-section` was physically relocated out of Self tab's shared accordion to become its own standalone `.self-acc`-styled card (the same reuse technique `#goal-row` already uses), landing at `order:2` in Home's flex layout — interestingly reviving a `order:10` CSS rule that had been silently dead since NN got nested inside Self's accordion in an earlier version. Added a short always-visible line under the title: "The basis of a well-formed life." Checked every place that could have assumed NN's old nested position (`selfToggle`, `renderNNTab`'s call sites, the Profile tab's hide-list, Global Search's index) — all either position-independent or never referenced NN in the first place. Not live-tested this round (Chrome's debug port was unreachable all session) — shipped on thorough static verification instead. See the v220 entry above for the full trail.*




