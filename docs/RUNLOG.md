# RUN LOG

Append-only. Newest at the bottom. Written by `node ops/tasks.js log` and by the agent.

## Carried over from TASKS.md v1

(Claude appends here as it works, so an interrupted run is always resumable)
- 2026-09-24 05:16 run started. clarvoyance_v246.html created from index.html (hash identical to v245).
- 2026-09-24 05:25 T-002 DONE + committed (7359366).
- 2026-09-24 05:31 T-003 code done + committed (f5599aa); BLOCKED only on owner running db/schema_v246_clar_posts.sql.
- (times above/below are real clock times from git; an earlier draft of this log had wrong ones)
- 2026-09-24 05:38 T-009 DONE + committed (9750142).
- 2026-09-24 05:48 T-006 built (commit 570e8dd) + tested with mocks (BLOCKED on owner steps). Next: final full regression pass on v246 + test-data cleanup in Supabase.
- 2026-09-24 05:55 FINAL REGRESSION PASS on v246 (rule 11), fresh isolated Chrome context, 390x844: all 48 script blocks parse; app boots; onboarding chain shows Life Quiz only and dismisses; all 6 bottom-nav tabs open and their section is visible (Self, Home incl. Non-Negotiables plate, Goal, Fortune, Vibe with a rendered card, Clar AI); Profile opens; Clar chat sent a message and a real AI reply came back ("Haha, yup, still here and ready to chat…"); Community opens (signed-out shows the Google gate). Console errors on a full session = 4, none from v246: (1) 403 admin_insights = pre-existing, see T-010; (2) 3x placeholder `replace_with_your_photo_worker_url` = known since v216 (Pixabay photo Worker not deployed); (3) 404 on one YouTube thumbnail. index.html hash unchanged from the start of the run (8C69045…) and sw.js untouched. `#prof-app-ver` label in v246 set to v246 (sw.js CACHE_VERSION deliberately NOT bumped — only bump on promotion).
- 2026-09-24 05:57 TEST DATA CLEANED: disposable test account (uid 8e8c1c8c-40ec-43e9-8d32-ba6514c3c4a8, handle t002test4629) — deleted its social_reactions/comments/achievements(incl. id 9)/activity/follows/profile; verified 0 rows remain. Not deletable by me (locked/normal visitor footprint): admin_insights chat-log rows and normal per-visitor user_* rows of the two anonymous test visitors — same footprint any real visitor leaves.
- RUN COMPLETE. Owner steps outstanding: (a) run db/schema_v246_clar_posts.sql [T-003]; (b) Bunny account + db/schema_v246_video.sql + Worker deploy + paste URLs [T-006]; (c) connect Supabase MCP (read-only) if wanted; (d) decide provider confirmation for video, nav layout build (T-004/T-005/T-008), invites/circles (T-007), T-010; (e) real-phone check of T-009 video; (f) promote v246 to index.html/sw.js only when satisfied.

- 2026-09-25 REAL-ACCOUNT TEST of T-006 (owner explicitly asked: test with the Google-signed-in Chrome; localhost copy of v246 with OAuth redirect changed to http://localhost:8787/, real Bunny+Worker+Supabase). VERIFIED FOR REAL: Worker minted a ticket for the signed-in account (200); real resumable upload of a 1.1 MB clip to Bunny succeeded; Bunny transcoded it (HLS master playlist 240/360/480p, thumbnail.jpg 200); ownership trigger accepted a post pointing at my own video and REFUSED one pointing at an invented guid ("video does not belong to this member"); social_achievement_feed has the new video column; Bunny domain rule blocks a foreign Referer (403) and allows clar.co.in; real HLS playback in the Reels viewer (854x481, advancing); MP4 fallback ladder works (play_720p 404 -> play_480p played). NOT DONE: an end-to-end post through the compose UI, because the owner account has no Community profile yet (joining would create a public profile — not done without asking). FOUND+FIXED in v246: (1) MP4 fallback hard-coded play_720p which does not exist for sub-720p sources -> now tries 720/480/360/240; (2) PWA install card (z 9999) covered Community/Reels when it had slid up before opening -> hidden while Community is open (body.soc-open). CLEANUP: test posts deleted (0 left). LEFT BEHIND: 1 ledger row + 1 Bunny video (guid d58e1ea0-3f8b-4f54-8c61-45d5f59221d8, title clar-e17e5639-…) — owner may delete the video in Bunny dashboard > library > Videos; harmless if kept. Bunny note: requests with NO Referer are allowed, so the domain list only stops casual hotlinking.

- 2026-09-25 T-006 FULL END-TO-END with the owner Community profile (handle videh) via the compose UI, localhost copy of v246, real Bunny/Worker/Supabase: Create post -> pick video -> preview -> Post -> Worker ticket 200 -> TUS 201/204 -> social_achievements insert 201 -> feed shows poster + 0:05 badge + real Bunny thumbnail -> tap -> Reels viewer plays real HLS (native in this Chrome, 854x481, advancing, real display name + caption). FOUND+FIXED: right after upload Bunny is still encoding so thumbnail.jpg 404s -> poster now shows a soft "Processing video…" state and retries every 6 s (up to ~2 min); viewer shows "Still processing — try again in a minute" if every source 404s (both verified with a fake guid). Test post deleted (0 posts left). Owner-visible residue: 2 test videos in Bunny (guids d58e1ea0…, 2f5490b0…) + 2 ledger rows — harmless, deletable in Bunny dashboard. T-006 code status: DONE pending owner decision on rollout (Route A all users / Route B ?video=1 first).

- 2026-09-25 PROMOTED v246 -> index.html + sw.js clv-v246 (owner: "A is fine… fallback to previous version if problems"). T-003 SQL confirmed run by owner (7 Clar posts live) -> DONE; T-006 -> DONE (video live for all signed-in members). Pre-promotion regression on final file (fresh isolated Chrome, 390x844): 48 script blocks parse; all 6 tabs + Profile open; Vibe card renders; Clar chat sends and a real reply came back; Community shows the sign-in gate when signed out; console errors = 2 known pre-existing (403 admin_insights T-010, photo-worker placeholder). ROLLBACK: copy clarvoyance_v245.html over index.html and bump sw.js to a new cache name (clv-v247).

