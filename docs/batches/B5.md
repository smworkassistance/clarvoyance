# BATCH CONTRACT — B5   (frozen 2026-09-25, IST; the scope hash is stored in tasks.json)

**Outcome:** the owner wakes up to (1) the video-upload "stuck" problem fixed, (2) the dead usage-vs-state feature repaired (needs one owner SQL step), and (3) the decided new navigation built and dark-launched behind a flag — with the live app unchanged for everyone until the owner flips the flag.
**Requested by owner (2026-09-25 night):** fix the video upload hang; fix the pending tasks as Claude sees fit; everything in a NEW version file; nothing existing may break; document everything; anti-stuck rule; infra documented and reusable.
**Time box:** 8 h.   **Run mode:** dark launch — flag `nav_v2` default OFF.   **Rollback point:** tag `pre-v247` created at promote.
**Candidate file:** `clarvoyance_v247.html` (copy of live v246 + this batch). Live entry untouched until T-013.

## Tasks (dependency order)
| ID | Task | Depends on | Risk |
|----|------|-----------|------|
| T-011 | Video upload: chunked progress, background chip, cancel, retry, stall notice | — | medium (touches the post flow of Community) |
| T-010 | Usage-vs-state correlation moved server-side (SQL function + rpc) | — | low (client soft-fails) |
| T-012 | OWNER STEP: run the SQL of T-010 | T-010 | none — will be BLOCKED by design |
| T-005a | `nav_v2` shell: new 6-button nav behind a flag | — | medium (bottom nav is central) — flag OFF ⇒ byte-identical behaviour |
| T-005b | Feed tab hosts Community as a tab, sub-tabs Following/Discover/Board | T-005a | medium |
| T-005c | You tab = Profile + Fortune + My Community profile entries | T-005a | low |
| T-005d | Self plate under Non-Negotiables on Home | T-005a | low |
| T-013 | Promote v247 (dark) with rollback tag + push, confirm live | T-011, T-010, T-005b/c/d | — |
| T-014 | Release documentation | T-013 | — |

## Explicitly NOT in this batch
- **T-008** Fortune theme match, **T-004** flipping the flag for users (owner's decision), **T-007** invites/circles, groups, DMs, location discovery.
- Any change to the default (flag-OFF) look or behaviour of existing screens.
- Any Supabase write from Claude; SQL is provided, the owner runs it.

## Design decisions taken (defaults, no further questions)
- nav_v2 ON only via `feature_flags.nav_v2` **or** the dev URL override `?nav=2` (`?nav=0` clears). Missing flag row ⇒ OFF.
- "You" hub v1 = existing Profile section + a top card with entries to Fortune and My Community profile (moving Fortune's canvases into Profile is deliberately not done — too risky for a dark v1).
- Feed-as-first-page (T-004) = flipping the flag + choosing the start tab is a later, separate owner decision.

## Owner actions that will appear as BLOCKED
- T-012: run `db/schema_v247_usage_correlation_rpc.sql` (Supabase SQL editor). Optional: `insert into feature_flags(key,enabled) values ('nav_v2', true)` when ready to activate the new nav.

## Verification plan
- Every task: `QA_TARGET=clarvoyance_v247.html node ops/verify.js` (scripts parse, browser suite Chromium/Android; WebKit lane on CI), plus the task's own Done-when evidence.
- New Playwright specs are added per task (ratchet): `video-upload.spec.js`, `nav-v2.spec.js`; T-010 gets a SQL-vs-JS equivalence test on a real Postgres engine (pglite).

## Promote plan
- After all tasks are DONE/BLOCKED and a final full verify passes: `node ops/promote.js clarvoyance_v247.html --push` (tag `pre-v247`), then confirm the live site (curl label + sw cache).
- Rollback: `node ops/rollback.js pre-v247 --push`.
